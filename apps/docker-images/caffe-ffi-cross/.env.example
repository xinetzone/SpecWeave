# =============================================================================
# caffe-ffi 跨平台编译环境变量配置
# =============================================================================
# 使用方法：复制此文件为 .env，根据需要修改配置
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# 镜像源配置（国内用户建议使用国内镜像加速）
# -----------------------------------------------------------------------------
# apt 镜像源：official / aliyun / tsinghua / ustc 等
APT_MIRROR=official

# conda 镜像源：official / aliyun / tsinghua / ustc 等
CONDA_MIRROR=official

# pip 镜像源：official / aliyun / tsinghua / ustc 等
PIP_MIRROR=official

# -----------------------------------------------------------------------------
# macOS 交叉编译配置
# -----------------------------------------------------------------------------
# 跳过 macOS SDK 下载（用于离线场景）
# 设置为 1 时需提前将 SDK 放在 ./macos-sdk 目录，或通过 MACOS_SDK_PATH 指定路径
SKIP_SDK_DOWNLOAD=0

# 本地 macOS SDK 路径（默认 ./macos-sdk）
# MACOS_SDK_PATH=/path/to/MacOSX11.3.sdk

# -----------------------------------------------------------------------------
# Windows 交叉编译配置
# -----------------------------------------------------------------------------
# 跳过 Wine 安装（可加速镜像构建，但无法运行 Windows 测试）
SKIP_WINE=0

# 跳过编译后的 Wine 测试
SKIP_WINE_TEST=0
