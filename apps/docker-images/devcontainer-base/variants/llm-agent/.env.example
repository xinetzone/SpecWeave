# llm-agent 变体环境变量
# 复制为 .env 后可覆盖默认值（build.sh --build-arg 优先于 .env）

# 基础镜像标签
BASE_TAG=latest

# 镜像源选择
APT_MIRROR=official
CONDA_MIRROR=official
PIP_MIRROR=official

# llm-agent 变体无额外构建参数
# LLM SDK API keys 通过运行时 -e 传入（OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY）
# 所有 Python 包使用最新版本，不固定版本号
