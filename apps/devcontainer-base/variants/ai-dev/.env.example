# ai-dev 变体环境变量
# 复制为 .env 后可覆盖默认值（build.sh --build-arg 优先于 .env）

# 基础镜像标签
BASE_TAG=latest

# 镜像源选择
APT_MIRROR=official
CONDA_MIRROR=official
PIP_MIRROR=official

# ai-dev 变体无额外构建参数
# 所有 Python 包使用最新版本，不固定版本号
# 如需固定版本，可在 Dockerfile Stage 2 中修改 pip install 命令
