# onnx-quantized 变体环境变量
# 复制为 .env 后可覆盖默认值（build.sh --build-arg 优先于 .env）

# 基础镜像标签
BASE_TAG=latest

# 镜像源选择
APT_MIRROR=official
CONDA_MIRROR=official
PIP_MIRROR=official

# 量化工具版本（可选，固定版本时取消注释）
# NEURAL_COMPRESSOR_VERSION=2.6
# ONNXCONVERTER_COMMON_VERSION=1.14.0
