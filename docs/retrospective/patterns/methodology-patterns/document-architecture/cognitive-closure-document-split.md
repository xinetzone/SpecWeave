---
id: "cognitive-closure-document-split"
source: "docs/retrospective/reports/project-reports/retrospective-mdi-project-completion-20260702/insight-extraction.md"
x-toml-ref: "../../../../../.meta/toml/docs/retrospective/patterns/methodology-patterns/document-architecture/cognitive-closure-document-split.toml"
---
> **提炼自**：[insight-extraction.md](../../../reports/project-reports/retrospective-mdi-project-completion-20260702/insight-extraction.md) —— MDI复盘文档原子化演进（洞察13）

# 认知闭环划分原则（Cognitive Closure Document Split）

## 模式类型

方法论模式（文档架构/文档工程）

## 成熟度

L1 首次提炼（MDI复盘6个文件合并验证）

## 适用场景

技术文档拆分、复盘报告组织、知识库原子化、论文/报告结构设计；当你需要判断"哪些内容应该放在同一个文件"时。

## 问题背景

文档原子化的核心难题：边界在哪里？
- 代码可以按"功能职责"清晰拆分（一个函数做一件事，一个类负一个责）
- 文档的"单一职责"边界非常模糊：按主题分？按阶段分？按读者分？
- 错误的拆分方式（机械按主题/阶段拆分）会导致：跨文件重复内容、读者需要在多个文件间跳转才能理解一个完整主题、维护困难

## 核心思想

**代码按"功能职责"拆分，文档按"读者认知闭环"拆分——"事实→分析→洞察→结论→行动"构成一个完整思考单元，不应强行按阶段/主题拆分。**

### 认知闭环单元

一个完整的认知闭环包含五个环节：

```mermaid
graph LR
    F["事实(Facts)"] --> A["分析(Analysis)"]
    A --> I["洞察(Insights)"]
    I --> C["结论(Conclusions)"]
    C --> AC["行动(Actions)"]
    style F fill:#e1f5fe
    style A fill:#fff9c4
    style I fill:#c8e6c9
    style C fill:#ffccbc
    style AC fill:#d1c4e9
```

当读者阅读一个主题时，自然期望经历完整的思考链条——看完事实想知道分析，看完分析想知道结论，看完结论想知道怎么做。强行把这些环节拆分到不同文件，会破坏阅读流，制造重复（每个文件都需要重新交代上下文）。

### 三层文档结构

遵循认知闭环原则，文档体系自然收敛为三层结构：

| 层级 | 文件类型 | 职责 | 闭环要求 |
|------|---------|------|---------|
| L1 索引层 | README/目录 | 导航、文件清单、阅读顺序建议 | 自身是完整闭环（告诉读者怎么读） |
| L2 事实层 | 事实数据、客观记录、过程日志 | 只陈述客观事实，不做分析判断 | 每个事实文件是独立的事实单元 |
| L3 思考层 | 完整复盘/洞察报告/建议文档 | 整合事实→分析→洞察→结论→行动 | **必须包含完整认知闭环** |

## MDI复盘验证

| 错误拆分（按阶段） | 后果 | 正确结构（按认知闭环） |
|------------------|------|---------------------|
| 02-phase1-analysis.md（只有分析） | 需要重复引用01中的事实 | 00-execution-overview.md（L1索引） |
| 03-phase1-insights.md（只有洞察） | 洞察缺少分析支撑 | 01-phase1-facts.md（L2事实层：客观数据） |
| 04-phase2-atomization.md（分析+洞察） | 与02/03内容重复 | insight-extraction.md（L3思考层：分析+洞察+结论+行动完整闭环） |
| 05-project-conclusion.md（只有结论） | 结论缺少前面的论证 | |
| 06-export-overview.md（状态记录） | 应该作为结论的一部分 | |
| 07-improvement-recommendations.md（行动） | 行动缺少洞察和结论支撑 | |

6个文件合并为insight-extraction.md后，读者可以从"阶段一过程分析"一直读到"后续行动计划"，形成完整的阅读流，无需跳转。

## 核心规则

### 规则1：L3思考层文件必须包含完整闭环

一个完整的思考类文档必须包含五个要素，缺一不可：

1. **事实/背景**：支撑分析的客观数据和过程描述（可以引用L2事实文件，但要在文内提供足够上下文）
2. **分析**：对事实的解读、趋势识别、异常发现
3. **洞察/模式**：从分析中提炼的可复用经验
4. **结论**：整体判断、核心发现总结
5. **行动**：基于结论的具体可执行建议

如果缺少任何一环，读者就会产生"然后呢？""所以呢？""怎么做？"的疑问。

### 规则2：L2事实文件只陈述事实，不做判断

事实层文件的职责是客观记录：
- 发生了什么
- 量化数据
- 时间线
- 关键事件

不要在事实文件中加入分析、结论或建议——那些属于L3思考层。

### 规则3：识别伪拆分（应该合并的信号）

当出现以下信号时，说明拆分是错误的，应该合并：
- 多个文件开头需要大段重复的背景介绍
- 读者需要同时打开3个以上文件才能理解一个主题
- 同一个洞察/结论在多个文件中重复叙述
- "下一章"链接的内容是理解当前章节的必要前提（不是可选参考）
- 新增内容不知道该放A文件还是B文件，边界模糊

### 规则4：合并时保留导航，消除重复

合并不是简单的文本拼接：
- 消除重复的背景介绍，只保留一份
- 统一术语和表述
- 调整章节顺序，形成线性阅读流
- 更新导航链接，确保跳转到正确位置（可以使用锚点链接到具体章节）

## 实施检查清单

**拆分前**：
- [ ] 这个文件是L1索引、L2事实还是L3思考？
- [ ] 如果是L3思考层，是否能在一个文件内承载完整闭环？
- [ ] 拆分后每个文件是否有独立存在的价值？

**拆分后**：
- [ ] 是否有文件大段重复其他文件的内容？
- [ ] 读者理解主题A需要同时打开几个文件？（理想：1个L3文件+必要的L2事实引用）
- [ ] 新增内容是否有清晰的归属？

**合并时**：
- [ ] 是否消除了重复的背景和上下文？
- [ ] 章节顺序是否符合"事实→分析→洞察→结论→行动"的阅读流？
- [ ] 导航链接是否更新到正确位置？

## 反例警示

| 错误拆分方式 | 为什么错 |
|------------|---------|
| 按项目阶段拆分（01分析/02洞察/03结论） | 人为切断认知闭环，每个文件都需要重复上下文 |
| 按文档长度机械拆分（每300行一个文件） | 不考虑内容逻辑，可能在一个论证中间切断 |
| 按"主题"拆分但主题边界重叠 | 新增内容无明确归属，产生重复 |
| 索引层包含大量分析内容 | 索引变厚，失去导航作用 |
| 事实层加入主观判断和结论 | 事实与观点混淆，可复用性降低 |

## 与现有模式的关系

- `document-atomization-u-curve.md`：U型曲线告诉我们"先拆后合是正常的"，本模式告诉我们"合并时按什么原则合"
- `progressive-doc-consolidation.md`：渐进式合并是实施本模式的具体策略
- `entry-container-separation.md`：入口（L1）与内容容器（L2/L3）分离的思想与三层结构一致
- `fact-statement-consistency-loop.md`：事实层（L2）需要保持一致性，不能前后矛盾