# projects 第一方子项目入口（可选区域）

本目录容纳第一方自有子项目（git submodule 管理，通过 gitlink 追踪，不可直接修改）。若项目不使用 submodule 结构，可删除本目录，并在根 `AGENTS.md` 的「四大顶层区域」表中删除 `projects/` 对应行。

## 角色

作为 projects 区域智能体路由入口，按「子项目路由表」进入对应子项目。