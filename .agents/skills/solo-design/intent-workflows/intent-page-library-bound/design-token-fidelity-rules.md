---
lane: library_bound
contract: token-fidelity
---

# Design Token Fidelity Rules

Use actual Library token names whenever a Library identity is active. Do not invent fallback token names when `actualTokenNameReference` is available.

Generic visual-experience rules may fill gaps only when the Library does not define the relevant token or component behavior.
