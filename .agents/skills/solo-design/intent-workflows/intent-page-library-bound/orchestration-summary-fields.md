---
lane: library_bound
contract: orchestration-summary-fields
---

# Library-Bound Summary Fields

Write `project.resolvedLane`, `designSource.libraryIdentity`, `designSource.actualTokenNameReference`, `designSource.componentPlan`, `designSource.tokenFidelity`, and page-level component usage evidence.

Existing edits from Library-bound projects must carry `sourceProjectLaneHeritage=library_bound`.
