---
lane: library_bound
contract: component-conformance
---

# Component Conformance Rules

When Library components are available, page HTML must preserve their structure, state model, spacing constraints, and semantic roles.

Do not flatten component nesting or replace Library patterns to satisfy generic visual rules. Component evidence wins over visual-experience defaults.
