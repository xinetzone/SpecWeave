---
lane: graphic_bitmap_first
contract: dispatch
---

# Graphic Asset Dispatch Contract

Required fields: `graphicStrategyGate=bitmap-first`, `bitmapFirstAllowed=true`, asset plan, prompt constraints, target canvas registration path, and image node metadata.

Forbidden fields: page HTML runtime guide, layout-static required assertions, and visual-experience creative authority.
