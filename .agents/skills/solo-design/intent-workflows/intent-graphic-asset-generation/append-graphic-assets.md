---
lane: image_asset_append
workflow: append
---

# Append Graphic Assets

Use this flow when generated or provided graphic assets must be added to an existing `.design` canvas.

Every kept image file must be registered as a `type: "image"` node. File-only output is allowed only when the user explicitly requests not to add assets to the canvas.
