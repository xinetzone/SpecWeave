---
lane: project_mutation
contract: orchestration-summary-fields
---

# Project Mutation Summary Fields

Write `project.resolvedLane`, `project.sourceProjectLaneHeritage`, affected page ids, derivation type, duplicate project path when applicable, variant axis/count, changed theme dimensions, and affected page/head refresh list.

When editing Library-bound or restore-derived projects, inherited constraints must be carried forward as read-only guardrails.
