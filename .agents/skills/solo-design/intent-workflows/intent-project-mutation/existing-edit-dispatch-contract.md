---
lane: existing_edit_add_comparison
contract: dispatch
---

# Existing Edit Dispatch Contract

Required fields: `sourcePageId`, `sourceHtmlSrc`, `derivationType`, `inPlaceEditAllowed`, `sourceProjectLaneHeritage`, and source page slice.

Comparison edit is default. In-place editing requires explicit user instruction.
