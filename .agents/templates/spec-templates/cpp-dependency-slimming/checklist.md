# [项目名称] C++ 依赖瘦身优化验证清单

> **模板版本**: v1.0 | 基于 Caffe→tvm-ffi 瘦身实践萃取
> **使用说明**: 在每个任务完成后勾选对应的检查项。使用 `programmatic` 标记的必须通过命令/代码验证，`human-judgment` 为人工审查项。

---

## 一、环境预检验证（Task 0）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 目标平台编译器可用 | `g++ --version` / `clang++ --version` | 版本满足 C++[XX] 要求 | ☐ |
| CMake 版本满足要求 | `cmake --version` | >= 3.18 | ☐ |
| 新依赖库可编译 | 编译新依赖库的 hello world | 编译运行成功 | ☐ |
| 必要系统依赖已安装 | 检查 [protobuf/BLAS/Threads 等] | 头文件和库文件可找到 | ☐ |
| 构建目录创建成功 | `cmake -B build` | 无错误 | ☐ |
| 跨平台风险已评估 | 检查 0.2 风险评估表 | 所有高/中风险有缓解措施 | ☐ |

## 二、API映射前置验证（Step 0.5）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 旧依赖使用完整扫描 | grep 命令输出 | 所有旧API使用点已记录 | ☐ |
| 每个旧API有新API映射 | 检查映射表 | 无遗漏 | ☐ |
| 每个映射有最小验证程序 | 编译运行验证程序 | 所有映射标记为"已验证" | ☐ |
| 语义差异已记录 | 检查映射表备注 | 存在语义差异的API有适配方案 | ☐ |
| 缺失API的兼容层方案已设计 | 检查设计文档 | 新库未覆盖的API有compat方案 | ☐ |

## 三、依赖闭包分析验证（Step 1）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 使用 -H/iwyu 获取 include 树 | 编译器输出 | 获得完整头文件依赖 | ☐ |
| 必需迁移文件清单完整 | 对照 include 树和 grep 结果 | 无遗漏，无多余 | ☐ |
| 不迁移文件清单明确 | 检查清单 | 边界清晰，无隐藏依赖 | ☐ |
| 目录结构已创建 | `ls [目标目录]` | include/src/compat/tests 目录存在 | ☐ |

## 四、旧依赖替换验证

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| [旧库1] 头文件引用完全移除 | `grep -rn "#include <[旧库1]" [目标目录]/include [目标目录]/src` | 返回空结果（compat/别名除外） | ☐ |
| [旧库2] 头文件引用完全移除 | `grep -rn "#include <[旧库2]" ...` | 返回空结果 | ☐ |
| [旧库1] 命名空间使用完全移除 | `grep -rn "[旧库1命名空间]::" ...` | 返回空结果（compat 层 using 别名除外） | ☐ |
| [旧库2] 命名空间使用完全移除 | `grep -rn "[旧库2命名空间]::" ...` | 返回空结果 | ☐ |
| CMake 中无旧库查找 | `grep -rn "find_package.*[旧库]" [目标目录]/` | 返回空结果 | ☐ |
| CMake 中无旧库链接 | 检查 target_link_libraries | 仅链接新依赖和系统库 | ☐ |
| 共享库无旧库动态依赖 | `ldd [共享库].so` / `dumpbin /dependents [共享库].pyd` | 不包含旧库 .so/.dll | ☐ |

## 五、兼容层（compat/）验证（Task 2）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| compat 头文件 header-only | 检查 compat/ 目录 | 无对应 .cpp 文件 | ☐ |
| compat 不引用旧库头文件 | grep 检查 compat/*.hpp | 无 #include <[旧库]> | ☐ |
| 日志宏功能正确 | test_logging.cpp | INFO/WARNING/ERROR 正常输出，FATAL 抛出异常 | ☐ |
| CHECK 宏功能正确 | test_check.cpp | 条件失败抛出异常，消息含文件/行号/条件 | ☐ |
| 智能指针别名正确 | test_compat.cpp | shared_ptr/unique_ptr/make_shared 行为符合预期 | ☐ |
| 线程同步原语正确 | test_thread.cpp | mutex/condition_variable/Barrier 工作正常 | ☐ |
| 字符串工具函数正确 | test_compat.cpp | split/trim/lexical_cast 返回正确结果 | ☐ |
| 计时器功能正确 | test_compat.cpp | Timer 计时结果合理 | ☐ |
| 随机数生成正确 | test_compat.cpp | rng_t 生成随机数分布合理 | ☐ |
| thread_local 封装正确 | test_compat.cpp | 线程局部存储各线程独立 | ☐ |

## 六、C++ 核心功能验证

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| CMake configure 成功 | `cmake -B build` | 成功找到所有依赖，无错误 | ☐ |
| 核心静态库编译（第一批） | `cmake --build build --target [核心库]` | 编译成功，0 errors | ☐ |
| 完整核心库编译 | `cmake --build build` | 所有文件编译成功 | ☐ |
| 绑定/FFI共享库编译 | `cmake --build build --target [绑定库]` | 编译成功，生成共享库文件 | ☐ |
| Blob/核心数据结构功能正确 | test_blob.cpp | 创建/变形/数据读写正常 | ☐ |
| 内存管理同步正确 | test_blob.cpp | 状态转换正确（CPU↔SYNCED） | ☐ |
| 工厂注册正确 | C++ 测试 | 可通过工厂创建所有迁移的类型 | ☐ |
| 核心对象 DAG 初始化正确 | test_net.cpp | 从配置文件初始化成功 | ☐ |
| 核心操作（Forward/推理）正确 | 端到端测试 | 输出结果形状正确，数值合理 | ☐ |
| 线程安全组件正确 | test_thread.cpp | 并发操作无死锁、无竞态 | ☐ |
| 异常传播正确 | test_check.cpp | 异常跨 FFI 边界正确传递，含回溯信息 | ☐ |
| 干净构建无回归 | 删除 build 目录重新编译 | 与之前结果一致 | ☐ |

## 七、绑定/FFI 模块验证（Task 6）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 旧绑定机制完全移除 | grep 检查绑定文件 | 无旧绑定 API 引用 | ☐ |
| 旧语言 C API 头文件移除 | grep 检查 | 如 Python.h/numpy 头文件已移除 | ☐ |
| 新绑定导出符号存在 | `nm -D [共享库]` / `dumpbin /exports` | 包含正确前缀的导出函数 | ☐ |
| 版本/模式函数可用 | FFI 调用 | 返回正确值 | ☐ |
| 核心对象创建/销毁正确 | FFI 测试 | 创建 handle 并正确销毁，无泄漏 | ☐ |
| 核心操作执行正确 | FFI 测试 | 结果正确 | ☐ |
| 数据输出返回跨语言容器 | FFI 测试 | 返回 Tensor/Array 等可访问数据 | ☐ |
| 数据输入写入正确 | FFI 测试 | 输入数据后结果符合预期 | ☐ |
| 零拷贝数据传递验证 | 内存地址检查 | 上层语言数组与 C++ 对象共享内存 | ☐ |
| 安全调用宏包裹所有导出函数 | 代码审查 | 所有导出函数体内有异常安全宏 | ☐ |
| 不需要的功能安全 stub | FFI 调用 | 如 HasNCCL 返回 false，不崩溃 | ☐ |

## 八、上层语言适配验证（Task 8）

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 模块 import 成功 | [语言] -c "import [模块]" | 无 ImportError | ☐ |
| 核心对象可实例化 | 创建核心对象 | 创建成功，不崩溃 | ☐ |
| 核心操作正常工作 | 执行核心功能 | 返回正确类型，结果正确 | ☐ |
| 数据属性可访问 | 访问对象属性 | 返回正确数据（numpy array等） | ☐ |
| 模式设置正常工作 | 调用模式设置函数 | 切换成功，无错误 | ☐ |
| 零拷贝数据访问 | 内存共享检查 | 修改上层数组后 C++ 数据同步变化 | ☐ |
| 对象析构正确 | 删除对象后检查内存 | 资源正确释放 | ☐ |
| 包配置含新依赖 | 检查 pyproject.toml/setup.py | dependencies 包含新依赖 | ☐ |
| API 向后兼容 | 运行现有上层代码 | 不需要大改即可运行 | ☐ |

## 九、核心功能覆盖验证（Task 5）

对迁移的每个功能模块（Layer/组件），验证其可用性：

| 模块 | 验证方式 | 预期结果 | 状态 |
|------|----------|----------|------|
| [核心模块1: 如 InputLayer] | 创建含该模块的网络/对象 | 初始化成功 | ☐ |
| [核心模块2: 如 Conv层] | 含该模块的核心操作 | 输出形状/数值正确 | ☐ |
| [核心模块3: 如 FC层] | 含该模块的核心操作 | 输出正确 | ☐ |
| [核心模块4: 如 激活层] | 含该模块的核心操作 | 激活函数效果正确 | ☐ |
| [核心模块5: 如 Pooling] | 含该模块的核心操作 | 输出正确 | ☐ |
| [核心模块6: 如 Softmax] | 含该模块的核心操作 | 输出和为1/概率分布正确 | ☐ |
| [核心模块7: 如 归一化层] | 含该模块的核心操作 | 归一化效果正确 | ☐ |
| [核心模块8: 如 拼接/切片层] | 含该模块的核心操作 | 维度正确 | ☐ |
| ... | ... | ... | ☐ |

## 十、不迁移组件的隔离验证

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 无 GPU/CUDA 代码（如 CPU_ONLY） | grep 检查 | 仅 NO_GPU stub 和条件编译 | ☐ |
| 无不需要的第三方IO库代码 | grep 检查 | 返回空结果 | ☐ |
| 无不需要的数据库后端代码 | grep 检查 | 仅基类，无具体后端实现 | ☐ |
| 无不需要的脚本绑定代码 | grep 检查 | 返回空结果 | ☐ |
| 无多GPU通信代码 | grep 检查 | 仅 stub 返回 false/disabled | ☐ |
| 无额外第三方库硬依赖 | 检查 target_link_libraries | 除白名单外无其他外部库 | ☐ |

## 十一、代码质量与规范验证

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 目录结构符合 include/src 分层 | 检查目标目录 | 头文件在 include/，源文件在 src/，结构清晰 | ☐ |
| 编译警告级别合理 | 构建日志 | 无新引入的编译警告（允许第三方库警告） | ☐ |
| 原始业务逻辑未破坏 | 对比原始代码与迁移代码 | 核心算法代码无修改（仅头文件替换） | ☐ |
| 无硬编码绝对路径 | grep 检查 | 无平台相关绝对路径硬编码 | ☐ |
| 所有文件使用 UTF-8 编码 | 检查文件编码 | 无乱码 | ☐ |
| LICENSE 文件存在 | 检查目标目录 | 许可证文件存在 | ☐ |
| 未使用 file(GLOB_RECURSE) | 检查 CMakeLists.txt | 源文件显式列出 | ☐ |
| C++ 标准正确设置 | 检查 CMakeLists.txt | CXX_STANDARD 设置为 [XX] | ☐ |

## 十二、端到端验证

| 检查项 | 验证方式 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 最小功能路径端到端 | 代码创建核心对象+执行操作 | 输出正确 | ☐ |
| 配置文件/模型加载 | 使用实际配置文件 | 初始化成功 | ☐ |
| 权重/参数加载（如适用） | 加载预训练参数 | 加载成功，数据正确 | ☐ |
| 数据输入通过标准方式设置 | 上层输入数据数组 | 结果合理 | ☐ |
| 多次操作一致性 | 相同输入多次运行 | 输出一致（确定性） | ☐ |
| 无内存泄漏 | 多次创建/销毁对象 + valgrind/ASan | 内存稳定，无泄漏报告 | ☐ |
| 现有测试用例运行通过 | 运行项目原有测试 | 测试通过，断言成功 | ☐ |
| 构建产物大小合理 | 检查 .so/.pyd 文件大小 | 比完整构建显著减小 | ☐ |
| 构建时间改善 | 对比构建时间 | 比原项目构建更快（无大头文件解析开销） | ☐ |

## 十三、反模式检查（避免已知陷阱）

| 反模式 | 检查方式 | 状态 |
|--------|---------|------|
| ❌ 未做环境预检就开始写代码 | 确认 Step 0 完成 | ☐ |
| ❌ "边猜边写"API映射，不做最小验证 | 确认 Step 0.5 完成 | ☐ |
| ❌ "我觉得需要哪些文件"正向猜测 | 确认 Step 1 使用工具做闭包分析 | ☐ |
| ❌ 兼容层未验证就开始迁移业务代码 | 确认 Task 2 测试全部通过 | ☐ |
| ❌ CMake 使用 GLOB_RECURSE 收集源文件 | grep CMakeLists.txt 无 GLOB_RECURSE | ☐ |
| ❌ "大爆炸式"一次性添加所有源文件 | 确认按 Task 3/4/5 分批增量添加 | ☐ |
| ❌ 不在每步编译验证，攒到最后一起编译 | 确认每个 Task 都有编译验证步骤 | ☐ |
| ❌ 在 Windows 上强行编译 Linux 原生项目 | 确认优先使用 WSL/Linux | ☐ |
| ❌ CHECK/LOG 宏语义理解错误（如 LOG(FATAL) 不抛异常） | 确认 AC-6 异常测试通过 | ☐ |
| ❌ 忘记在 FFI 边界加安全调用宏 | 代码审查所有导出函数 | ☐ |
| ❌ handle 模式忘记释放资源（new无对应delete） | 确认 Delete 函数正确释放内存 | ☐ |
