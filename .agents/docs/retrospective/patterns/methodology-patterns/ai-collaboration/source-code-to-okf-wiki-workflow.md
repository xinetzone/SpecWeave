---
id: "source-code-to-okf-wiki-workflow"
source: "spec:pyinvoke-okf-wiki"
maturity: "L1"
validation_count: 1
---
# 源码阅读→OKF Wiki生成工作流

## 模式类型

方法论模式 / AI协作 / 知识沉淀

## 成熟度

L1 已验证（1次验证，2026-08-21 PyInvoke v3.0.3 源码Wiki）

## 模式概述

系统化学习一个开源项目/库的源码并产出结构化的 OKF v0.2 规范 Wiki 教程时，采用 seven-concepts 方法论的 R→I→E→V→C 五阶段链路（源码阅读与事实采集→架构洞察与知识结构设计→批量生成OKF文档→独立审查与修复→模式萃取与沉淀），确保产出**事实可溯源、架构有洞察、格式合规、经得起验证**。区别于"直接读源码写文档"的常见做法，本模式先建立零推测的事实清单（R），再从事实中提炼架构洞察设计知识结构（I），按OKF规范分批生成文档（E），独立审查修复虚构内容和断链（V），最后沉淀通用模式（C）。核心价值是避免AI生成文档中最常见的**虚构API、结构混乱、信源缺失、无法复用**四大问题。

## 问题现象

直接让AI"读源码写Wiki"的常见失败模式：

1. **虚构API**：AI凭"合理推测"编造不存在的类、方法、参数（如本次实践中虚构的 `Response` 类），读者照抄代码必然报错。
2. **事实无溯源**：文档中的API声明、参数说明不指向具体源码位置，无法验证真伪，专业可信度不足。
3. **结构混乱**：概念文档的排列顺序凭AI直觉，没有从架构本质推导出学习路径，读者阅读体验差。
4. **格式不统一**：frontmatter字段缺失、交叉链接路径不一致、index文件遗漏，导致OKF合规检查失败。
5. **经验不可复用**：任务完成后只留下产出物，没有沉淀通用workflow和prompt模板，下次做同类任务仍从零开始。
6. **信源后置**：先写概念文档再补信源登记，导致sources字段指向不存在的文件，交叉引用断裂。

这些问题的共同根因是：把"读源码写文档"当作一次性内容生成任务，跳过了"建立事实基础"和"独立验证"两个关键环节，且没有对过程经验进行模式萃取。

## 解决方案

采用 R→I→E→V→C 五阶段链路，每阶段有明确产出与质量门：

### R阶段（Read/Retrospective）：源码深度阅读与事实采集

**目标**：通读源码核心模块，提取可验证的事实清单，零推测。

**步骤**：
1. 列出源码目录结构，识别核心模块文件
2. 逐个阅读核心模块，对每个模块提取事实（类名、方法签名、关键参数、数据流、继承关系）
3. 所有事实编号为 F-xxx，每个事实必须指向具体文件路径
4. 事实清单写入 `.trae/specs/<task-name>/facts.md`

**质量门**：事实中不出现"我认为"/"这是用于"/"设计目的是"等推断性表述，只记录"代码里有什么"。

**事实采集模板**：
```
F-001: <模块名> - <类/函数名> 定义于 <文件路径>，继承自 <父类>
F-002: <类>.<方法> 接受参数 (<参数列表>)，返回 <返回类型>
F-003: <模块A> 中的 <对象> 被 <模块B> 的 <方法> 引用，传递 <数据>
```

#### R阶段专项：大型 C/C++ 项目的头文件优先采集（2026-08-23 新增）

针对源码规模 >1000 文件、含头文件与实现文件分离的 C/C++ 项目（如 Apache TVM、TuyaOpen），R 阶段采用**头文件优先事实采集策略**——已在 2 个大型 C/C++ 项目验证（L2），完整模式见 [cpp-header-first-fact-collection.md](../research-knowledge/cpp-header-first-fact-collection.md)：

1. **头文件为 API 权威来源**：优先阅读 `include/` 下的头文件，提取类声明、函数签名、枚举类型、宏定义
2. **实现文件仅用于验证**：`.c`/`.cc` 仅用于验证函数存在性和理解内部数据流，不作为 API 声明的权威来源（可能包含静态函数或内部宏）
3. **构建系统文件作补充**：Kconfig/CMakeLists.txt 作为模块依赖关系和架构裁剪的补充信息来源（识别可选组件、条件编译分支、模块间链接依赖）
4. **V 阶段在头文件中 Grep 验证**：对每个类名/宏名在 `include/` 中验证存在性，并检查命名空间前缀；版本迁移期项目以最新 `.h` 头文件为权威

> **为什么要加这一步**：C++ 项目的 API 虚构风险显著高于 Python 和 Markdown 项目——AI 容易把训练数据中的旧版 API 当作源码中真实存在的新版 API（"版本迁移期的统计惯性"），如 TVM 案例中虚构的 `PackedFunc`/`TVMArgs`/`TVM_FFI_REGISTER_GLOBAL` 8 类 API 涉及 10 个文件。头文件权威性分层是消除该风险的最有效手段。

### I阶段（Insight）：架构洞察与知识结构设计

**目标**：从事实中提炼架构洞察，设计知识包的文档结构。

**步骤**：
1. 基于事实清单，提炼3-5个核心架构洞察（每个洞察包含四元组）：
   - **陈述**：一句话说清架构特征
   - **证据**：引用哪些 F-xxx 事实支持
   - **反常识**：为什么初学者会误解/这个设计的反直觉之处
   - **行动**：这对文档组织有什么指导意义
2. 设计知识地图：文档分组（入门/核心/高级）、依赖关系、学习路径
3. 确定每个概念文档覆盖哪些事实
4. 洞察写入 `.trae/specs/<task-name>/insights.md`

### E阶段（Extraction/Execution）：批量生成OKF文档

**目标**：按OKF v0.2规范生成所有文档。

**关键原则**：**信源先行、分批生成、Index最后写**。

**步骤**：
1. 创建Bundle目录结构：
   ```
   <bundle-name>/
   ├── index.md              # 根索引（含 okf_version frontmatter）
   ├── log.md                # 变更日志
   ├── concepts/             # 概念文档
   │   ├── index.md          # 概念索引（无frontmatter）
   │   ├── 00-xxx.md ~ NN-xxx.md
   ├── examples/             # 示例文档
   │   ├── index.md
   │   └── ...
   └── references/           # 信源登记
       ├── index.md
       └── <source>.md
   ```
2. **先生成 references/ 信源登记**（这是所有sources字段的指向目标）
3. 再分批生成 concepts/ 概念文档（每批5-7个，按学习路径顺序）
4. 然后生成 examples/ 示例文档（每个对应一个或多个概念）
5. **最后生成各级 index.md** 导航文件
6. 文档生成可通过 `general_purpose_task` 分批并行委派

**文档frontmatter模板**：
```yaml
---
type: <Concept|Example|Reference|Pattern>
title: <文档标题>
description: <一句话描述，30-80字>
tags: [<标签1>, <标签2>, ...]
generated: { by: <生成者标识>, at: <ISO8601时间> }
verified: { by: "process:seven-concepts-v", at: <ISO8601时间> }
status: <draft|stable|deprecated>
stale_after: <YYYY-MM-DD过期日期>
sources:
  - id: <信源ID>
    resource: /references/<source-file>.md
    title: <信源标题>
---
```

**文档内容规范**：
- 开头1-2段概述概念/示例是什么
- 使用 `##` 二级标题分节，不使用 `#`（留给文件标题）
- 代码块标注语言，API调用必须与源码一致
- 每个文档结尾有 `## 相关概念` 章节
- 交叉链接使用 `/` 开头的bundle-relative路径（如 `/concepts/02-task-basics.md`）
- 中文撰写，英文技术术语首次出现时括号注释

### V阶段（Verification）：独立审查与修复

**目标**：验证所有文档的格式合规性、链接有效性、事实准确性。

**检查清单**：
1. **结构检查**：目录结构符合OKF规范，无遗漏文件
2. **Frontmatter检查**：每个文档有完整的必填字段
3. **链接检查**：所有交叉链接目标文件存在
4. **事实溯源检查**：抽查关键API声明，确认可追溯到源码（用Grep验证）
5. **代码示例检查**：代码示例语法正确、API调用与源码一致
6. **Index完整性检查**：各级index.md列出所有对应目录的文件
7. **虚构API检测**：对文档中引用的每个类/方法，用Grep在源码中验证存在性

### C阶段（Commit）：模式萃取与沉淀

**目标**：将本次经验沉淀为可复用的模式文档和prompt模板。

**步骤**：
1. 回顾流程，记录顺利点和问题点
2. 萃取通用prompt模板（见下方）
3. 补充反模式和迁移验证
4. 模式存入 `.agents/docs/retrospective/patterns/methodology-patterns/ai-collaboration/`
5. 更新模式库索引

## 适用场景

### 适用目标用户

- ✅ **需要深度学习开源库的开发者**：系统化学习Python/JS/Go库，需要深入理解架构和API，产出可溯源的知识笔记
- ✅ **技术文档团队**：为团队内部库生成统一格式的Wiki，要求格式规范、可溯源、可审计
- ✅ **AI辅助知识沉淀实践者**：需要AI批量生成技术文档，但对事实准确性有严格要求，不能接受虚构API
- ✅ **开源项目贡献者**：需要为开源项目撰写官方/非官方中文文档，要求代码示例准确可运行

### 不适用场景与反目标用户

- ❌ **快速查阅API用法（反目标：临时使用者）**：只想快速查一个函数怎么调用的人，五阶段流程过于重量级——直接看官方文档或用AI问答即可，不需要建立事实清单和知识结构。
- ❌ **生成单篇说明文档（反目标：轻量需求者）**：只需要写一篇README或单文件说明，E阶段分批生成、V阶段独立审查都是过度工程——直接撰写效率更高。
- ❌ **超大规模源码（如Linux内核/Chromium）（边界场景）**：源码核心模块超过50个、总代码量超过50万行时，R阶段全量阅读不现实——需要调整为分层采样策略（先读架构文档→再读核心接口→最后按需深入模块），本模式的R阶段模板不直接适用。
- ❌ **闭源/无源码访问的项目（边界场景）**：只有编译产物或API文档、无法访问源码时，R阶段事实采集无法执行——应改用外部文档分析模式（如external-tech-article-learning-closed-loop）。
- ❌ **纯翻译/改写已有文档（反目标：内容搬运者）**：如果已有完善的官方文档，只是需要翻译或改写，本模式的R→I洞察提炼是多余的——直接用翻译/改写prompt即可。

## 实际案例

本次《PyInvoke v3.0.3 OKF Wiki》知识包生成：

| 阶段 | 产出 | 关键指标 |
|---|---|---|
| R | 70+ 条源码事实（F-001~F-070+） | 覆盖15个核心模块 |
| I | 3个核心架构洞察 + 知识地图 | 12概念+5示例的分组设计 |
| E | 23个OKF文档（12概念+5示例+1信源+4索引+1日志） | 5963行，分批3轮生成 |
| V | 虚构`Response`类修复 + 信源补全 + 链接验证 | Grep验证关键API存在性 |
| C | 本模式文档 + 通用prompt模板 | 沉淀为L1模式 |

## 失败案例

### 案例：虚构的Response类混入文档（PyInvoke实践）

**背景**：在E阶段生成 `concepts/11-advanced-patterns.md`（高级模式）时，文档中引用了一个名为 `Response` 的类，声称它是PyInvoke中用于配置MockContext的类，包含状态码、响应体等属性。文档给出了看起来很合理的代码示例：

```python
# ❌ 虚构的API——PyInvoke源码中根本不存在Response类
from invoke import Response, MockContext
mock = MockContext({Response(status=200, body='ok'): 'result'})
```

**发现过程**：V阶段执行事实验证时，用Grep在PyInvoke源码中搜索 `class Response`，结果为零。进一步搜索 `Response` 关键字，发现PyInvoke的 `MockContext` 使用正则表达式字典作为键，不存在任何名为 `Response` 的类。

**根因分析**：
1. AI在生成"HTTP测试"相关示例时，基于其他Python库（如requests/responses）的"合理印象"编造了 `Response` 类
2. E阶段生成高级概念文档时，距离R阶段事实采集已过了几轮对话，AI对事实清单的遵循度下降
3. 初始V阶段只检查了链接有效性和frontmatter完整性，未对代码示例做API真实性Grep验证

**修复**：将虚构的 `Response` 类替换为正确的 `MockContext` 用法（字典键为正则表达式，值为返回结果），并在V阶段检查清单中强制增加了第7项"虚构API检测：对文档中引用的每个类/方法，用Grep在源码中验证存在性"。

**教训**：AI在生成"看起来合理"的代码时非常危险——越是常见的编程模式（如HTTP响应对象），AI越容易凭训练数据的"统计惯性"编造不存在的API。V阶段的Grep验证不是"建议"而是"必须"。

## 早期预警信号

在执行本工作流时，如果出现以下信号，说明某个阶段可能出了问题，需要暂停并回退检查：

| 预警信号 | 可能问题 | 建议行动 |
|---|---|---|
| R阶段事实中出现"用于"/"目的是"/"设计为"等词 | 违反零推测原则，事实中混入了推断 | 回到R阶段，将推断移至I阶段，事实只保留"代码里有什么" |
| AI在E阶段说"根据常见模式"或"通常情况下" | AI正在凭印象编造内容，而非基于事实清单 | 暂停生成，要求AI引用具体F-xxx编号，无编号的声明不允许写入 |
| 文档中引用的类名/方法名在facts.md中找不到 | 可能出现了虚构API | 立即用Grep在源码中验证该标识符是否存在 |
| 某批生成的文档数量超过7个 | 上下文过载风险，质量可能下降 | 拆分为更小的批次，每批≤5个文件 |
| references/目录尚未创建就开始生成concepts/ | 违反信源先行原则 | 暂停，先完成references/信源登记 |
| index.md中包含frontmatter字段 | 不符合OKF规范（子目录index不应有frontmatter） | 移除frontmatter，仅根index.md可保留okf_version |
| 交叉链接中出现`../`相对路径 | 路径风格不一致 | 替换为`/`开头的bundle-relative绝对路径 |
| V阶段检查发现1个以上虚构API | E阶段事实遵循度不足，可能还有更多未发现的虚构内容 | 对所有文档执行全面Grep验证，而非只抽查 |

## 反模式

### 反模式1："跳过R阶段，直接让AI读源码写文档"

不给AI事实采集步骤，直接要求"读这个项目写Wiki"。后果：AI凭训练数据的"印象"写，可能包含过时版本的API、不存在的方法、错误的参数。**正确做法**：强制R阶段，逐模块提取编号事实，所有后续内容基于事实清单生成。

### 反模式2："信源后置——先写文档再补references"

先写concepts/和examples/，最后才写references/。后果：文档中sources字段指向不存在的文件，交叉引用断裂，后续修复成本高。**正确做法**：E阶段第一步就是生成references/信源登记，其他文档的sources字段统一指向已存在的信源文件。

### 反模式3："一次生成所有文档"

在一个prompt中要求AI生成全部20+个文档。后果：上下文过长导致质量下降，后期文档遗忘前期约定，格式不一致。**正确做法**：分批生成，每批5-7个文件，每批之间保持独立上下文。

### 反模式4："Index先写/边写边更新"

一开始就写好index.md，后续生成文档时不更新。后果：index遗漏新增文档，或列出不存在的文件。**正确做法**：所有内容文档生成完毕后，最后统一写各级index.md。

### 反模式5："不验证API真实性，相信AI的输出"

文档写完就交付，不验证代码示例中引用的API是否真实存在。后果：虚构API（如本次的`Response`类）混入文档，读者照抄报错。**正确做法**：V阶段用Grep对关键类名/方法名做源码存在性验证。

### 反模式6："产出物留在spec目录，不入库模式库"

任务完成后，通用模板和workflow留在`.trae/specs/`规划目录中，不沉淀到模式库。后果：下次做同类任务时无法复用，经验浪费。**正确做法**：C阶段将通用模式迁移到 `.agents/docs/retrospective/patterns/` 对应分类目录。

### 反模式7："交叉链接使用相对路径"

在文档中使用 `../concepts/xxx.md` 这类相对路径。后果：文件移动后链接断裂，路径不一致增加维护成本。**正确做法**：统一使用 `/` 开头的bundle-relative绝对路径（如 `/concepts/02-task-basics.md`）。

## 通用Prompt模板

### R阶段：源码阅读与事实采集

```
请帮我学习项目 `<源码路径>`，生成符合OKF v0.2规范的Wiki教程到 `<输出bundle路径>`，
遵循 `<规范文件路径>` 的格式要求。

【R阶段：源码阅读与事实采集】
1. 首先列出源码目录结构，识别所有核心模块文件
2. 逐个阅读核心模块源码，提取可验证的事实（类定义、方法签名、参数、数据流、继承关系）
3. 所有事实编号为 F-xxx，写入 `<spec-dir>/facts.md`
4. 每个事实必须指向具体文件路径，禁止推测和编造

事实采集完成后，将事实清单提供给我确认，然后进入下一阶段。
```

### I阶段：架构洞察

```
基于 facts.md 中的 <N> 个事实，提炼架构洞察和知识结构：

1. 提炼3-5个核心架构洞察，每个包含：
   - 陈述：一句话说清架构特征
   - 证据：引用哪些F-xxx事实
   - 反常识：初学者容易误解的点
   - 行动：对文档组织的指导意义

2. 设计知识地图：概念文档分组（入门/核心/高级）、文档依赖关系和学习路径

3. 确定文档清单：concepts/examples/references 各需要哪些文件

将洞察和知识地图写入 `<spec-dir>/insights.md`。
```

### E阶段：批量生成文档

```
请生成OKF规范的中文Markdown文档，遵循以下规则：

【格式规则】
- YAML frontmatter必须包含：type, title, description, tags, generated, verified, status, stale_after, sources
- sources: 指向 references/ 下已存在的信源文件（信源先行）
- 交叉链接使用 / 开头的bundle-relative路径
- 中文撰写，英文术语保留并首次解释
- 每个文档结尾有"## 相关概念"章节
- 代码块标注语言，API调用必须与 facts.md 中的事实一致
- 禁止虚构API或行为——拿不准的回到 facts.md 核对

【内容要求】
- 每个概念文档500-5000字，用 ## 分节
- 示例文档必须包含完整可运行代码
- 信源文档列出所有核心模块和版本信息

请生成以下文件：
<逐文件列出文件路径、type、title、description、应覆盖的事实编号>
```

### V阶段：独立审查

```
请对 `<bundle路径>` 下的所有文档执行独立审查：

1. 结构检查：目录结构是否完整，是否有遗漏
2. Frontmatter检查：每个文档是否有完整的必填字段
3. 链接检查：所有交叉链接的目标文件是否存在
4. 事实抽查：对文档中引用的每个类名/方法名，用Grep在 `<源码路径>` 中验证存在性
5. 代码检查：代码示例语法是否正确，API调用是否匹配源码
6. Index检查：各级index.md是否完整列出所有文件

输出检查报告，列出发现的问题，然后逐一修复。
```

## 检验标准

| 维度 | 检验点 |
|---|---|
| R阶段 | 事实清单无推断性表述、每个事实指向源码路径、核心模块全覆盖 |
| I阶段 | 洞察四元组完整（陈述/证据/反常识/行动）、知识地图有学习路径设计 |
| E阶段 | 信源文件先于其他文档生成、分批生成（每批≤7个文件）、index最后写 |
| V阶段 | 链接无断裂、无虚构API（Grep验证）、frontmatter字段完整、index无遗漏 |
| C阶段 | 模式文档含反模式（≥3个）、prompt模板可复用、模式入库到正确目录 |
| G3（模式） | 本模式含触发场景、核心步骤、反模式（≥5）、检验标准、迁移示例 |

## 跨场景迁移示例

将本工作流从"PyInvoke源码学习→OKF Wiki"迁移到其他知识沉淀场景：

- **学习其他Python库（如FastAPI/Click）**：R阶段读取核心模块→I阶段提炼中间件/路由/依赖注入等架构洞察→E阶段分批生成OKF概念/示例文档→V阶段Grep验证API→C阶段补充特定库的注意事项。
- **学习前端框架（如React/Vue）**：R阶段阅读核心API和hooks→I阶段提炼响应式/虚拟DOM/组件生命周期洞察→E阶段生成概念+示例→V阶段对照官方文档验证API签名→C阶段沉淀前端特有的模式。
- **内部代码库文档化**：R阶段阅读内部源码（可能需处理私有依赖）→I阶段提炼团队特定的架构约定→E阶段生成面向新成员的Wiki→V阶段由团队成员review验证准确性→C阶段沉淀内部文档化流程。
- **API文档生成**：R阶段阅读路由定义和schema→I阶段提炼API设计风格和认证模型→E阶段生成endpoint概念文档+调用示例→V阶段实际调用验证→C阶段沉淀API文档生成模板。

共性：**先建事实基础→再提炼架构洞察→信源先行分批生成→Grep级事实验证→模式萃取沉淀**——五阶段链路与具体语言/框架无关，可跨场景复用。

## 与其他模式的关系

| 关系模式 | 关系类型 | 说明 |
|---|---|---|
| seven-concepts-wiki-creation-methodology | 上位模式 | 本模式是七概念Wiki创作方法论在"源码学习→OKF规范输出"场景的具体化，增加了OKF格式约束和源码验证步骤 |
| spec-driven-batch-doc-generation | 互补 | 本模式的E阶段复用了spec驱动批量文档生成的核心思想（统一模板+分批并行+统一验证），但增加了R阶段事实采集和V阶段API真实性验证 |
| batched-creation-independent-review | 依赖 | V阶段的独立审查与修复借鉴了分批创建+独立审查模式 |
| external-content-fact-verification | 相关 | V阶段的Grep验证是外部内容事实验证在源码场景的应用 |
| [cpp-header-first-fact-collection.md](../research-knowledge/cpp-header-first-fact-collection.md) | 专项细化 | 本工作流R阶段在大型C/C++项目上的细化——以include/头文件为API权威来源、实现文件仅用于验证、构建系统文件补充模块依赖，2次验证（Apache TVM/TuyaOpen） |
| spec-driven-subagent-execution | 工具模式 | E阶段分批并行委派使用subagent执行模式 |

<!-- changelog -->
- 2026-08-23 | pattern | R阶段新增"大型C/C++项目头文件优先采集"专项小节，反向传播自新沉淀模式 cpp-header-first-fact-collection（L2，Apache TVM/TuyaOpen 双案例验证）
- 2026-08-21 | pattern | 初始创建：从 PyInvoke v3.0.3 OKF Wiki 生成实践萃取
