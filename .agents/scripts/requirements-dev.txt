# SpecWeave scripts 工具链测试依赖（本地开发环境）
# 与 .github/workflows/ci-quality-gates.yml 的 Install dependencies 步骤保持同步
# 安装：python -m pip install -r requirements-dev.txt
# 注意：默认 pip 源可能找不到部分包，如失败请加 -i https://pypi.org/simple

pytest>=9.0
pytest-asyncio>=1.3
pytest-benchmark>=5.0
pytest-cov>=5.0
pytest-xdist>=3.0
pyyaml>=6.0
markdown-it-py>=3.0
mdit-py-plugins>=0.4
openpyxl>=3.1
