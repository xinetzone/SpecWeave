# facts-cargo.md — rust-lang/cargo 事实清单
> 仓库基线：master @ 75d17360928f57ff2a7d2f2da1c753f5fe1926d1（2026-08-26）；路径前缀 external/libs/rust-lang/cargo/
> 采集原则：零推测，只记录源码中存在的 struct/enum/fn 签名、常量字面量、配置原文、文档注释原文。所有事实可用 Grep/Read 按路径验证。
> 结构说明：此版本 master 中 cargo 主 crate 源码位于 `src/`（旧版 `src/cargo/` 布局已重组），主 crate 的 Cargo.toml 位于仓库根（package 与 workspace 合一）；`Config` 类型已更名为 `GlobalContext`（见 F-cargo-040）。

## A. 仓库总览

- F-cargo-001: 根 `Cargo.toml` 的 `[workspace]` 段：`resolver = "2"`，`members = ["crates/*", "credential/*", "benches/benchsuite", "benches/capture"]`，`exclude = ["target/"]`（注释 `# exclude bench testing`）（源：Cargo.toml）
- F-cargo-002: `[workspace.package]`：`rust-version = "1.95"  # MSRV:3`、`edition = "2024"`、`license = "MIT OR Apache-2.0"`、`repository = "https://github.com/rust-lang/cargo"`（源：Cargo.toml）
- F-cargo-003: `[package]`：`name = "cargo"`、`version = "0.101.0"`、`rust-version = "1.98"  # MSRV:1`、`documentation = "https://docs.rs/cargo"`、`description = "Cargo, a package manager for Rust."`（源：Cargo.toml）
- F-cargo-004: 根 Cargo.toml 声明 `[[bin]] name = "cargo"`, `test = false`, `doc = false`（源：Cargo.toml）
- F-cargo-005: `[features]`：`default = ["http-transport-curl"]`；其余 feature 为 `vendored-openssl`、`vendored-libgit2`、`all-static`（= `['vendored-openssl', 'curl/static-curl', 'curl/force-system-lib-on-osx', 'vendored-libgit2']`）、`http-transport-curl`（= `["gix/blocking-http-transport-curl"]`）、`http-transport-reqwest`（= `["gix/blocking-http-transport-reqwest"]`）；注释原文："Exactly one of 'http-transport-curl' or 'http-transport-reqwest' must be enabled when using Cargo as a library."（源：Cargo.toml）
- F-cargo-006: 平台特定依赖：`[target.'cfg(target_os = "linux")'.dependencies]` 含 `cargo-credential-libsecret`；`cfg(target_os = "macos")` 含 `cargo-credential-macos-keychain`；`cfg(windows)` 含 `cargo-credential-wincred` 与 `windows-sys`（features 列表含 `Win32_System_JobObjects` 等）；`cfg(not(windows))` 含 optional 的 `openssl`/`openssl-src`（源：Cargo.toml）
- F-cargo-007: `[workspace.dependencies]` 中关键版本号：`clap = "4.6.0"`、`git2 = { version = "0.21.0", features = ["https", "ssh"] }`、`gix = "0.85.0"`、`curl = "0.4.49"`、`semver = { version = "1.0.27", features = ["serde"] }`、`toml = "1.1.2"`、`toml_edit = "0.25.10"`、`home = "0.5.12"`、`im-rc = "15.1.0"`、`rusqlite = { version = "0.40.0", features = ["bundled"] }`、`pasetors = { version = "0.7.8", ... }`、`rustc-stable-hash = "0.1.2"`、`varisat = "0.2.2"`（源：Cargo.toml）
- F-cargo-008: `[workspace.lints.clippy]`：`all = { level = "allow", priority = -2 }`、`correctness = { level = "warn", priority = -1 }`、`dbg_macro`/`disallowed_methods`/`disallowed_types`/`print_stderr`/`print_stdout`/`self_named_module_files` 均 `"warn"`（源：Cargo.toml）
- F-cargo-009: README.md 首句原文："Cargo downloads your Rust project's dependencies and compiles your project."；README 另有声明："This crate may make major changes to its APIs."（源：README.md）
- F-cargo-010: src 目录布局：`src/bin/cargo/{main.rs, cli.rs, commands/}`；`src/{lib.rs, macros.rs, version.rs}`；模块目录 `src/{compiler, context, diagnostics, ops, resolver, sources, util, workspace}/`（源：目录列举）
- F-cargo-011: crates/ 目录含 19 个子 crate：build-rs、build-rs-test-lib、cargo-platform、cargo-test-macro、cargo-test-support、cargo-util、cargo-util-schemas、cargo-util-terminal、crates-io、home、mdman、resolver-tests、rustfix、semver-check、xtask-build-man、xtask-bump-check、xtask-lint-docs、xtask-spellcheck、xtask-stale-label（源：crates/ 目录）
- F-cargo-012: CHANGELOG.md 全文仅一行："The changelog has moved to the [Cargo Book](https://doc.rust-lang.org/nightly/cargo/CHANGELOG.html)."（源：CHANGELOG.md）
- F-cargo-013: 仓库根存在 `publish.py`、`clippy.toml`、`deny.toml`、`rustfmt.toml`、`LICENSE-APACHE`、`LICENSE-MIT`、`LICENSE-THIRD-PARTY`、`.git-blame-ignore-revs`、`CODE_OF_CONDUCT.md`、`CONTRIBUTING.md`（源：目录列举）

## B. CLI 入口（src/bin/cargo/）

- F-cargo-014: `main()`（src/bin/cargo/main.rs:17）流程代码顺序：`setup_logger()` → `GlobalContext::default()` → 若 `features::channel()` 为 `"nightly" | "dev"` 则构建 `clap_complete::CompleteEnv`（`.var("CARGO_COMPLETE")`）→ `cargo::ops::fix_get_proxy_lock_addr()` 非空则 `fix_exec_rustc`，否则 `cargo::util::job::setup()` 后调 `cli::main(&mut gctx)`（源：src/bin/cargo/main.rs）
- F-cargo-015: `setup_logger()` 使用 `tracing_subscriber::EnvFilter::from_env("CARGO_LOG")`；`CARGO_LOG_PROFILE` 为真值时构建 `tracing_chrome::ChromeLayerBuilder`，`CARGO_LOG_PROFILE_CAPTURE_ARGS` 控制 `include_args`（源：src/bin/cargo/main.rs:62-110）
- F-cargo-016: `const BUILTIN_ALIASES: [(&str, &str, &str); 6]`：`("b","build")`、`("c","check")`、`("d","doc")`、`("r","run")`、`("t","test")`、`("rm","remove")`，第三元均为 `"alias: <cmd>"`（源：src/bin/cargo/main.rs:114-121）
- F-cargo-017: `aliased_command()` 查找链（文档注释原文）："1. Get the aliased command as a string. 2. If an `Err` occurs ... try to get it as an array again. 3. If still cannot find any, finds one insides [`BUILTIN_ALIASES`]."；空别名报错信息："subcommand is required, but `{alias_name}` is empty"（源：src/bin/cargo/main.rs:129-162）
- F-cargo-018: `third_party_subcommands()` 扫描 `search_directories(gctx)` 中前缀 `cargo-`、后缀 `env::consts::EXE_SUFFIX` 的可执行文件；`search_directories()` 将 `gctx.home().into_path_unlocked().join("bin")` 前插到 PATH 目录列表（注释引用 issue #11020）（源：src/bin/cargo/main.rs:203-367）
- F-cargo-019: `execute_subcommand()` 通过 `cargo_util::ProcessBuilder` 执行：`cmd.env(cargo::CARGO_ENV, cargo_exe)`，若 `gctx.jobserver_from_env()` 存在则 `inherit_jobserver`；找不到外部命令时错误码 101，错误文案包含 "help: view all installed commands with `cargo --list`"（源：src/bin/cargo/main.rs:305-330）
- F-cargo-020: `init_git()` 内容：`unsafe { git2::opts::set_verify_owner_validation(false) }`，注释说明 libgit2 不启动可执行文件（关联 rust-lang/rfcs#3279）（源：src/bin/cargo/main.rs:369-393）
- F-cargo-021: `cli::main()`（src/bin/cargo/cli.rs:23）顺序：`cli(gctx).try_get_matches()` → 处理 `-C/--directory`（错误文案："the `-C` flag is unstable, pass `-Z unstable-options` on the nightly channel to enable it"）→ `expand_aliases` → 依次分支处理 `-Z help` / `--version` / `--explain` / `--list` → `Exec::infer(cmd)` → `configure_gctx` → `super::init_git()` → `exec.exec(gctx, subcommand_args)`（源：src/bin/cargo/cli.rs）
- F-cargo-022: `enum Exec { Builtin(commands::Exec), Manifest(String), External(String) }`；`Exec::infer` 的匹配顺序：`commands::builtin_exec(cmd)` → `commands::run::is_manifest_command(cmd)` → `External`；注释列出实际优先级："1. built-ins xor manifest-command 2. aliases 3. external subcommands"（源：src/bin/cargo/cli.rs:463-487）
- F-cargo-023: `struct GlobalArgs` 字段：`verbose: u32`、`quiet: bool`、`color: Option<String>`、`frozen: bool`、`locked: bool`、`offline: bool`、`unstable_flags: Vec<String>`、`config_args: Vec<String>`（源：src/bin/cargo/cli.rs:533-543）
- F-cargo-024: `pub fn cli(gctx: &GlobalContext) -> Command` 构造 `Command::new("cargo")`，链式调用含：`.allow_external_subcommands(true)`、`.disable_help_subcommand(true)`、`.override_usage(usage)`、`.next_display_order(800)`；help_template 首行 "Rust's package manager"；`is_rustup()` 为真时 usage 含 `[+toolchain]`（源：src/bin/cargo/cli.rs:569-644）
- F-cargo-025: 顶层 CLI 参数定义：`-V/--version`、`--list`、`--explain CODE`、`-v/--verbose`（`ArgAction::Count`, global）、`-q/--quiet`（global）、`--color WHEN`（value_parser `["auto","always","never"]`, global）、`-C DIRECTORY`（help 文案 "Change to DIRECTORY before doing anything (nightly-only)"）、`--locked`/`--offline`/`--frozen`（均 global，help_heading 为 MANIFEST_OPTIONS）、`--config KEY=VALUE|PATH`（global）、`-Z FLAG`（global）；另有两个隐藏参数用 `UnknownArgumentValueParser::suggest_arg` 提示 `--config` 与 `-Z`（源：src/bin/cargo/cli.rs:645-727）
- F-cargo-026: `commands::builtin()` 返回 39 个子命令的 `Vec<Command>`：add、bench、build、check、clean、config、doc、fetch、fix、generate-lockfile、git-checkout、help、info、init、install、locate-project、login、logout、metadata、new、owner、package、pkgid、publish、read-manifest、remove、report、run、rustc、rustdoc、search、test、tree、uninstall、update、vendor、verify-project、version、yank（源：src/bin/cargo/commands/mod.rs:3-45）
- F-cargo-027: `pub type Exec = fn(&mut GlobalContext, &ArgMatches) -> CliResult;`（src/bin/cargo/commands/mod.rs:47）；`builtin_exec(cmd: &str)` 以字符串 match 分发到同名模块的 `exec` 函数（源：src/bin/cargo/commands/mod.rs:49-93）
- F-cargo-028: `pub fn is_manifest_command(arg: &str) -> bool` 实现：`let path = Path::new(arg); 1 < path.components().count() || path.extension() == Some(OsStr::new("rs"))`（源：src/bin/cargo/commands/run.rs:92-95）
- F-cargo-029: `exec_manifest_command` 对 `(manifest_path.is_file(), gctx.cli_unstable().script)` 组合分支：`(true, false)` 返回错误 "running the file `{cmd}` requires `-Zscript`"（源：src/bin/cargo/commands/run.rs:97-103）
- F-cargo-030: `cli.rs` 内含测试 `verify_cli()`（调 `cli(&gctx).debug_assert()`）与 `avoid_ambiguity_between_builtins_and_manifest_commands()`（断言所有 builtin 命令名不满足 `is_manifest_command`）（源：src/bin/cargo/cli.rs:789-804）
- F-cargo-031: `expand_aliases()` 递归展开别名；`already_expanded.contains(&new_cmd)` 时报错 "alias {} has unresolvable recursive definition: {} -> {}"；内置命令与用户别名冲突时 warn "user-defined alias `{}` is ignored, because it is shadowed by a built-in command"（源：src/bin/cargo/cli.rs:274-403）
- F-cargo-032: `get_version_string(is_verbose)` 输出含：`cargo {version}`、`release:`、`commit-hash:`、`commit-date:`、`host: {env!("RUST_HOST_TARGET")}`、`libgit2: x.y.z (sys:{} {})`、`libcurl: ... (sys:{} {} ssl:{})`、`ssl:`（openssl feature）、`os: {os_info::get()}`（源：src/bin/cargo/cli.rs:207-272）

## C. lib.rs 骨架

- F-cargo-033: `src/lib.rs` 模块声明：`#[macro_use] mod macros;`、`pub mod compiler; pub mod context; pub mod diagnostics; pub mod ops; pub mod resolver; pub mod sources; pub mod util; mod version; pub mod workspace;`（源：src/lib.rs:162-173）
- F-cargo-034: lib.rs re-exports：`pub use crate::util::errors::{AlreadyPrintedError, InternalError, VerboseError};`、`pub use crate::util::{CargoResult, CliError, CliResult, GlobalContext, indented_lines};`、`pub use crate::version::version;`（源：src/lib.rs:156-158）
- F-cargo-035: `pub const CARGO_ENV: &str = "CARGO";`（源：src/lib.rs:160）
- F-cargo-036: lib.rs 公共函数：`pub fn exit_with_error(err: CliError, shell: &mut Shell) -> !`、`pub fn display_error(err: &Error, shell: &mut Shell)`、`pub fn display_warning_with_error(warning: &str, err: &Error, shell: &mut Shell)`；`display_error` 遇到 `InternalError` 链时输出 "this is an unexpected cargo internal error"（源：src/lib.rs:175-220）
- F-cargo-037: lib.rs 文档注释列出的主要组件（原文摘录）：`ops`（"Every major operation is implemented here. Each command is a thin wrapper around ops."）、`ops::cargo_compile`（"the entry point for all the compilation commands"）、`ops::resolve`（"Top-level API for dependency and feature resolver (e.g. ops::resolve_ws)"）、`resolver`（"The core algorithm"）、`compiler`（"the code responsible for running rustc and rustdoc"）、`sources::source`（"The Source trait is an abstraction over different sources of packages"）、`context`（"the global application context"）、`workspace::parser`（"the code for parsing Cargo.toml files"）、`ops::lockfile`（"where Cargo.lock files are loaded and saved"）（源：src/lib.rs:21-67）
- F-cargo-038: lib.rs "File Overview" 文档（原文摘录）：`Cargo.toml` "loaded with workspace::parser::read_manifest and then translated to workspace::manifest::Manifest"；`Cargo.lock` "loaded with ops::resolve_ws or a variant of it into a resolver::Resolve"；`target/` "abstracted with compiler::layout"；`$CARGO_HOME/registry/` 下有 `index/`、`cache/*/*.crate`、`src/*/*`；`**/.cargo/config.toml` 见 `context` 模块（源：src/lib.rs:109-130）
- F-cargo-039: lib.rs 文档声明的相关 crate：cargo-platform（"handles parsing cfg expressions"）、cargo-util、cargo-util-schemas（"contains the serde schemas for cargo"）、crates-io（"code for accessing the crates.io API"）、home（"shared between cargo and rustup and is used for finding their home directories"，非 path 依赖）、rustfix（"defines structures that represent fix suggestions from rustc"）、cargo-test-support、cargo-test-macro（"#[cargo_test] proc-macro"）、credential（"several packages for implementing the credential providers"）、mdman、resolver-tests（源：src/lib.rs:68-107）
- F-cargo-040: 版本说明注释（原文）："The library is permanently unstable, so it always has a 0 major version. However, the CLI now reports a stable 1.x version (starting in 1.26) which stays in sync with rustc's version."；非 bootstrap 构建时 `version()` 由 `CARGO_PKG_VERSION_MINOR - 1` 推导 `1.{minor}.{patch}`（源：src/version.rs:66-82）

## D-1. context/ 子系统

- F-cargo-041: context/mod.rs 文档描述两层反序列化（原文）："1. **External sources → `ConfigValue`** ... parsed into ConfigValue instances via ConfigValue::from_toml. 2. **`ConfigValue` → Target types** ... uses a custom serde deserializer (Deserializer)"；优先级在检索时按 `Definition` 解析（源：src/context/mod.rs:27-41）
- F-cargo-042: `pub const TOP_LEVEL_CONFIG_KEYS: &[&str]` 含 20 个键：`paths, alias, build, credential-alias, doc, env, future-incompat-report, cache, cargo-new, http, install, net, patch, profile, resolver, registries, registry, source, target, term`（源：src/context/mod.rs:163-184）
- F-cargo-043: `pub struct GlobalContext`（src/context/mod.rs:209）字段含：`home_path: Filesystem`、`shell: Mutex<Shell>`、`values: OnceLock<HashMap<String, ConfigValue>>`、`credential_values: OnceLock<...>`、`cli_config: Option<Vec<String>>`、`cwd: PathBuf`、`cargo_exe/rustdoc/sysroot: OnceLock<PathBuf>`、`frozen: bool`、`locked: bool`、`offline: bool`、`jobserver: Option<&'static jobserver::Client>`、`unstable_flags: CliUnstable`、`easy: OnceLock<Mutex<Easy>>`、`crates_io_source_id: OnceLock<SourceId>`、`invocation_instant: Instant`、`invocation_time: jiff::Timestamp`、`target_dir: Option<Filesystem>`、`env: Env`、`credential_cache: Mutex<HashMap<CanonicalUrl, CredentialCacheValue>>`、`registry_config: Mutex<HashMap<SourceId, Option<RegistryConfig>>>`、`package_cache_lock: CacheLocker`、`http_config/net_config/build_config/target_cfgs/doc_extern_map/env_config` 等 OnceLock 字段（源：src/context/mod.rs:209-283）
- F-cargo-044: `pub struct CredentialCacheValue { pub token_value: Secret<String>, pub expiration: Option<OffsetDateTime>, pub operation_independent: bool }`（源：src/context/mod.rs:200-204）
- F-cargo-045: context/mod.rs 公开项：`pub fn homedir(cwd: &Path) -> Option<PathBuf>`、`pub fn save_credentials(...)`、`pub struct StringList(Vec<String>)`（源：src/context/mod.rs:2188, 2197, 2503）
- F-cargo-046: context/mod.rs 内部模块：`mod de; mod error; mod value; mod key; mod config_value; mod path; mod target; mod environment; mod schema;`，re-export `ConfigError`、`{Definition, OptValue, Value}`、`ConfigKey`、`ConfigValue`、`{BracketType, ConfigRelativePath, PathAndArgs, ResolveTemplateError}`、`{TargetCfgConfig, TargetConfig}`、`pub use schema::*`（源：src/context/mod.rs:108-137）
- F-cargo-047: schema.rs 定义的配置 schema 类型（23 个）：CargoHttpConfig、CargoFutureIncompatConfig、CargoFutureIncompatFrequencyConfig、SslVersionConfig、SslVersionConfigRange、CargoNetConfig、CargoSshConfig、JobsConfig、CargoBuildConfig、CargoBuildAnalysis、WarningHandling、FingerprintMethod、BuildTargetConfig、CargoResolverConfig、IncompatibleRustVersions、IncompatiblePublishAge、FeatureUnification、TermConfig、ProgressConfig、ProgressWhen、EnvConfigValue、RegistryConfig、GlobalRegistryConfig（源：src/context/schema.rs）
- F-cargo-048: `pub struct Value<T> { pub val: T, pub definition: Definition }`；`pub enum Definition { BuiltIn, Path(PathBuf), Environment(String), Cli(Option<PathBuf>) }`，`Definition` 实现按优先级的 `Ord`（源：src/context/value.rs:55-83）
- F-cargo-049: key.rs：`pub struct ConfigKey`、`pub enum KeyOrIdx`、`pub struct ArrayItemKeyPath`（源：src/context/key.rs:12-130）
- F-cargo-050: de.rs 文档（原文）："The Deserializer type is the main driver of deserialization."，流程含 `ConfigMapAccess`（structs/maps）、`ConfigSeqAccess`/`ArrayItemDeserializer`（sequences）、`ValueDeserializer`（Value<T>）（源：src/context/de.rs:1-20）

## D-2. ops/ 子系统

- F-cargo-051: ops/mod.rs 的 mod 声明清单：`pub mod cargo_add; mod cargo_clean; pub mod cargo_config; mod cargo_doc; mod cargo_fetch; mod cargo_fix; mod cargo_install; mod cargo_metadata; mod cargo_new; mod cargo_package; mod cargo_pkgid; mod cargo_read_manifest; pub mod cargo_remove; mod cargo_run; mod cargo_test; pub mod cargo_tree; mod cargo_uninstall; mod cargo_update; mod cargo_vendor; mod common_for_install_and_uninstall;`（另有目录形式 cargo_add/、cargo_compile/、cargo_fix/、cargo_package/、cargo_report/、cargo_tree/、registry/）（源：src/ops/mod.rs:55-76）
- F-cargo-052: ops/mod.rs re-exports 关键项：`{CompileOptions, compile, compile_with_exec, create_bcx, print, resolve_all_features}`、`{CleanContext, CleanOptions, clean}`、`{DocOptions, OutputFormat, doc}`、`{FetchOptions, fetch}`、`{NewOptions, NewProjectKind, VersionControl, init, new}`、`run`、`{TestOptions, run_benches, run_tests}`、`{load_pkg_lockfile, resolve_to_string, write_pkg_lockfile}`、`{publish, search, yank, info, registry_login, registry_logout, modify_owners, PublishOpts, OwnersOptions, RegistryCredentialConfig, RegistryOrIndex}`、`{WorkspaceResolve, add_overrides, get_resolved_packages, resolve_with_previous, resolve_ws, resolve_ws_with_opts}`、`{VendorOptions, vendor}`、`{install, install_list}`、`{OutputMetadataOptions, ExportInfo, output_metadata}`、`UnitGenerator`、`{CompileFilter, FilterRule, LibRule, Packages}`（源：src/ops/mod.rs:3-54）
- F-cargo-053: `pub struct WorkspaceResolve<'gctx>` 字段：`pkg_set: PackageSet<'gctx>`、`workspace_resolve: Option<Resolve>`（注释："`cargo install` and `-Zavoid-dev-deps` 时可为 None"）、`targeted_resolve: Resolve`、`specs_and_features: Vec<SpecsAndResolvedFeatures>`（源：src/ops/resolve.rs:96-110）
- F-cargo-054: `pub fn resolve_ws<'a>(ws: &Workspace<'a>, dry_run: bool) -> CargoResult<(PackageSet<'a>, Resolve)>`：依次调用 `ws.package_registry()` → `resolve_with_registry` → `get_resolved_packages`（源：src/ops/resolve.rs:135-140）
- F-cargo-055: `pub fn resolve_ws_with_opts<'gctx>(...)`（src/ops/resolve.rs:152）、`pub fn resolve_with_previous<'gctx>(...)`（:425）、`pub fn add_overrides<'a>(...)`（:559）、`pub fn get_resolved_packages<'gctx>(...)`（:591）（源：src/ops/resolve.rs）
- F-cargo-056: ops/lockfile.rs 三个公开函数：`pub fn load_pkg_lockfile(ws: &Workspace<'_>) -> CargoResult<Option<Resolve>>`、`pub fn resolve_to_string(ws, resolve) -> CargoResult<String>`、`pub fn write_pkg_lockfile(ws, resolve: &mut Resolve) -> CargoResult<bool>`（源：src/ops/lockfile.rs:15-45）
- F-cargo-057: ops/cargo_new.rs：`pub enum VersionControl`、`pub struct NewOptions`、`pub enum NewProjectKind`、`pub fn new(opts: &NewOptions, gctx: &GlobalContext) -> CargoResult<()>`、`pub fn init(opts: &NewOptions, gctx: &GlobalContext) -> CargoResult<NewProjectKind>`（源：src/ops/cargo_new.rs:22-493）
- F-cargo-058: ops/cargo_test.rs：`pub struct TestOptions`、`pub fn run_tests(ws: &Workspace<'_>, options: &TestOptions, test_args: &[&str]) -> CliResult`、`pub fn run_benches(ws, options, args) -> CliResult`（源：src/ops/cargo_test.rs:15-87）
- F-cargo-059: ops/cargo_doc.rs：`pub enum OutputFormat`、`pub struct DocOptions`、`pub fn doc(ws: &Workspace<'_>, options: &DocOptions) -> CargoResult<()>`（源：src/ops/cargo_doc.rs:19-58）
- F-cargo-060: ops/cargo_compile/mod.rs：`pub struct CompileOptions`、`pub fn compile<'a>(ws: &Workspace<'a>, options: &CompileOptions) -> CargoResult<Compilation<'a>>`、`pub fn compile_with_exec<'a>(...)`、`pub fn print<'a>(...)`、`pub fn create_bcx<'a, 'gctx>(...)`、`pub fn resolve_all_features(...)`（源：src/ops/cargo_compile/mod.rs:86-1207）
- F-cargo-061: ops/cargo_run.rs：`pub fn run(...)`（源：src/ops/cargo_run.rs:12）

## D-3. resolver/ 子系统

- F-cargo-062: resolver/mod.rs re-exports：`{ActivateError, ActivateResult, ResolveError}`（errors）、`{CliFeatures, ForceAllTargets, HasDevUnits}`（features）、`{Resolve, ResolveVersion}`、`{ResolveBehavior, ResolveOpts}`（types）、`{PublishAgePolicy, PublishAgeViolation, VersionOrdering, VersionPreferences}`（version_prefs）；`pub fn resolve(...)` 定义于 mod.rs:125（源：src/resolver/mod.rs:80-125）
- F-cargo-063: resolver 子模块：`mod conflict_cache; mod context; mod dep_cache; mod encode; mod errors; pub mod features; mod resolve; mod types; mod version_prefs;`（源：src/resolver/mod.rs:89-97）
- F-cargo-064: `pub struct Resolve`（src/resolver/resolve.rs:19）字段：`graph: Graph<PackageId, HashSet<Dependency>>`、`replacements: HashMap<PackageId, PackageId>`、`reverse_replacements`、`features: HashMap<PackageId, Vec<InternedString>>`、`checksums: HashMap<PackageId, Option<String>>`、`metadata: TomlLockfileMetadata`、`unused_patches: Vec<PackageId>`、`public_dependencies: HashMap<PackageId, HashSet<PackageId>>`、`version: ResolveVersion`、`summaries: HashMap<PackageId, Summary>`（源：src/resolver/resolve.rs:19-50）
- F-cargo-065: `pub enum ResolveVersion { V1, V2, V3, V4, V5 }`；`fn default() -> ResolveVersion { ResolveVersion::V4 }`；`pub fn max_stable() -> ResolveVersion { ResolveVersion::V4 }`；`with_rust_version` 分支：`>= 1.83 → V4`、`>= 1.53 → V3`、`>= 1.41 → V2`、否则 V1；V5 注释："Unstable. Will collect a certain amount of changes and then go."（源：src/resolver/resolve.rs:71-150）
- F-cargo-066: `Resolve::new(graph, replacements, features, checksums, metadata, unused_patches, version, summaries)`；`pub fn iter(&self) -> impl Iterator<Item = PackageId>`、`pub fn deps(&self, pkg: PackageId) -> impl Iterator<Item = (PackageId, &HashSet<Dependency>)>`、`pub fn deps_not_replaced(...)`（源：src/resolver/resolve.rs:154-340）
- F-cargo-067: types.rs：`pub enum ResolveBehavior { V1, V2, V3 }`；`ResolveBehavior::from_manifest` 接受 "1"/"2"/"3"，否则报错 "`resolver` setting `{}` is not valid"；`pub struct ResolveOpts { pub dev_deps: bool, pub features: RequestedFeatures }`；其余类型：ResolverProgress、ActivationsKey、SemverCompatibility、DepsFrame、RemainingDeps、ConflictReason、RcVecIter；`pub type FeaturesSet = Rc<BTreeSet<InternedString>>`（源：src/resolver/types.rs:100-376）

## D-4. sources/ 子系统

- F-cargo-068: sources/mod.rs 文档列出 5 个内置 Source 实现（原文）："RegistrySource --- A source that provides an index ... crates.io falls into this category. So do local registry and sparse registry."、"DirectorySource --- Files are downloaded ahead of time. Primarily designed for crates generated from cargo vendor."、"GitSource"、"PathSource"、"ReplacedSource --- This manages the source replacement feature"；另有 `SourceConfigMap`，"which is effectively the representation of the `[source.*]` value"（源：src/sources/mod.rs:1-27）
- F-cargo-069: sources/mod.rs re-exports：`SourceConfigMap`、`DirectorySource`、`GitSource`、`{PathEntry, PathSource, RecursivePathSource}`、`{CRATES_IO_DOMAIN, CRATES_IO_INDEX, CRATES_IO_REGISTRY, IndexSummary, RegistrySource}`、`ReplacedSource`；子模块 `pub mod config/directory/git/overlay/path/registry/replaced/source`（源：src/sources/mod.rs:29-47）
- F-cargo-070: `#[async_trait::async_trait(?Send)] pub trait Source`（src/sources/source.rs:32）方法清单：`fn source_id(&self) -> SourceId`、`fn replaced_source_id(&self) -> SourceId`、`fn supports_checksums(&self) -> bool`、`fn requires_precise(&self) -> bool`、`async fn query(&self, dep: &Dependency, kind: QueryKind, f: &mut dyn FnMut(IndexSummary)) -> CargoResult<()>`、`async fn query_vec(...)`、`fn invalidate_cache(&self)`、`fn set_quiet(&mut self, quiet: bool)`、`async fn download(&self, package: PackageId) -> CargoResult<MaybePackage>`、`async fn finish_download(&self, pkg_id: PackageId, contents: Vec<u8>) -> CargoResult<Package>`、`fn fingerprint(...)`（源：src/sources/source.rs:32-103）
- F-cargo-071: source.rs 另有：`pub enum QueryKind`（:132）、`pub enum MaybePackage`（:159）、`pub struct SourceMap<'src>`（:236）（源：src/sources/source.rs）
- F-cargo-072: registry/mod.rs 常量原文：`pub const CRATES_IO_INDEX: &str = "https://github.com/rust-lang/crates.io-index";`、`pub const CRATES_IO_HTTP_INDEX: &str = "sparse+https://index.crates.io/";`、`pub const CRATES_IO_REGISTRY: &str = "crates-io";`、`pub const CRATES_IO_DOMAIN: &str = "crates.io";`（源：src/sources/registry/mod.rs:224-227）
- F-cargo-073: registry/mod.rs：`pub struct RegistrySource<'gctx>`（:245）、`pub enum LoadResponse`（:269）、`pub enum MaybeLock`（:385）、`pub use cargo_util_schemas::index::RegistryConfig`（:216）、`pub use index::IndexSummary`（:403）；子模块 `mod download; mod http_remote; mod git_remote; mod local;` 及 `index/{mod.rs, cache.rs}`（源：src/sources/registry/mod.rs:400-405）
- F-cargo-074: path.rs：`pub struct PathSource<'gctx>`（:34）含 `pub fn new(path: &Path, source_id: SourceId, gctx: &'gctx GlobalContext) -> Self`；`pub struct RecursivePathSource<'gctx>`（:223）、`pub struct PathEntry`（:457）（源：src/sources/path.rs）
- F-cargo-075: config.rs：`pub struct SourceConfigMap<'gctx>`（:26）（源：src/sources/config.rs）
- F-cargo-076: sources/git/ 子模块文件：`known_hosts.rs`、`mod.rs`、`oxide.rs`、`source.rs`、`utils.rs`（源：目录列举）

## D-5. workspace/ 子系统

- F-cargo-077: workspace/mod.rs re-exports：`{Dependency, Patch, PatchLocation, SerializedDependency}`（dependency）、`{CliUnstable, Edition, Feature, Features}`（features）、`{EitherManifest, VirtualManifest}`、`{Manifest, Target, TargetKind}`（manifest）、`{Package, PackageSet}`、`PackageId`、`PackageIdSpecQuery`、`Registry`、`SourceId`、`{FeatureMap, FeatureValue, Summary}`、workspace 模块项；`pub use cargo_util_schemas::core::{GitReference, PackageIdSpec, SourceKind};`（源：src/workspace/mod.rs:1-15）
- F-cargo-078: workspace/ 子模块：`pub mod dependency; pub mod editor; pub mod features; pub mod gc; pub mod global_cache_tracker; pub mod manifest; pub mod package; pub mod package_id; mod package_id_spec; pub mod parser; pub mod profiles; pub mod registry; mod source_id; pub mod summary; mod workspace;`（源：src/workspace/mod.rs:17-31）
- F-cargo-079: `pub struct Workspace<'gctx>`（src/workspace/workspace.rs:50）字段：`gctx: &'gctx GlobalContext`、`current_manifest: PathBuf`、`packages: Packages<'gctx>`、`root_manifest: Option<PathBuf>`、`target_dir: Option<Filesystem>`、`build_dir: Option<Filesystem>`、`members: IndexSet<PathBuf>`、`member_ids: HashSet<PackageId>`、`default_members: Vec<PathBuf>`、`is_ephemeral: bool`、`require_optional_deps: bool`、`loaded_packages: RefCell<HashMap<PathBuf, Package>>`、`ignore_lock: bool`、`requested_lockfile_path: Option<PathBuf>`、`resolve_behavior: ResolveBehavior`、`resolve_honors_rust_version: bool`、`resolve_feature_unification: FeatureUnification`、`resolve_honors_publish_age: bool`、`resolve_publish_time: Option<jiff::Timestamp>`、`custom_metadata: Option<toml::Value>`、`local_overlays: HashMap<SourceId, PathBuf>`（源：src/workspace/workspace.rs:50-138）
- F-cargo-080: `impl Workspace` 方法：`pub fn new(manifest_path: &Path, gctx: &'gctx GlobalContext) -> CargoResult<Workspace<'gctx>>`（:224）、`pub fn ephemeral(...)`（:286）、`pub fn default_members<'a>(&'a self) -> impl Iterator<Item = &'a Package>`（:662）（源：src/workspace/workspace.rs）
- F-cargo-081: `pub enum MaybePackage { Package(Package), Virtual(VirtualManifest) }`（workspace/workspace.rs:149）；`pub enum WorkspaceConfig { Root(WorkspaceRootConfig), Member { root: Option<String> } }`（:156）；`pub struct WorkspaceRootConfig { root_dir: PathBuf, members: Option<Vec<String>>, default_members: Option<Vec<String>>, exclude: Vec<String>, inheritable_fields: InheritableFields, custom_metadata: Option<toml::Value> }`（:208）（源：src/workspace/workspace.rs）
- F-cargo-082: workspace/workspace.rs 自由函数：`pub fn resolve_relative_path(...)`（:2126）、`pub fn find_workspace_root(...)`（:2154）、`pub fn find_workspace_root_with_membership_check(...)`（:2173）（源：src/workspace/workspace.rs）
- F-cargo-083: `pub struct PackageId { inner: &'static PackageIdInner }`；`struct PackageIdInner { name: InternedString, version: semver::Version, source_id: SourceId }`；`static PACKAGE_ID_CACHE: OnceLock<Mutex<HashSet<&'static PackageIdInner>>>`；自定义 `PartialEq`/`Hash` 使用 `source_id.full_eq`/`full_hash`；`Serialize` 输出格式 `"{} {} ({})"`（name version url）（源：src/workspace/package_id.rs:18-68）
- F-cargo-084: package.rs：`pub struct Package`（:43）含 `pub fn new(manifest: Manifest, manifest_path: &Path) -> Package`（:103）、方法 `dependencies/manifest/manifest_path/name/package_id/root/summary/targets/library/version/authors/publish/proc_macro/rust_version/hints/has_custom_build/map_source`；`pub struct PackageSet<'gctx>`（:288）含 `package_ids()`/`packages()`（源：src/workspace/package.rs）
- F-cargo-085: `pub struct SourceId { inner: &'static SourceIdInner }`；`struct SourceIdInner { url: Url, canonical_url: CanonicalUrl, kind: SourceKind, precise: Option<Precise>, registry_key: Option<KeyOf> }`；`enum Precise { Locked, Updated { name: InternedString, from: semver::Version, to: semver::Version }, GitUrlFragment(String) }`；`pub fn as_url(&self) -> SourceIdAsUrl<'_>`（源：src/workspace/source_id.rs:40-184）
- F-cargo-086: features.rs：`pub enum Edition { Edition2015, Edition2018, Edition2021, Edition2024, EditionFuture }`；`pub const LATEST_STABLE: Edition = Edition::Edition2024;`；`pub const CLI_VALUES: [&'static str; 4] = ["2015", "2018", "2021", "2024"];`；`pub struct Feature { name: &'static str, stability: Status, version: &'static str, docs: &'static str, get: fn(&Features) -> bool }`；`Features::new(features: &[String], gctx, warnings, is_local)`（源：src/workspace/features.rs:183-616）
- F-cargo-087: `pub fn channel() -> String`（features.rs:1588）读取顺序：`__CARGO_TEST_CHANNEL_OVERRIDE_DO_NOT_USE_THIS` → `RUSTC_BOOTSTRAP == "1"` 时返回 `"dev"` → `crate::version().release_channel.unwrap_or("dev")`；文档注释："Returns the current release channel (\"stable\", \"beta\", \"nightly\", \"dev\")."（源：src/workspace/features.rs:1587-1608）
- F-cargo-088: features.rs 其他：`pub const SEE_CHANNELS: &str = "See https://doc.rust-lang.org/book/appendix-07-nightly-rust.html ..."`（:137）、`pub struct GitFeatures`（:1045）、`pub struct GitoxideFeatures`（:1141）、宏 `macro_rules! features!`（:422）、`macro_rules! stab!`（:490）、`macro_rules! unstable_cli_options!`（:805）、`impl CliUnstable`（:1257）（源：src/workspace/features.rs）

## D-6. util/ 子系统

- F-cargo-089: util/mod.rs re-exports：`CanonicalUrl`、`DependencyQueue`、`RustfixDiagnosticServer`、`{closest, closest_msg, edit_distance}`、`CliError`、`{CargoResult, CliResult, internal}`、`{FileLock, Filesystem}`、`Graph`、`StableHasher`、`{hash_u64, short_hash, to_hex}`、`IntoUrl`、`IntoUrlWithBase`、`{LockServer, LockServerClient, LockServerStarted}`、`BuildLogger`、`OnceExt`、`{Progress, ProgressStyle}`、`Queue`、`Rustc`、`{OptVersionReq, VersionExt}`、`Unhashed`、`{FossilRepo, GitRepo, HgRepo, PijulRepo, existing_vcs_repo}`、`{ConfigValue, GlobalContext, homedir}`、`pub use cargo_util_terminal::style;`（源：src/util/mod.rs:4-31）
- F-cargo-090: util/mod.rs 子模块声明（37 个）：auth、cache_lock、canonical_url、command_prelude、counter、cpu、credential、dependency_queue、diagnostic_server、edit_distance、errors、flock、frontmatter、graph、hasher、hex、important_paths、interning、into_url、into_url_with_base、io、job、local_poll_adapter、data_structures、lockserver、log_message、logger、machine_message、network、once、open、progress、queue、restricted_names、rustc、semver_eval_ext、semver_ext、sqlite、time_span、unhashed、vcs、workspace（源：src/util/mod.rs:33-75）
- F-cargo-091: util/mod.rs 公开函数：`pub fn is_rustup() -> bool`（:81）、`pub fn elapsed(duration: Duration) -> String`（:86）、`pub struct HumanBytes(pub u64)`（:97）、`pub fn indented_lines(text: &str) -> String`（:119）、`pub fn truncate_with_ellipsis(s: &str, max_width: usize) -> String`（:131）、`pub fn try_canonicalize<P: AsRef<Path>>(path: P) -> std::io::Result<PathBuf>`（:145）、`pub fn get_umask() -> u32`（:169）（源：src/util/mod.rs:81-169）
- F-cargo-092: graph.rs：`pub struct Graph<N: Clone, E: Clone> { nodes: im_rc::OrdMap<N, im_rc::OrdMap<N, E>> }`；方法 `new/add/link/reversed/contains/edge/edges/sort`（sort 为拓扑排序）（源：src/util/graph.rs:5-60）
- F-cargo-093: progress.rs：`pub struct Progress<'gctx>`（:29）、`pub enum ProgressStyle`（:213）（源：src/util/progress.rs）
- F-cargo-094: flock.rs 模块文档（原文）："This module defines the Filesystem type which is an abstraction over a filesystem, ensuring that access to the filesystem is only done through coordinated locks."；`pub struct FileLock { f: Option<File>, path: PathBuf }` 实现 Read/Write/Seek，drop 释放锁；导出 `lock_exclusive/lock_shared/try_lock_exclusive/try_lock_shared/unlock`（源：src/util/flock.rs:1-46）
- F-cargo-095: cpu.rs：`pub struct State(imp::State)`；`State::current() -> io::Result<State>`、`idle_since(&self, previous: &State) -> f64`（"as a percentage from 0.0 to 100.0"）；linux `imp::State` 字段 user/nice/system/idle/iowait/irq/softirq/steal/guest（源：src/util/cpu.rs:3-41）
- F-cargo-096: queue.rs：`pub struct Queue<T> { state: Mutex<State<T>>, popper_cv: Condvar, bounded_cv: Condvar, bound: usize }`；文档（原文）："`push` will never block, and allows the queue to grow without bounds. `push_bounded` will block if the queue is over capacity"；`pub fn new(bound: usize) -> Queue<T>`（源：src/util/queue.rs:5-38）
- F-cargo-097: job.rs 模块文档（原文）："Job management (mostly for windows)"、"On Windows, however, this does not happen and Ctrl-C just kills cargo."、"we use Job Objects to ensure that all processes die at the same time"；`pub fn setup() -> Option<Setup>`（源：src/util/job.rs:1-20）
- F-cargo-098: hasher.rs 模块文档（原文）："A hasher that produces the same values across releases and platforms."、"not sufficient for cryptographic purposes"；`pub use rustc_stable_hash::StableSipHasher128 as StableHasher;`（源：src/util/hasher.rs:1-6）
- F-cargo-099: interning.rs：`pub struct InternedString`，实现 `Deref<str>`/`AsRef<str>`/`AsRef<OsStr>`/`AsRef<Path>`/`Hash`/`Borrow<str>`/`Serialize`（源：src/util/interning.rs:32-163）
- F-cargo-100: sqlite.rs：模块文档 "Utilities to help with working with sqlite."；`pub type Migration = Box<dyn Fn(&Connection) -> CargoResult<()>>`、`pub fn basic_migration(stmt: &'static str) -> Migration`、`pub fn migrate(conn: &mut Connection, migrations: &[Migration]) -> CargoResult<()>`（源：src/util/sqlite.rs:68-83）
- F-cargo-101: util/workspace.rs：`pub fn print_available_packages(ws: &Workspace<'_>) -> CargoResult<()>`、`print_available_examples/binaries/benches/tests(ws, options)`、`pub fn path_args(ws, unit) -> (PathBuf, PathBuf)`、`pub fn add_path_args(ws, unit, cmd: &mut ProcessBuilder)`（源：src/util/workspace.rs:68-148）
- F-cargo-102: into_url.rs：`pub trait IntoUrl`（:8）（源：src/util/into_url.rs）
- F-cargo-103: errors.rs：`pub type CargoResult<T> = anyhow::Result<T>;`（:11）、`pub type CliResult = Result<(), CliError>;`（:308）、`pub struct CliError`（:316）、`pub struct VerboseError`（:144）、`pub struct InternalError`（:179）、`pub struct AlreadyPrintedError`（:212）、`pub struct ManifestError`（:246）、`pub struct HttpNotSuccessful`（:34）、`pub struct GitCliError`（:375）、`pub fn internal<S: fmt::Display>(error: S) -> anyhow::Error`（:426）（源：src/util/errors.rs）
- F-cargo-104: logger.rs：`pub struct BuildLogger`（:102）内部含 FileLogger/InMemoryLogger 与 `pub struct RunId`（:161）（源：src/util/logger.rs）
- F-cargo-105: rustc.rs：`pub struct Rustc` 字段：`path: PathBuf`、`wrapper: Option<PathBuf>`、`workspace_wrapper: Option<PathBuf>`、`verbose_version: String`、`version: semver::Version`、`host: InternedString`、`commit_hash: Option<String>`、`cache: Mutex<Cache>`；`Rustc::new(path, wrapper, workspace_wrapper, rustup_rustc, cache_location, gctx)` 通过执行 `rustc -vV` 获取信息（源：src/util/rustc.rs:19-69）

## D-7. compiler/ 子系统

- F-cargo-106: compiler/mod.rs 模块清单（25 个）：artifact、build_config、build_context、build_runner、compilation、compile_kind、crate_type、custom_build、fingerprint、future_incompat、job_queue、layout、links、locking、lto、output_depinfo、output_sbom、rustdoc、standard_lib、timings、trim_paths、unit、unit_dependencies、unit_graph、unused_deps（源：src/compiler/mod.rs:31-55）
- F-cargo-107: compiler/mod.rs 文档（原文摘录）："`BuildContext` is a static context containing all information you need before a build gets started."、"`BuildRunner` is the center of the world, coordinating a running build"、"If you consider `ops::cargo_compile::compile` as a `rustc` driver but on Cargo side, this module is kinda the `rustc_interface`"（源：src/compiler/mod.rs:1-29）
- F-cargo-108: compiler/mod.rs re-exports：`{BuildConfig, CompileMode, MessageFormat, UserIntent}`、`BuildContext`、`{DepKindSet, FileFlavor, FileType, RustcTargetData, TargetInfo}`、`{BuildRunner, Metadata, UnitHash}`、`{Compilation, Doctest, UnitOutput}`、`{CompileKind, CompileKindFallback, CompileTarget}`、`CrateType`、`{BuildOutput, BuildScriptOutputs, BuildScripts, LibraryPath, LinkArgTarget}`、`Freshness`、`Lto`、`Unit/UnitIndex/UnitInterner`、`UnitDep`（源：src/compiler/mod.rs:77-108）
- F-cargo-109: `pub struct Unit { inner: Rc<UnitInner> }`；`pub struct UnitInner` 字段：`pkg: Package`、`target: Target`、`profile: Profile`、`kind: CompileKind`、`mode: CompileMode`、`features: Vec<InternedString>`、`rustflags: Rc<[String]>`、`rustdocflags: Rc<[String]>`、`links_overrides: Rc<BTreeMap<String, BuildOutput>>`、`artifact: IsArtifact`、`is_std: bool`、`dep_hash: u64`、`artifact_target_for_features: Option<CompileTarget>`、`skip_non_compile_time_dep: bool`（源：src/compiler/unit.rs:53-140）
- F-cargo-110: `pub struct UnitIndex(pub u64)`（serde transparent）；`pub struct UnitInterner`（源：src/compiler/unit.rs:30, 228）
- F-cargo-111: lto.rs：`pub enum Lto { Run(Option<InternedString>), Off, OnlyBitcode, ObjectAndBitcode, OnlyObject }`，文档表格标注各 variant 对应的 rustc flag（`-C lto=foo`、`-C lto=off`、`-C linker-plugin-lto`、`-C embed-bitcode=no`）；`pub fn generate(bcx: &BuildContext<'_, '_>) -> CargoResult<HashMap<Unit, Lto>>`（源：src/compiler/lto.rs:21-46）
- F-cargo-112: links.rs：`pub fn validate_links(resolve: &Resolve, unit_graph: &UnitGraph) -> CargoResult<()>`（src/compiler/links.rs:21，文件内唯一 pub 项）（源：src/compiler/links.rs）

## E. crates/ 成员 crate

- F-cargo-113: crates/cargo-platform：description = "Cargo's representation of a target platform."（src/cfg.rs、error.rs、lib.rs）（源：crates/cargo-platform/Cargo.toml）
- F-cargo-114: crates/cargo-util：description = "Miscellaneous support code used by Cargo."（含 du.rs、paths.rs、read2.rs、registry.rs、sha256.rs）（源：crates/cargo-util/Cargo.toml）
- F-cargo-115: crates/cargo-util-schemas：description = "Deserialization schemas for Cargo"（源：crates/cargo-util-schemas/Cargo.toml）
- F-cargo-116: crates/cargo-util-terminal：description = "Cargo's terminal rendering"（workspace 依赖版本 0.1.3）（源：crates/cargo-util-terminal/Cargo.toml）
- F-cargo-117: crates/crates-io：description = "Helpers for interacting with crates.io"（源：crates/crates-io/Cargo.toml:8-10）
- F-cargo-118: crates/home：description = "Shared definitions of home directories."，crate 内 version = "0.5.14"（workspace.dependencies 声明为 "0.5.12"）；源码 src/{env.rs, lib.rs}（源：crates/home/Cargo.toml）
- F-cargo-119: crates/mdman：description = "Creates a man page from markdown."（源：crates/mdman/Cargo.toml）
- F-cargo-120: crates/rustfix：description = "Automatically apply the suggestions made by rustc"（源：crates/rustfix/Cargo.toml）
- F-cargo-121: crates/build-rs：description = "API for writing Cargo `build.rs` files"（源：crates/build-rs/Cargo.toml）
- F-cargo-122: crates 其他成员 description：cargo-test-macro = "Helper proc-macro for Cargo's testsuite."、cargo-test-support = "Testing framework for Cargo's testsuite."；resolver-tests、build-rs-test-lib、semver-check、xtask-*（5 个）无 description 字段（源：crates/*/Cargo.toml）
- F-cargo-123: credential/ 目录 5 个 crate：cargo-credential（description = "A library to assist writing Cargo credential helpers."，version 0.4.11）、cargo-credential-1password（"A Cargo credential process that stores tokens in a 1password vault."）、cargo-credential-libsecret（"A Cargo credential process that stores tokens with GNOME libsecret."，version 0.5.11）、cargo-credential-macos-keychain（"A Cargo credential process that stores tokens in a macOS keychain."，version 0.4.26）、cargo-credential-wincred（"A Cargo credential process that stores tokens with Windows Credential Manager."，version 0.4.26）（源：credential/*/Cargo.toml）

## F. credential 与认证

- F-cargo-124: credential/README.md 原文："cargo-credential is a generic library to assist writing a credential process. The other directories contain implementations that integrate with specific credential systems."（源：credential/README.md）
- F-cargo-125: `pub trait Credential { fn perform(&self, registry: &RegistryInfo<'_>, action: &Action<'_>, args: &[&str]) -> Result<CredentialResponse, Error>; }`；`pub fn main(credential: impl Credential)`（源：credential/cargo-credential/src/lib.rs:222-240）
- F-cargo-126: `pub enum CredentialResponse { Get { token: Secret<String>, cache: CacheControl, operation_independent: bool }, Login, Logout, Unknown }`（serde tag "kind", kebab-case, non_exhaustive）（源：credential/cargo-credential/src/lib.rs:184-195）
- F-cargo-127: `pub enum CacheControl { Never, Expires { expiration: OffsetDateTime }, Session, Unknown }`；`pub const PROTOCOL_VERSION_1: u32 = 1;`，注释："Credential process JSON protocol version."（源：credential/cargo-credential/src/lib.rs:200-221）
- F-cargo-128: cargo-credential 其他公开类型：CredentialHello、UnsupportedCredential、CredentialRequest、RegistryInfo、Action、LoginOptions、Operation（源：credential/cargo-credential/src/lib.rs:60-184）
- F-cargo-129: util/auth/mod.rs `credential_provider()`：默认 provider 为 `vec![vec!["cargo:token"]]`；`gctx.cli_unstable().asymmetric_token` 时为 `vec![vec!["cargo:token"], vec!["cargo:paseto"]]`；全局配置键 `registry.global-credential-providers`（读取后 `.rev()` 逐项 `resolve_credential_alias`）；registry 专属 `credential-provider` 覆盖全局（源：src/util/auth/mod.rs:28-97）
- F-cargo-130: util/credential/mod.rs 文档："Built-in Cargo credential providers"，子模块 adaptor/paseto/process/token（源：src/util/credential/mod.rs:1-8）

## G. 文档资产

- F-cargo-131: doc/man/ 含 37 个 .md 手册文件：cargo.md 及 cargo-add/bench/build/check/clean/doc/fetch/fix/generate-lockfile/help/info/init/install/locate-project/login/logout/metadata/new/owner/package/pkgid/publish/remove/report/report-future-incompatibilities/run/rustc/rustdoc/search/test/tree/uninstall/update/vendor/version/yank.md；另有 includes/ 目录（options-display/index/jobs/locked/new/profile/registry/release/targets/test/timings/token.md 与 section-features.md 共 13 个片段）（源：doc/man/ 目录）
- F-cargo-132: doc/book/src/SUMMARY.md 四大部分：Getting Started（installation、first-steps）、Cargo Guide（11 小节，含 why-cargo-exists、cargo-toml-vs-cargo-lock、build-performance）、Cargo Reference（manifest/workspaces/dependencies/features/profiles/config/environment-variables/build-scripts/build-cache/pkgid-spec/external-tools/registries（下含 registry-authentication → credential-provider-protocol、running-a-registry → registry-index + registry-web-api）/semver/future-incompat-report/timings/lints/unstable）、Cargo Commands（general/build/manifest/package/misc 分组）（源：doc/book/src/SUMMARY.md）
- F-cargo-133: doc/book/src/ 子目录：appendix（glossary.md）、commands（cargo-fmt.md、cargo-miri.md、index.md）、getting-started、guide、images、reference（源：doc/book/src/ 目录）
- F-cargo-134: doc/contrib/（贡献者指南，book.toml 独立）：src 含 process/{index,llm-usage,release,rfc,security,unstable}.md、tests/{crater,index,profiling,running,writing}.md、design.md、issues.md、team.md（源：doc/contrib/ 目录）

## H. 构建与配置

- F-cargo-135: 根 build.rs `fn main()` 调用三个函数：`commit_info()`、`compress_man()`、`windows_manifest()`，并输出 `cargo:rustc-env=RUST_HOST_TARGET={target}`（源：build.rs:7-17）
- F-cargo-136: build.rs `compress_man()` 将 `etc/man/*.{1}` 与 `doc/man/generated_txt/*.txt` 打包为 `OUT_DIR/man.tgz`（tar `HeaderMode::Deterministic` + gzip `Compression::best()`），每个文件输出 `cargo:rerun-if-changed`（源：build.rs:19-52）
- F-cargo-137: build.rs `commit_info()`：环境变量 `CFG_OMIT_GIT_HASH` 存在时直接返回；否则从 `git log -1 --date=short --format="%H %h %cd"` 或 rustc source tarball 的 `git-commit-info` 文件读取，输出 `CARGO_COMMIT_HASH`/`CARGO_COMMIT_SHORT_HASH`/`CARGO_COMMIT_DATE`（源：build.rs:118-136）
- F-cargo-138: build.rs `windows_manifest()`：仅 `target_os = "windows"` 且 `target_env = "msvc"` 时嵌入 `windows.manifest.xml`（`cargo:rustc-link-arg-bin=cargo=/MANIFEST:EMBED` 与 `/MANIFESTINPUT`，并加 `/WX`）（源：build.rs:142-162）
- F-cargo-139: .cargo/config.toml `[alias]` 段定义 5 个 xtask 别名：`build-man`、`stale-label`、`bump-check`、`lint-docs`、`spellcheck`（均形如 `run --package xtask-* --`）；`[env]` 段设置 `CARGO_RUSTC_CURRENT_DIR = { value = "", relative = true }`（注释提及 snapbox polyfill 与 rust-lang/rust 仓库）（源：.cargo/config.toml）
- F-cargo-140: tests/ 目录布局：`tests/testsuite/`（集成测试套件）与 `tests/build-std/main.rs`；testsuite/main.rs 以 `mod` 声明 120+ 测试模块，实测前 120 行含：advanced_env、alt_registry、artifact_dep、artifact_dir、bad_config、bad_manifest_path、bench、binary_name、build、build_analysis、build_dir、build_dir_fine_grain_locking、build_dir_legacy、build_script、build_script_env、build_script_extra_link_arg、build_scripts_multiple、cache_lock、cache_messages、cargo、cargo_add、cargo_alias_config、cargo_bench、cargo_build、cargo_check、cargo_clean、cargo_command、cargo_config、cargo_doc、cargo_env_config、cargo_features、cargo_fetch、cargo_fix、cargo_generate_lockfile、cargo_git_checkout、cargo_help、cargo_info、cargo_init、cargo_install、cargo_locate_project、cargo_login、cargo_logout、cargo_metadata、cargo_new、cargo_owner、cargo_package、cargo_pkgid、cargo_publish、cargo_read_manifest、cargo_remove、cargo_report、cargo_report_future_incompat、cargo_report_rebuilds、cargo_report_sessions、cargo_report_timings、cargo_run、cargo_rustc、cargo_rustdoc、cargo_search、cargo_targets、cargo_test、cargo_tree、cargo_uninstall、cargo_update、cargo_vendor、cargo_verify_project、cargo_version、cargo_yank、cfg、check、check_cfg、clean、clean_legacy_layout、collisions、compile_time_deps、concurrent、config、config_cli、config_include、corrupt_git、credential_process、cross_compile、cross_publish、custom_target、death、dep_info、diagnostics、direct_minimal_versions、directory、doc、docscrape、edition、error、feature_unification、features、features2、features_namespaced、fetch、fix、fix_n_times、freshness、freshness_checksum、freshness_mtime、future_incompat_report、generate_lockfile、git、git_auth、git_gc、git_shallow、glob_targets、global_cache_tracker、help、hint_msrv、hints、https、inheritable_workspace_fields、install、install_upgrade、jobserver、lints…（源：tests/testsuite/main.rs）
- F-cargo-141: benches/ 布局：`benchsuite/`（Cargo.toml + src/lib.rs）、`capture/`（src/main.rs）、`workspaces/`（9 个 .tgz 样本：cargo、diem、empty、gecko-dev、rust、servo、substrate、tikv、toml-rs）（源：benches/ 目录）
- F-cargo-142: benches/README.md（原文）："This uses [Criterion] for running benchmarks"；三类基准："global_cache_tracker — Benchmarks saving data to the global cache tracker database"、"resolve — Benchmarks the resolver against simulations of real-world workspaces"、"workspace_initialization — Benchmarks initialization of a workspace"（源：benches/README.md:16-24）
- F-cargo-143: 仓库级 CI/工具链文件：.github/workflows/ 含 audit.yml、contrib.yml、main.yml、release.yml；ci/ 含 generate.py、validate-man.sh、validate-version-bump.sh、fetch-smoke-test.sh 等；etc/ 含 cargo.bashcomp.sh 与 _cargo（源：.github/、ci/、etc/ 目录）

## 附：版本与 git 基线

- F-cargo-144: 版本推导链：`cargo --version` 显示 `1.{CARGO_PKG_VERSION_MINOR - 1}.{CARGO_PKG_VERSION_PATCH}`（当前 Cargo.toml version = "0.101.0" → 二进制显示 1.100.0），或 bootstrap 注入的 `CFG_RELEASE`；commit 信息来自 build.rs 注入的 `CARGO_COMMIT_HASH`/`CARGO_COMMIT_SHORT_HASH`/`CARGO_COMMIT_DATE`（源：src/version.rs:54-98）
