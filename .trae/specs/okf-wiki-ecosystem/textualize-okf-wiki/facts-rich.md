---
source: external/dao/action/Textualize/rich/rich/
stage: R
repo: rich
fact_prefix: F-R
---

# facts-rich.md — rich 核心模块事实登记（R 阶段）

> 仅登记代码中可直接验证的事实（类名、方法签名、参数、继承关系、数据流），不含推断。
> 源码根路径：`external/dao/action/Textualize/rich/rich/`

## protocol.py

- **F-R-001** `is_renderable(check_object: Any) -> bool`：返回 `isinstance(check_object, str) or hasattr(check_object, "__rich__") or hasattr(check_object, "__rich_console__")`。〔external/dao/action/Textualize/rich/rich/protocol.py〕
- **F-R-002** `rich_cast(renderable: object) -> "RenderableType"`：while 循环中反复调用对象的 `__rich__()`；用 `rich_visited_set: Set[type]` 记录已出现类型以跳出循环；模块级常量 `_GIBBERISH = "aihwerij235234ljsdnp34ksodfipwoe234234jlskjdf"`，若对象 `hasattr(renderable, _GIBBERISH)` 则返回 `repr(renderable)`。〔external/dao/action/Textualize/rich/rich/protocol.py〕

## measure.py

- **F-R-003** `class Measurement(NamedTuple)`，字段 `minimum: int`、`maximum: int`；属性 `span` 返回 `maximum - minimum`。〔external/dao/action/Textualize/rich/rich/measure.py〕
- **F-R-004** `Measurement` 方法：`normalize() -> Measurement`、`with_maximum(width: int)`、`with_minimum(width: int)`、`clamp(min_width: Optional[int] = None, max_width: Optional[int] = None)`。〔external/dao/action/Textualize/rich/rich/measure.py〕
- **F-R-005** `Measurement.get(cls, console, options, renderable) -> Measurement`（classmethod）：`options.max_width < 1` 时返回 `Measurement(0, 0)`；str 先经 `console.render_str(renderable, markup=options.markup, highlight=False)`；再 `rich_cast`；存在 `__rich_measure__` 时调用并 `.normalize().with_maximum(_max_width)`，否则返回 `Measurement(0, _max_width)`；非可渲染对象抛 `errors.NotRenderableError`。〔external/dao/action/Textualize/rich/rich/measure.py〕
- **F-R-006** `measure_renderables(console, options, renderables: Sequence["RenderableType"]) -> Measurement`：空序列返回 `Measurement(0, 0)`；否则对每个 renderable 调 `Measurement.get`，取 minimum 最大者与 maximum 最大者组合。〔external/dao/action/Textualize/rich/rich/measure.py〕

## markup.py

- **F-R-007** 模块级正则 `RE_TAGS = re.compile(r"""((\\*)\[([a-z#/@][^[]*?)])""", re.VERBOSE)` 与 `RE_HANDLER = re.compile(r"^([\w.]*?)(\(.*?\))?$")`。〔external/dao/action/Textualize/rich/rich/markup.py〕
- **F-R-008** `class Tag(NamedTuple)`，字段 `name: str`、`parameters: Optional[str]`；属性 `markup` 返回 `"[{name}]"` 或 `"[{name}={parameters}]"`。〔external/dao/action/Textualize/rich/rich/markup.py〕
- **F-R-009** `escape(markup: str, _escape: _EscapeSubMethod = re.compile(r"(\\*)(\[[a-z#/@][^[]*?])").sub) -> str`：经内部函数 `escape_backslashes` 将匹配反斜杠加倍转义；若结果以单个 `\` 结尾则再补一个 `\`。〔external/dao/action/Textualize/rich/rich/markup.py〕
- **F-R-010** `_parse(markup: str) -> Iterable[Tuple[int, Optional[str], Optional[Tag]]]`：基于 `RE_TAGS.finditer` 产出 `(position, text, tag)` 三元组；转义反斜杠按 `divmod(len(escapes), 2)` 拆分；标签文本按 `partition("=")` 拆出 name 与 parameters。〔external/dao/action/Textualize/rich/rich/markup.py〕
- **F-R-011** `render(markup: str, style: Union[str, Style] = "", emoji: bool = True, emoji_variant: Optional[EmojiVariant] = None) -> Text`：无 `[` 时直接返回 `Text(...)`；否则维护 `style_stack: List[Tuple[int, Tag]]` 与 `spans: List[Span]`；以 `/` 开头的标签为闭合标签（显式闭合经 `pop_style`，隐式闭合经 `pop()`），失配抛 `MarkupError`；以 `@` 开头的开标签参数经 `ast.literal_eval` 解析，最终 `text.spans = sorted(spans[::-1], key=attrgetter("start"))`。〔external/dao/action/Textualize/rich/rich/markup.py〕

## highlighter.py

- **F-R-012** `_combine_regex(*regexes: str) -> str` 返回 `"|".join(regexes)`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-013** `class Highlighter(ABC)`：`__call__(self, text: Union[str, Text]) -> Text`——str 包装为 `Text(text)`，Text 取 `text.copy()`，其他类型抛 `TypeError`；抽象方法 `highlight(self, text: Text) -> None`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-014** `class NullHighlighter(Highlighter)`：`highlight` 方法体为空。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-015** `class RegexHighlighter(Highlighter)`：类变量 `highlights: ClassVar[Sequence[str]] = []`、`base_style: ClassVar[str] = ""`；`highlight` 对每个正则调用 `text.highlight_regex(re_highlight, style_prefix=self.base_style)`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-016** `class ReprHighlighter(RegexHighlighter)`：`base_style = "repr."`；`highlights` 含命名分组正则 `tag_start`、`attrib_name`、`brace`，以及经 `_combine_regex` 组合的 `ipv4`、`ipv6`、`eui64`、`eui48`、`uuid`、`call`、`bool_true`/`bool_false`/`none`、`ellipsis`、`number_complex`、`number`、`path`/`filename`、`str`、`url`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-017** `class JSONHighlighter(RegexHighlighter)`：`base_style = "json."`；类属性 `JSON_STR = r"(?<![\\\w])(?P<str>b?\".*?(?<!\\)\")"`、`JSON_WHITESPACE = {" ", "\n", "\r", "\t"}`；`highlight` 先调 `super().highlight(text)`，再扫描字符串后随 `:` 的位置追加 `Span(start, end, "json.key")`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕
- **F-R-018** `class ISO8601Highlighter(RegexHighlighter)`：`base_style = "iso8601."`；`highlights` 为 12 条以 `^...$` 锚定的正则，命名分组含 `year`、`month`、`day`、`date`、`week`、`time`、`hour`、`minute`、`second`、`timezone`、`frac`、`ms`、`hyphen`。〔external/dao/action/Textualize/rich/rich/highlighter.py〕

## style.py

- **F-R-019** `class Style` 构造签名：`Style(*, color=None, bgcolor=None, bold=None, dim=None, italic=None, underline=None, blink=None, blink2=None, reverse=None, conceal=None, strike=None, underline2=None, frame=None, encircle=None, overline=None, link=None, meta=None)`（全部 `Optional`）；内部以位掩码 `_set_attributes`/`_attributes` 记录 13 个布尔属性（bold=1、dim=2、italic=4、underline=8、blink=16、blink2=32、reverse=64、conceal=128、strike=256、underline2=512、frame=1024、encircle=2048、overline=4096）。〔external/dao/action/Textualize/rich/rich/style.py〕
- **F-R-020** `Style` classmethods：`null()` 返回模块级 `NULL_STYLE`；`from_color(color: Optional[Color] = None, bgcolor: Optional[Color] = None)`；`from_meta(meta: Optional[Dict[str, Any]])`；`on(meta=None, **handlers)`；`parse(style_definition: str) -> Style`；`combine(styles: Iterable[Style])`；`chain(*styles: Style)`；`normalize(style: str) -> str`；`pick_first(*values: Optional[StyleType]) -> StyleType`。〔external/dao/action/Textualize/rich/rich/style.py〕
- **F-R-021** `Style` 实例方法/属性：`render(text, color_system, reset=True)`、`copy()`、`clear_meta_and_links()`、`update_link(link: Optional[str] = None)`、`get_html_style(theme: Optional[TerminalTheme] = None)`、`_make_ansi_codes(color_system)`；属性 `color`、`bgcolor`、`link`、`meta`、`link_id`、`transparent_background`、`background_style`、`without_color`；支持 `__add__`（经 `_add`）。〔external/dao/action/Textualize/rich/rich/style.py〕
- **F-R-022** `class StyleStack`：`__init__(self, default_style: Style)`；属性 `current`；方法 `push(style: Style)`、`pop() -> Style`。〔external/dao/action/Textualize/rich/rich/style.py〕

## segment.py

- **F-R-023** `class ControlType(IntEnum)` 共 16 个成员：BELL=1、CARRIAGE_RETURN=2、HOME=3、CLEAR=4、SHOW_CURSOR=5、HIDE_CURSOR=6、ENABLE_ALT_SCREEN=7、DISABLE_ALT_SCREEN=8、CURSOR_UP=9、CURSOR_DOWN=10、CURSOR_FORWARD=11、CURSOR_BACKWARD=12、CURSOR_MOVE_TO_COLUMN=13、CURSOR_MOVE_TO=14、ERASE_IN_LINE=15、SET_WINDOW_TITLE=16。〔external/dao/action/Textualize/rich/rich/segment.py〕
- **F-R-024** `class Segment(NamedTuple)`（`@rich_repr()` 装饰），字段 `text: str`、`style: Optional[Style] = None`、`control: Optional[Sequence[ControlCode]] = None`；属性 `cell_length` 在 control 非空时返回 0，否则返回 `cell_len(text)`；属性 `is_control`。〔external/dao/action/Textualize/rich/rich/segment.py〕
- **F-R-025** `Segment` 类方法（均操作 `Iterable[Segment]`）：`line()`、`apply_style`、`filter_control`、`split_lines`、`split_lines_terminator`、`split_and_crop_lines`、`adjust_line_length`、`get_line_length(line)`、`get_shape(lines)`、`set_shape`、`align_top`、`align_bottom`、`align_middle`、`simplify`、`strip_links`、`strip_styles`、`remove_color`、`divide`；实例方法 `split_cells(cut: int) -> Tuple[Segment, Segment]`（内部 `_split_cells` 带 `@lru_cache(1024 * 16)`）。〔external/dao/action/Textualize/rich/rich/segment.py〕
- **F-R-026** `class Segments`：`__init__(self, segments: Iterable[Segment], new_lines: bool = False)`，实现 `__rich_console__`；`class SegmentLines`：`__init__(self, lines: Iterable[List[Segment]], new_lines: bool = False)`，实现 `__rich_console__`。〔external/dao/action/Textualize/rich/rich/segment.py〕

## text.py

- **F-R-027** `class Span(NamedTuple)`，字段 `start: int`、`end: int`、`style: Union[str, Style]`；方法 `split(offset)`、`move(offset)`、`right_crop(offset)`、`extend(cells)`；`__bool__` 返回 `self.end > self.start`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-028** `class Text(JupyterMixin)`：`__slots__` 为 `_text, style, justify, overflow, no_wrap, end, tab_size, _spans, _length`；构造签名 `Text(text: str = "", style: Union[str, Style] = "", *, justify=None, overflow=None, no_wrap=None, end="\n", tab_size=None, spans=None)`；构造时对输入执行 `strip_control_codes(text)`，`_text` 存为单元素列表。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-029** `Text` classmethods：`from_markup(...)`、`from_ansi(...)`、`styled(...)`、`assemble(...)`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-030** `Text` 样式操作方法：`stylize(...)`、`stylize_before(...)`、`apply_meta(...)`、`on(meta=None, **handlers)`、`get_style_at_offset(console, offset)`、`extend_style(spaces)`、`copy_styles(text)`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-031** `Text` 高亮方法：`highlight_regex(...)`、`highlight_words(...)`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-032** `Text` 渲染与测量：`__rich_console__(self, console, options)`、`__rich_measure__(self, console, options)`、`render(self, console, end: str = "") -> Iterable[Segment]`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-033** `Text` 追加与拼接：`append(...)`、`append_text(text)`、`append_tokens(...)`、`join(lines: Iterable[Text]) -> Text`、`__add__(self, other)`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-034** `Text` 形变方法：`truncate(...)`、`pad(count, character=" ")`、`pad_left(count, character=" ")`、`pad_right(count, character=" ")`、`align(align, width, character=" ")`、`rstrip()`、`rstrip_end(size)`、`set_length(new_length)`、`right_crop(amount=1)`、`expand_tabs(tab_size=None)`、`remove_suffix(suffix)`。〔external/dao/action/Textualize/rich/rich/text.py〕
- **F-R-035** `Text.wrap(self, console, width: int, *, justify=None, overflow=None, tab_size: int = 8, no_wrap=None) -> Lines`；`fit(self, width: int) -> Lines`；`split(...)`；`divide(self, offsets: Iterable[int]) -> Lines`；`detect_indentation() -> int`；`with_indent_guides(...)`。〔external/dao/action/Textualize/rich/rich/text.py〕

## console.py

- **F-R-036** `class ConsoleDimensions(NamedTuple)`，字段 `width: int`、`height: int`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-037** `class ConsoleOptions`（`@dataclass`）：字段 `size`、`legacy_windows`、`min_width`、`max_width`、`is_terminal`、`encoding`、`max_height`，以及带默认值字段 `justify=None`、`overflow=None`、`no_wrap=False`、`highlight=None`、`markup=None`、`height=None`；属性 `ascii_only` 返回 `not self.encoding.startswith("utf")`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-038** `ConsoleOptions` 方法：`copy()`；`update(*, width, min_width, max_width, justify, overflow, no_wrap, highlight, markup, height)`（未改参数以哨兵 `NO_CHANGE`/`NoChange` 标记）；`update_width(width)`；`update_height(height)`；`reset_height()`；`update_dimensions(width, height)`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-039** 协议类型定义：`class RichCast(Protocol)`（`@runtime_checkable`，方法 `__rich__`）、`class ConsoleRenderable(Protocol)`（`@runtime_checkable`，方法 `__rich_console__(self, console, options) -> RenderResult`）；`RenderableType = Union[ConsoleRenderable, RichCast, str]`；`RenderResult = Iterable[Union[RenderableType, Segment]]`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-040** `class RenderHook(ABC)`：抽象方法 `process_renderables(...)`；`class Group`：`__init__(self, *renderables, fit: bool = True)`，属性 `renderables`，实现 `__rich_measure__` 与 `__rich_console__`；装饰器工厂 `def group(fit: bool = True)`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-041** 上下文类：`class Capture`（`__init__(console)`、`get() -> str`）、`class ThemeContext`（`__init__(console, theme, inherit=True)`）、`class PagerContext`、`class ScreenContext`（含 `update(...)`）；异常 `class CaptureError(Exception)`；辅助类 `class NewLine`（`__init__(count: int = 1)`）与 `class ScreenUpdate`（`__init__(lines, x, y)`）。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-042** `Console.__init__` 签名（全部关键字参数）：`color_system: Optional[Literal["auto", "standard", "256", "truecolor", "windows"]] = "auto"`、`force_terminal=None`、`force_jupyter=None`、`force_interactive=None`、`soft_wrap=False`、`theme=None`、`stderr=False`、`file=None`、`quiet=False`、`width=None`、`height=None`、`style=None`、`no_color=None`、`tab_size=8`、`record=False`、`markup=True`、`emoji=True`、`emoji_variant=None`、`highlight=True`、`log_time=True`、`log_path=True`、`log_time_format="[%X]"`、`highlighter=ReprHighlighter()`、`legacy_windows=None`、`safe_box=True`、`get_datetime=None`、`get_time=None`、`_environ=None`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-043** `Console.__init__` 内部赋值：`self.get_time = get_time or monotonic`、`self.get_datetime = get_datetime or datetime.now`、`self.highlighter = highlighter or _null_highlighter`；`no_color` 未显式给出时取 `self._environ.get("NO_COLOR", "") != ""`；宽度未给出时读环境变量 `COLUMNS`，高度读 `LINES`；Jupyter 模式下读 `JUPYTER_COLUMNS`/`JUPYTER_LINES`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-044** `Console.print(self, *objects, sep=" ", end="\n", style=None, justify=None, overflow=None, no_wrap=None, emoji=None, markup=None, highlight=None, width=None, height=None, crop=True, soft_wrap=None, new_line_start=False) -> None`；无对象且 `end == "\n"` 时以 `NewLine()` 替代。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-045** `Console.measure(self, renderable, *, options=None) -> Measurement`：调用 `Measurement.get(self, options or self.options, renderable)`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-046** `Console.render(self, renderable, options=None) -> Iterable[Segment]`：先 `rich_cast`；有 `__rich_console__` 则调用之；str 则经 `render_str(...)`；否则抛 `errors.NotRenderableError`；对产出逐项判断，非 `Segment` 递归 `self.render(...)`（递归前 `_options = _options.reset_height()`）。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-047** `Console` 其他渲染/输出方法：`render_lines(...)`、`render_str(...)`、`get_style(...)`、`rule(...)`、`control(*control)`、`out(...)`、`print_json(...)`、`update_screen(...)`、`update_screen_lines(...)`、`print_exception(...)`、`log(...)`、`input(...)`、`line(count=1)`、`clear(home=True)`、`status(...)`、`show_cursor(show=True)`、`set_alt_screen(enable=True)`、`set_window_title(title)`、`screen(...)`、`bell()`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-048** `Console` 导出方法：`export_text(*, clear=True, styles=False)`、`save_text(...)`、`export_html(...)`、`save_html(...)`、`export_svg(...)`、`save_svg(...)`；状态/缓冲方法：`begin_capture()`、`end_capture()`、`capture()`、`push_theme(theme, *, inherit=True)`、`pop_theme()`、`use_theme(theme, *, inherit=True)`、`push_render_hook(hook)`、`pop_render_hook()`、`set_live(live)`、`clear_live()`、`on_broken_pipe()`、`_check_buffer()`、`_write_buffer()`、`_render_buffer(buffer)`。〔external/dao/action/Textualize/rich/rich/console.py〕
- **F-R-049** `Console` 属性：`file`（getter/setter）、`color_system`、`encoding`、`is_terminal`、`is_dumb_terminal`、`is_alt_screen`、`options`、`size`（getter/setter）、`width`（getter/setter）、`height`（getter/setter）。〔external/dao/action/Textualize/rich/rich/console.py〕

## table.py

- **F-R-050** `class Column`（`@dataclass`）：字段 `header=""`、`footer=""`、`header_style=""`、`footer_style=""`、`style=""`、`justify="left"`、`vertical="top"`、`overflow="ellipsis"`、`width=None`、`min_width=None`、`max_width=None`、`ratio=None`、`no_wrap=False`、`highlight=False`、`_index=0`、`_cells: List = field(default_factory=list)`；方法 `copy()`（`replace(self, _cells=[])`）、属性 `cells`、属性 `flexible`（`self.ratio is not None`）。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-051** `class Row`（`@dataclass`）：字段 `style: Optional[StyleType] = None`、`end_section: bool = False`。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-052** `class Table(JupyterMixin)` 构造签名：`Table(*headers: Union[Column, str], title=None, caption=None, width=None, min_width=None, box=box.HEAVY_HEAD, safe_box=None, padding=(0, 1), collapse_padding=False, pad_edge=True, expand=False, show_header=True, show_footer=False, show_edge=True, show_lines=False, leading=0, style="none", row_styles=None, header_style="table.header", footer_style="table.footer", border_style=None, title_style=None, caption_style=None, title_justify="center", caption_justify="center", highlight=False)`；str 类型 header 经 `self.add_column(header=header)` 转换。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-053** `Table.grid(cls, *headers, padding=0, collapse_padding=True, pad_edge=False, expand=False) -> Table`（classmethod）：内部以 `box=None, show_header=False, show_footer=False, show_edge=False` 构造。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-054** `Table.add_column(self, header="", footer="", *, header_style=None, highlight=None, footer_style=None, style=None, justify="left", vertical="top", overflow="ellipsis", width=None, min_width=None, max_width=None, ratio=None, no_wrap=False) -> None`。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-055** `Table.add_row(self, *renderables: Optional[RenderableType], style=None, end_section=False) -> None`：内部 `add_cell` 将 renderable 追加到 `column._cells`；`add_section(self) -> None`。〔external/dao/action/Textualize/rich/rich/table.py〕
- **F-R-056** `Table` 渲染与宽度计算：`__rich_console__`、`__rich_measure__`、`_calculate_column_widths(...)`、`_collapse_widths(...)`、`_get_cells(...)`、`_get_padding_width(column_index)`、`_measure_column(...)`、`_render(...)`；属性 `row_count`、`padding`（getter/setter）、`expand`（getter/setter）、`_extra_width`；方法 `get_row_style(console, index)`。〔external/dao/action/Textualize/rich/rich/table.py〕

## markdown.py

- **F-R-057** `class MarkdownElement`：类变量 `new_line: ClassVar[bool] = True`；方法 `create(cls, markdown, token)`（classmethod，返回 `cls()`）、`on_enter(context)`、`on_text(context, text)`、`on_leave(context)`、`on_child_close(context, child) -> bool`（默认返回 True）、`__rich_console__(console, options)`（返回 `()`）。〔external/dao/action/Textualize/rich/rich/markdown.py〕
- **F-R-058** 元素类继承关系：`UnknownElement(MarkdownElement)`；`TextElement(MarkdownElement)`（`style_name = "none"`）；`Paragraph(TextElement)`（`style_name = "markdown.paragraph"`）；`Heading(TextElement)`；`CodeBlock(TextElement)`；`BlockQuote(TextElement)`；`HorizontalRule(MarkdownElement)`；`TableElement`/`TableHeaderElement`/`TableBodyElement`/`TableRowElement`/`TableDataElement`/`ListElement` 均继承 `MarkdownElement`；`ListItem(TextElement)`；`Link(TextElement)`；`ImageItem(TextElement)`。〔external/dao/action/Textualize/rich/rich/markdown.py〕
- **F-R-059** `class HeadingFormat`（`@dataclass`）：字段 `justify: JustifyMethod = "left"`、`style: str = ""`；`Heading` 类变量 `LEVEL_ALIGN: ClassVar[dict[str, JustifyMethod]]` 映射 h1→"center"，h2-h6→"left"。〔external/dao/action/Textualize/rich/rich/markdown.py〕
- **F-R-060** `class MarkdownContext`：`__init__(self, console, options, style, inline_code_lexer=None, inline_code_theme="monokai")`；持有 `style_stack: StyleStack` 与 `stack: Stack[MarkdownElement]`；方法 `on_text(text, node_type)`（node_type 为 `fence`/`code_inline` 且 `_syntax` 非空时经 `Syntax.highlight`）、`enter_style(style_name)`、`leave_style()`；属性 `current_style`。〔external/dao/action/Textualize/rich/rich/markdown.py〕
- **F-R-061** `class Markdown(JupyterMixin)`：`__init__(self, markup: str, code_theme: str = "monokai", justify=None, style="none", hyperlinks=True, inline_code_lexer=None, inline_code_theme=None)`；构造时 `MarkdownIt().enable("strikethrough").enable("table")` 并 `self.parsed = parser.parse(markup)`；`inline_code_theme` 缺省回落 `code_theme`。〔external/dao/action/Textualize/rich/rich/markdown.py〕
- **F-R-062** `Markdown.elements: ClassVar[dict[str, type[MarkdownElement]]]` 共 16 个键：`paragraph_open`→Paragraph、`heading_open`→Heading、`fence`→CodeBlock、`code_block`→CodeBlock、`blockquote_open`→BlockQuote、`hr`→HorizontalRule、`bullet_list_open`→ListElement、`ordered_list_open`→ListElement、`list_item_open`→ListItem、`image`→ImageItem、`table_open`→TableElement、`tbody_open`→TableBodyElement、`thead_open`→TableHeaderElement、`tr_open`→TableRowElement、`td_open`→TableDataElement、`th_open`→TableDataElement；`inlines = {"em", "strong", "code", "s"}`；方法 `_flatten_tokens(tokens)` 与 `__rich_console__(console, options)`。〔external/dao/action/Textualize/rich/rich/markdown.py〕

## progress.py

- **F-R-063** 模块级类型：`TaskID = NewType("TaskID", int)`；`ProgressType = TypeVar("ProgressType")`；`GetTimeCallable = Callable[[], float]`；`_I = typing.TypeVar("_I", TextIO, BinaryIO)`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-064** `class ProgressColumn(ABC)`：类属性 `max_refresh: Optional[float] = None`；`__init__(self, table_column: Optional[Column] = None)`；`get_table_column() -> Column`（返回 `self._table_column or Column()`）；`__call__(self, task) -> RenderableType`（按 `max_refresh` 与 `_renderable_cache` 缓存）；抽象方法 `render(self, task) -> RenderableType`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-065** `ProgressColumn` 子类：`RenderableColumn`（`__init__(renderable="", *, table_column=None)`）、`SpinnerColumn`（`__init__(spinner_name="dots", style="progress.spinner", speed=1.0, finished_text=" ", table_column=None)`，含 `set_spinner(...)`）、`TextColumn`（`__init__(text_format, style="none", justify="left", markup=True, highlighter=None, table_column=None)`，render 中 `self.text_format.format(task=task)`）、`BarColumn`（`__init__(bar_width=40, style="bar.back", complete_style="bar.complete", finished_style="bar.finished", pulse_style="bar.pulse", table_column=None)`，render 返回 `ProgressBar(...)`）、`TimeElapsedColumn`、`TaskProgressColumn(TextColumn)`、`TimeRemainingColumn`、`FileSizeColumn`、`TotalFileSizeColumn`、`MofNCompleteColumn`、`DownloadColumn`（`__init__(binary_units=False, table_column=None)`）、`TransferSpeedColumn`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-066** `class ProgressSample(NamedTuple)`：字段 `timestamp: float`、`completed: float`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-067** `class Task`（`@dataclass`）：字段 `id: TaskID`、`description: str`、`total: Optional[float]`、`completed: float`、`_get_time: GetTimeCallable`、`finished_time=None`、`visible=True`、`fields: Dict[str, Any] = field(default_factory=dict)`、`start_time`/`stop_time`（`init=False`）、`finished_speed=None`、`_progress: Deque[ProgressSample] = field(default_factory=lambda: deque(maxlen=1000), init=False)`、`_lock: RLock`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-068** `Task` 属性：`started`（`start_time is not None`）、`remaining`（`total - completed`，total 为 None 时返回 None）、`elapsed`、`finished`（`finished_time is not None`）、`percentage`（total 为假值时返回 0.0，结果经 `min(100.0, max(0.0, ...))` 夹取）、`speed`、`time_remaining`（`ceil(remaining / speed)`）；方法 `get_time()`、`_reset()`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-069** `class Progress(JupyterMixin)` 构造签名：`Progress(*columns: Union[str, ProgressColumn], console=None, auto_refresh=True, refresh_per_second=10, speed_estimate_period=30.0, transient=False, redirect_stdout=True, redirect_stderr=True, get_time=None, disable=False, expand=False)`；首行 `assert refresh_per_second > 0`；内部创建 `self.live = Live(console=console or get_console(), ..., get_renderable=self.get_renderable)`；`self.print = self.console.print`、`self.log = self.console.log`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-070** `Progress.get_default_columns(cls) -> Tuple[ProgressColumn, ...]` 返回 `(TextColumn("[progress.description]{task.description}"), BarColumn(), TaskProgressColumn(), TimeRemainingColumn())`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-071** `Progress` 属性：`console`（返回 `self.live.console`）、`tasks`、`task_ids`、`finished`（所有任务 `task.finished` 全真，空任务字典返回 True）。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-072** `Progress` 任务管理方法：`add_task(self, description, start=True, total=100.0, completed=0, visible=True, **fields) -> TaskID`；`remove_task(task_id)`；`start_task(task_id)`；`stop_task(task_id)`；`update(self, task_id, *, total=None, completed=None, advance=None, description=None, visible=None, refresh=False, **fields)`；`reset(self, task_id, *, start=True, total=None, completed=0, visible=None, description=None, **fields)`；`advance(self, task_id, advance: float = 1)`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-073** `Progress.update` 数据流：`total` 变更时调用 `task._reset()`；超出 `speed_estimate_period` 窗口的样本经 `_progress.popleft()` 移除；`update_completed > 0` 时追加 `ProgressSample(current_time, update_completed)`；`completed >= total` 且 `finished_time is None` 时置 `task.finished_time = task.elapsed`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-074** `Progress` 显示方法：`start()`（`self.live.start(refresh=True)`）、`stop()`、`__enter__`/`__exit__`、`refresh()`（`self.live.refresh()`）、`get_renderable()`（返回 `Group(*self.get_renderables())`）、`get_renderables()`、`make_tasks_table(tasks) -> Table`（经 `Table.grid(*table_columns, padding=(0, 1), expand=self.expand)` 构建；str 列经 `column.format(task=task)`，ProgressColumn 列经 `column(task)`）、`__rich__()`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-075** `Progress.track(self, sequence, total=None, completed=0, task_id=None, description="Working...", update_period=0.1)`：`total` 为 None 时取 `float(length_hint(sequence)) or None`；`live.auto_refresh` 为真时在 `_TrackThread` 内 yield，否则每次 yield 后 `advance(task_id, 1)` + `refresh()`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-076** `Progress.wrap_file(self, file: BinaryIO, total=None, *, task_id=None, description="Reading...") -> BinaryIO`：无法确定 total 时抛 `ValueError`；返回 `_Reader(file, self, task_id, close_handle=False)`。`Progress.open(...)` 支持 mode `"r"`/`"rb"`/`"rt"`（其他抛 `ValueError`），total 缺省取 `stat(file).st_size`，文本模式以 `io.TextIOWrapper` 包装。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-077** 模块级函数 `track(sequence, description="Working...", total=None, completed=0, auto_refresh=True, console=None, transient=False, get_time=None, refresh_per_second=10, style="bar.back", complete_style="bar.complete", finished_style="bar.finished", pulse_style="bar.pulse", update_period=0.1, disable=False, show_speed=True) -> Iterable[ProgressType]`。〔external/dao/action/Textualize/rich/rich/progress.py〕
- **F-R-078** 辅助类：`class _TrackThread(Thread)`（`__init__(progress, task_id, update_period)`，`run` 中 `while not wait(update_period) and self.progress.live.is_started`，支持 `__enter__`/`__exit__`）；`class _Reader(RawIOBase, BinaryIO)`；`class _ReadContext(ContextManager[_I], Generic[_I])`。〔external/dao/action/Textualize/rich/rich/progress.py〕

## live.py

- **F-R-079** `class Live(JupyterMixin, RenderHook)` 构造签名：`Live(renderable=None, *, console=None, screen=False, auto_refresh=True, refresh_per_second=4, transient=False, redirect_stdout=True, redirect_stderr=True, vertical_overflow="ellipsis", get_renderable=None)`；首行 `assert refresh_per_second > 0`；`self.transient = True if screen else transient`；内部持有 `self._live_render = LiveRender(self.get_renderable(), vertical_overflow=vertical_overflow)`。〔external/dao/action/Textualize/rich/rich/live.py〕
- **F-R-080** `Live` 方法：`start(self, refresh: bool = False)`、`stop()`、`__enter__`/`__exit__`、`update(self, renderable, *, refresh=False)`、`refresh()`、`process_renderables(...)`（实现 RenderHook 接口）；属性 `is_started`、`renderable`；私有方法 `_enable_redirect_io()`、`_disable_redirect_io()`；辅助类 `class _RefreshThread(Thread)`（`__init__(live, refresh_per_second)`、`stop()`、`run()`）。〔external/dao/action/Textualize/rich/rich/live.py〕

## layout.py

- **F-R-081** `class LayoutRender(NamedTuple)`：字段 `region: Region`、`render: List[List[Segment]]`；类型别名 `RegionMap = Dict["Layout", Region]`、`RenderMap = Dict["Layout", LayoutRender]`；异常 `class LayoutError(Exception)` 与 `class NoSplitter(LayoutError)`。〔external/dao/action/Textualize/rich/rich/layout.py〕
- **F-R-082** `class Splitter(ABC)`：类属性 `name: str = ""`；抽象方法 `get_tree_icon() -> str` 与 `divide(self, children: Sequence[Layout], region: Region) -> Iterable[Tuple[Layout, Region]]`；`class RowSplitter(Splitter)`（`name = "row"`，经 `ratio_resolve(width, children)` 横向划分）；`class ColumnSplitter(Splitter)`（`name = "column"`，经 `ratio_resolve(height, children)` 纵向划分）。〔external/dao/action/Textualize/rich/rich/layout.py〕
- **F-R-083** `class Layout`（`@rich_repr`）：类属性 `splitters = {"row": RowSplitter, "column": ColumnSplitter}`；构造签名 `Layout(renderable=None, *, name=None, size=None, minimum_size=1, ratio=1, visible=True)`；renderable 为 None 时以 `_Placeholder(self)` 替代；默认 `self.splitter = self.splitters["column"]()`。〔external/dao/action/Textualize/rich/rich/layout.py〕
- **F-R-084** `Layout` 方法：`split(self, *layouts, splitter: Union[Splitter, str] = "column")`（未知 splitter 名抛 `NoSplitter`）、`add_split(*layouts)`、`split_row(*layouts)`、`split_column(*layouts)`、`unsplit()`、`update(renderable)`、`refresh_screen(console, layout_name)`、`get(name) -> Optional[Layout]`、`__getitem__(name)`、`tree()`、`render(console, options) -> RenderMap`、`_make_region_map(width, height) -> RegionMap`、`__rich_console__`；属性 `renderable`、`children`、`map`。〔external/dao/action/Textualize/rich/rich/layout.py〕

## panel.py

- **F-R-085** `class Panel(JupyterMixin)` 构造签名：`Panel(renderable, box=ROUNDED, *, title=None, title_align="center", subtitle=None, subtitle_align="center", safe_box=None, expand=True, style="none", border_style="none", width=None, height=None, padding=(0, 1), highlight=False)`。〔external/dao/action/Textualize/rich/rich/panel.py〕
- **F-R-086** `Panel.fit(cls, renderable, box=ROUNDED, *, ...) -> Panel`（classmethod）：与 `__init__` 同参但强制 `expand=False`；属性 `_title`/`_subtitle`（str 标题经 `Text.from_markup`）；实现 `__rich_console__` 与 `__rich_measure__`。〔external/dao/action/Textualize/rich/rich/panel.py〕

## box.py

- **F-R-087** `class Box`：`__init__(self, box: str, *, ascii: bool = False)`；将 8 行字符串 `box.splitlines()` 逐字符解包为 `top_left/top/top_divider/top_right`、`head_*`、`head_row_*`、`mid_*`、`row_*`、`foot_row_*`、`foot_*`、`bottom_*` 共 32 个字符属性。〔external/dao/action/Textualize/rich/rich/box.py〕
- **F-R-088** `Box` 方法：`substitute(self, options, safe=True) -> Box`（`options.legacy_windows and safe` 时查 `LEGACY_WINDOWS_SUBSTITUTIONS`，`options.ascii_only and not box.ascii` 时替换为 `ASCII`）、`get_plain_headed_box()`（查 `PLAIN_HEADED_SUBSTITUTIONS`）、`get_top(widths)`、`get_row(...)`、`get_bottom(widths)`。〔external/dao/action/Textualize/rich/rich/box.py〕
- **F-R-089** 模块级 Box 常量共 18 个：`ASCII`、`ASCII_DOUBLE_HEAD`、`SQUARE`、`SQUARE_DOUBLE_HEAD`、`MINIMAL`、`MINIMAL_HEAVY_HEAD`、`MINIMAL_DOUBLE_HEAD`、`SIMPLE`、`SIMPLE_HEAD`、`SIMPLE_HEAVY`、`HORIZONTALS`、`ROUNDED`、`HEAVY`、`HEAVY_EDGE`、`HEAVY_HEAD`、`DOUBLE`、`DOUBLE_EDGE`、`MARKDOWN`。〔external/dao/action/Textualize/rich/rich/box.py〕
