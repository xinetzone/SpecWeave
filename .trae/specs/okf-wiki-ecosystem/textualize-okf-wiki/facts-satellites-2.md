# Facts: textual-dev / textual-serve / textual-web（R 阶段·零推测）

- 采集日期：2026-09-01
- 信源根：`external/dao/action/Textualize/`（commit hash 见 `source-versions.md`）
  - textual-dev @ `e563f96f32d582b7cf22622a401f537c40349adc`
  - textual-serve @ `fa5cf5f5b4273c97ed286a55747106701ddc9917`
  - textual-web @ `7d6741c9f7869881722d7d8dcf70a286cc270db9`
- 规则：只写"代码里有什么"；每条标注源码相对路径；无推断词。

## 1. textual-dev（开发工具功能，F-SD-）

- **F-SD-01**：`external/dao/action/Textualize/textual-dev/pyproject.toml`：`[tool.poetry]` name="textual-dev"，version="1.8.0"，description="Development tools for working with Textual"，license="MIT"；依赖 `python = "^3.9"`、`textual = ">=0.86.2"`、`textual_serve = ">=1.0.3"`、`aiohttp = ">=3.8.1"`、`click = ">=8.1.2"`、`msgpack = ">=1.0.3"`、`typing-extensions = "^4.4.0"`。
- **F-SD-02**：`external/dao/action/Textualize/textual-dev/pyproject.toml`：`[tool.poetry.scripts]` 定义 `textual = "textual_dev.cli:run"`。
- **F-SD-03**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py`：`@click.group()` 定义函数 `run`，`@click.version_option(version("textual"))`；文件中注册的子命令有 `console`、`run`、`serve`、`borders`、`easing`、`colors`、`keys`、`diagnose`（8 个）。
- **F-SD-04**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py`：`console` 子命令选项为 `--port`（默认 None，help 文本引用 `DEVTOOLS_PORT`）、`-v`（verbose flag）、`-x/--exclude`（multiple）；函数体调用 `textual_dev.server._run_devtools(verbose=..., exclude=..., port=...)`。
- **F-SD-05**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py`：`run` 子命令参数 `import_name`（metavar "FILE or FILE:APP"），选项 `--dev`、`--host`、`--port`、`--press`、`--screenshot`、`--screenshot-path`、`--screenshot-filename`、`-c/--command`、`-r/--show-return`；`--dev` 时向 features 集合 add("debug") 与 add("devtools") 并写回 `environment["TEXTUAL"]`；host/port/press/screenshot 分别写入环境变量 `TEXTUAL_DEVTOOLS_HOST`、`TEXTUAL_DEVTOOLS_PORT`、`TEXTUAL_PRESS`、`TEXTUAL_SCREENSHOT`、`TEXTUAL_SCREENSHOT_LOCATION`、`TEXTUAL_SCREENSHOT_FILENAME`、`TEXTUAL_SHOW_RETURN`。
- **F-SD-06**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py`：`serve` 子命令默认 `-h/--host "localhost"`、`-p/--port 8000`、`-t/--title "Textual App"`；函数体 `from textual_serve.server import Server`，构造 `Server(run_command, host, port, title=title, public_url=url)` 并调用 `server.serve(debug=dev)`；`_is_python_path(import_name)` 为真时 run_command 为 `f"python {import_name}"`。
- **F-SD-07**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py` 与 `external/dao/action/Textualize/textual-dev/src/textual_dev/previews/__init__.py`：`borders`/`easing`/`colors`/`keys` 子命令分别导入并 `.run()` `BorderApp`/`EasingApp`/`ColorsApp`/`KeysApp`；`previews/__init__.py` 的 `__all__ = ["BorderApp", "ColorsApp", "EasingApp", "KeysApp"]`。
- **F-SD-08**：`external/dao/action/Textualize/textual-dev/src/textual_dev/tools/diagnose.py`：`diagnose()` 依次打印 `"# Textual Diagnostics"` 并调用 `_versions()`（Textual/Rich 版本）、`_python()`、`_os()`、`_term()`、`_console()`；`_section()` 以 Markdown 表格（`| Name | Value |`）格式输出；首行打印 HTML 注释 `<!-- This is valid Markdown, please paste the following directly in to a GitHub issue -->`。
- **F-SD-09**：`external/dao/action/Textualize/textual-dev/src/textual_dev/server.py`：`_make_devtools_aiohttp_app()` 创建 aiohttp `Application`，`app["service"] = DevtoolsService(update_frequency=..., port=..., verbose=..., exclude=...)`，注册路由 `get("/textual-devtools-websocket", websocket_handler)`；模块常量 `DEFAULT_SIZE_CHANGE_POLL_DELAY_SECONDS = 2`；`_run_devtools()` 调用 `run_app(app, port=DEVTOOLS_PORT if port is None else port, ...)`，OSError 时打印 "Couldn't start server"。
- **F-SD-10**：`external/dao/action/Textualize/textual-dev/src/textual_dev/service.py`：`DevtoolsService` 属性 `clients: list[ClientHandler]`、`exclude = {name.upper() for name in exclude}`；`_console_size_poller()` 在 `console.width/height` 变化时调用 `_send_server_info_to_all()`；`send_server_info()` 发送 `{"type": "server_info", "payload": {"width", "height", "verbose"}}`；模块常量 `QUEUEABLE_TYPES = {"client_log", "client_spillover"}`。
- **F-SD-11**：`external/dao/action/Textualize/textual-dev/src/textual_dev/service.py`：`ClientHandler._consume_incoming()` 对 `client_log` 执行 `pickle.loads(payload["segments"])` 并打印 `DevConsoleLog(...)`，对 `client_spillover` 打印 `DevConsoleNotice(f"Discarded {spillover} messages", level="warning")`；与上一条消息间隔大于 0.5 秒时调用 `self.service.console.rule()`；`run()` 中对 bytes 消息 `msgpack.unpackb`、对 str 消息 `json.loads`。
- **F-SD-12**：`external/dao/action/Textualize/textual-dev/src/textual_dev/client.py`：`DevtoolsClient.__init__(host="127.0.0.1", port=None)`，`self.url = f"ws://{host}:{port}"`；`connect()` 调用 `session.ws_connect(f"{self.url}/textual-devtools-websocket")`；模块常量 `READY_TIMEOUT = 0.5`、`LOG_QUEUE_MAXSIZE = 512`；`log()` 以 `msgpack.packb` 打包 `{"type": "client_log", "payload": {group, verbosity, timestamp, path, line_number, segments}}`；`_encode_segments()` 返回 `pickle.dumps(segments, protocol=4)`；QueueFull 时 `self.spillover += 1`。
- **F-SD-13**：`external/dao/action/Textualize/textual-dev/src/textual_dev/client.py`：`DevtoolsConsole(Console)` 在 `__init__` 中设置 `self.record = True`；`export_segments()` 在 `_record_buffer_lock` 内返回并清空 `_record_buffer`；`DevtoolsLog` 是 NamedTuple，字段 `objects_or_string` 与 `caller`。
- **F-SD-14**：`external/dao/action/Textualize/textual-dev/src/textual_dev/redirect_output.py`：`StdoutRedirector.write()` 将 `DevtoolsLog(string, caller)` 追加到 `_buffer`，字符串含 `"\n"` 时调用 `flush()`；`_write_to_devtools()` 按 caller 的 filename/lineno 是否相同切分批次；`_log_devtools_batched()` 以 `"".join(...)` 合并、`rstrip()` 后调用 `self.devtools.log(..., LogGroup.PRINT, LogVerbosity.NORMAL)`。
- **F-SD-15**：`external/dao/action/Textualize/textual-dev/src/textual_dev/tools/run.py`：`_is_python_path(path)` 当 `path.endswith(".py")` 或文件首行以 `"#!"` 开头且含 `"py"` 时返回 True；`exec_python()` 在 WINDOWS 分支调用 `subprocess.call([sys.executable, *args], env=environment)`，否则 `os.execve(sys.executable, ["python", *args], environment)`；`exec_import()` 以 `":"` 分割 module/app，`app = app or "app"`，用 `EXEC_SCRIPT` 模板（`from $MODULE import $APP as app`；`isinstance(app, App)` 为真则 `app.run()`，否则 `app().run()`）生成脚本并 `compile(script, "textual-exec", "exec")`。
- **F-SD-16**：`external/dao/action/Textualize/textual-dev/src/textual_dev/renderables.py`：定义三个 renderable 类 `DevConsoleHeader`、`DevConsoleLog`、`DevConsoleNotice`；`DevConsoleHeader` 输出 "Textual Development Console v{version('textual')}" 与 `textual run --dev my_app.py`（带 port 时为 `textual run --port {port} --dev my_app.py`）；`DevConsoleLog` 渲染时间、`LogGroup(self.group).name`、`{文件名}:{行号}`（带 `file://` 链接），group 为 "PRINT" 时以 `Styled(Segments(...), "bold")` 输出；`DevConsoleNotice` 的 level→style 映射为 `{"info": "dim", "warning": "yellow", "error": "red"}`。
- **F-SD-17**：`external/dao/action/Textualize/textual-dev/src/textual_dev/cli.py`：`_pre_run_warnings()` 的 warnings 列表含一项：`platform.system() == "Darwin"` 且 `os.environ.get("TERM_PROGRAM") == "Apple_Terminal"` 时，打印 "The default terminal app on macOS is limited to 256 colors." 面板。
- **F-SD-18**：`external/dao/action/Textualize/textual-dev/src/textual_dev/previews/borders.py` 与 `external/dao/action/Textualize/textual-dev/src/textual_dev/previews/keys.py`：`BorderApp` 以 `sorted(VALID_BORDER)`（导入自 `textual.css.constants`）构建 `OptionList`；`KeysApp(App[None], inherit_bindings=False)` 的 `TITLE = "Textual Keys"`、`ENABLE_COMMAND_PALETTE = False`，`on_key()` 将 `event` 写入 `KeyLog`（`KeyLog(RichLog, inherit_bindings=False)`），`ctrl+c` 需连按两次才 `self.exit()`。

## 2. textual-serve（Web 服务机制，F-SV-）

- **F-SV-01**：`external/dao/action/Textualize/textual-serve/pyproject.toml`：`[project]` name="textual-serve"，version="1.1.3"，description="Turn your Textual TUIs in to web applications"，license="MIT"，requires-python=">= 3.9"；dependencies 为 `aiohttp>=3.9.5`、`aiohttp-jinja2>=1.6`、`jinja2>=3.1.4`、`rich`、`textual>=0.66.0`；build-backend 为 `hatchling.build`。
- **F-SV-02**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`Server.__init__(command, host="localhost", port=8000, title=None, public_url=None, statics_path="./static", templates_path="./templates")`；`base_path = (Path(__file__) / "../").resolve().absolute()`，statics/templates 路径相对 base_path 解析；public_url 为 None 时：port==80 → `http://{host}`，port==443 → `https://{host}`，否则 `http://{host}:{port}`。
- **F-SV-03**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`_make_app()` 调用 `aiohttp_jinja2.setup(app, loader=jinja2.FileSystemLoader(self.templates_path))`，ROUTES 为 `web.get("/", handle_index, name="index")`、`web.get("/ws", handle_websocket, name="websocket")`、`web.get("/download/{key}", handle_download, name="download")`、`web.static("/static", statics_path, show_index=True, name="static")`。
- **F-SV-04**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`handle_index` 带装饰器 `@aiohttp_jinja2.template("app_index.html")`；`font_size = to_int(request.query.get("fontsize", "16"), 16)`；`get_websocket_url()` 在 public_url 以 "https" 开头时返回 `"wss:" + url.split(":", 1)[1]`，否则 `"ws:" + ...`；context 含 `font_size`、`app_websocket_url`、`config.static.url`、`application.name`。
- **F-SV-05**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`handle_websocket()` 创建 `web.WebSocketResponse(heartbeat=15)`；`width = to_int(request.query.get("width", "80"), 80)`、`height = to_int(request.query.get("height", "24"), 24)`；创建 `AppService(self.command, write_bytes=websocket.send_bytes, write_str=websocket.send_str, close=websocket.close, download_manager=..., debug=...)` 并 `await app_service.start(width, height)`。
- **F-SV-06**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`_process_messages()` 遍历 websocket 的 TEXT 消息，`envelope = message.json()` 为 list；`type_ == "stdin"` → `app_service.send_bytes(data.encode("utf-8"))`；`"resize"` → `app_service.set_terminal_size(data["width"], data["height"])`；`"ping"` → `websocket.send_json(["pong", data])`；`"blur"`/`"focus"` → `app_service.blur()`/`app_service.focus()`。
- **F-SV-07**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`_build_environment()` 设置 `environment["TEXTUAL_DRIVER"] = "textual.drivers.web_driver:WebDriver"`、`TEXTUAL_FPS="60"`、`TEXTUAL_COLOR_SYSTEM="truecolor"`、`TERM_PROGRAM="textual"`、`TERM_PROGRAM_VERSION=version("textual-serve")`、`COLUMNS=str(width)`、`ROWS=str(height)`；debug 为真时追加 `TEXTUAL="debug,devtools"` 与 `TEXTUAL_LOG="textual.log"`。
- **F-SV-08**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`_open_app_process()` 调用 `asyncio.create_subprocess_shell(self.command, stdin=PIPE, stdout=PIPE, stderr=PIPE, env=environment)`；`self.app_service_id = uuid.uuid4().hex`。
- **F-SV-09**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`encode_packet(packet_type, payload)` 返回 `b"%s%s%s" % (packet_type, len(payload).to_bytes(4, "big"), payload)`；`send_bytes()` 写 `encode_packet(b"D", data)`；`send_meta()` 将 dict `json.dumps(...).encode("utf-8")` 后以 `encode_packet(b"M", ...)` 写入；`set_terminal_size()` 发送 `{"type": "resize", "width", "height"}`，`blur()`/`focus()` 发送 `{"type": "blur"}`/`{"type": "focus"}`。
- **F-SV-10**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`run()` 先从 stdout 读取最多 10 行，匹配 `b"__GANGLION__\n"` 则 `ready = True`；随后循环 `readexactly(1)` 取 type、`readexactly(4)` 取 size（big-endian）、`readexactly(size)` 取 payload；type 为 `b"D"` → `on_data`，`b"M"` → `on_meta`，`b"P"` → `on_packed`；stderr 由独立任务以 `stderr.read(1024 * 4)` 读入 `io.BytesIO`。
- **F-SV-11**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`on_meta()` 中 `meta_type == "exit"` → `remote_close()`；`"open_url"` → `remote_write_str(json.dumps(["open_url", {"url", "new_tab"}]))`；`"deliver_file_start"` → `download_manager.create_download(app_service, delivery_key, file_name=Path(meta_data["path"]).name, open_method, mime_type, encoding, name)` 并向浏览器发送 `json.dumps(["deliver_file_start", delivery_key])`；其他 type 记录 warning "Unknown meta type"。
- **F-SV-12**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`on_packed()` 调用 `_binary_encode.load(payload)`（导入名 `binary_load`），当 `unpacked[0] == "deliver_chunk"` 时解包 `(_, delivery_key, chunk)` 并调用 `download_manager.chunk_received(delivery_key, chunk)`。
- **F-SV-13**：`external/dao/action/Textualize/textual-serve/src/textual_serve/download_manager.py`：模块常量 `DOWNLOAD_TIMEOUT = 4`、`DOWNLOAD_CHUNK_SIZE = 1024 * 64`；`Download` dataclass 字段为 `app_service`、`delivery_key`、`file_name`、`open_method`、`mime_type`、`encoding=None`、`name=None`、`incoming_chunks: asyncio.Queue`；`DownloadManager._active_downloads: dict[str, Download]`。
- **F-SV-14**：`external/dao/action/Textualize/textual-serve/src/textual_serve/download_manager.py`：`download()` 循环内先 `app_service.send_meta({"type": "deliver_chunk_request", "key": delivery_key, "size": DOWNLOAD_CHUNK_SIZE, "name": download.name})`，再 `asyncio.wait_for(incoming_chunks.get(), DOWNLOAD_TIMEOUT)`；chunk 为空时 `del self._active_downloads[delivery_key]` 并 break，否则 `yield chunk`；`chunk_received()` 对 str chunk 以 `download.encoding or "utf-8"` 编码。
- **F-SV-15**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`handle_download()` 以 `request.match_info["key"]` 查元数据，KeyError 时 `raise web.HTTPNotFound(text=f"Download with key {key!r} not found")`；`Content-Disposition` 在 `open_method == "browser"` 时为 `"inline"`，否则 `"attachment"`，均附 `filename={file_name}`；以 `web.StreamResponse()` 流式写出 `download(key)` 的 chunks。
- **F-SV-16**：`external/dao/action/Textualize/textual-serve/src/textual_serve/_binary_encode.py`：模块 docstring 声明 "based on https://en.wikipedia.org/wiki/Bencode with some extensions"，可编码类型为 None/int/bool/bytes/str/list/tuple/dict；`encode_none` 返回 `b"N"`，`encode_bool` 返回 `b"T"`/`b"F"`，`encode_int` 返回 `b"i%ie" % datum`；定义 `DecodeError(Exception)` 与 `dump(data)` 函数。
- **F-SV-17**：`external/dao/action/Textualize/textual-serve/src/textual_serve/templates/app_index.html` 与 `external/dao/action/Textualize/textual-serve/src/textual_serve/static/`：模板引用 `css/xterm.css`、`js/textual.js`、`images/background.png` 与 Google Fonts Roboto Mono；含 `div#terminal.textual-terminal`（`data-session-websocket-url="{{ app_websocket_url }}"`、`data-font-size="{{ font_size }}"`）、intro 对话框（含 Start 按钮）与 closed 对话框（"Session ended." + Restart 按钮）；`body.-first-byte` 类切换 intro 隐藏与终端显示；static 目录含 `js/textual.js`、`css/xterm.css`、`fonts/RobotoMono-VariableFont_wght.ttf`、`fonts/RobotoMono-Italic-VariableFont_wght.ttf`、`images/background.png`（5 个文件）。
- **F-SV-18**：`external/dao/action/Textualize/textual-serve/src/textual_serve/server.py`：`serve()` 调用 `initialize_logging()`（`RichHandler(show_path=False, show_time=False, rich_tracebacks=True, tracebacks_show_locals=True, highlighter=LogHighlighter())`），注册 `SIGINT`/`SIGTERM` → `request_exit`（`raise GracefulExit()`），`web.run_app(..., handle_signals=False)`；`on_startup()` 打印模块级 `LOGO`（ASCII 艺术，末尾 `.replace("VVVVV", f"v{version('textual-serve')}")`）与 `f"Serving {self.command!r} on {self.public_url}"`；`LogHighlighter(RegexHighlighter)` 定义 number/path/str 三组高亮正则。
- **F-SV-19**：`external/dao/action/Textualize/textual-serve/src/textual_serve/app_service.py`：`stop()` 先 `await self._download_manager.cancel_app_downloads(app_service_id=self.app_service_id)`，再 `await self.send_meta({"type": "quit"})`，然后 `await self._task`；`cancel_app_downloads()` 对匹配 app_service_id 的 download 执行 `download.incoming_chunks.put(None)`。
- **F-SV-20**：`external/dao/action/Textualize/textual-serve/README.md` 与 `external/dao/action/Textualize/textual-serve/examples/serve.py`：README 首段为 "Every Textual application is now a web application." 与 "With 3 lines of code, any Textual app can run in the browser."；`examples/serve.py` 全文为 `from textual_serve.server import Server`、`server = Server("python -m textual")`、`server.serve()`；README "See also" 节指向 textual-web 并称 "You can consider this project to essentially be a self-hosted equivalent of Textual-web."。

## 3. textual-web（浏览器发布机制，F-SW-）

- **F-SW-01**：`external/dao/action/Textualize/textual-web/pyproject.toml`：`[tool.poetry]` name="textual_web"，version="0.8.0"，description="Serve Textual apps"；依赖 `python = "^3.8.1"`、`textual = "^0.43.0"`、`aiohttp = "^3.9.3"`、`uvloop`（markers `sys_platform != 'win32'`）、`click = "^8.1.3"`、`aiohttp-jinja2`、`pydantic = "^2.1.1"`、`xdg`、`msgpack`、`importlib-metadata`、`httpx`、`tomli`；`[tool.poetry.scripts]` 定义 `textual-web = "textual_web.cli:app"`。
- **F-SW-02**：`external/dao/action/Textualize/textual-web/src/textual_web/cli.py`：`@click.command()` 函数 `app`，选项为 `-c/--config`、`-e/--environment`（`type=click.Choice(list(ENVIRONMENTS))`）、`-a/--api-key`、`-r/--run`（multiple）、`--dev`（flag）、`-t/--terminal`（flag）、`-x/--exit-on-idle`（int，默认 0）、`-w/--web-interface`（flag）、`-s/--signup`（flag）、`--welcome`（flag）、`--merlin`（flag）。
- **F-SW-03**：`external/dao/action/Textualize/textual-web/src/textual_web/cli.py`：`print_disclaimer()` 打印 Panel，正文含 "textual-web is currently under active development, and not suitable for production use."；`--signup` → `SignUpApp.signup(_environment)`，`--welcome` → `WelcomeApp().run()`，`--merlin` → `MerlinApp().run()`（分别自 `.apps.signup`/`.apps.welcome`/`.apps.merlin` 导入）。
- **F-SW-04**：`external/dao/action/Textualize/textual-web/src/textual_web/cli.py`：`-r/--run` 的每个 command 执行 `ganglion_client.add_app(app_command, app_command, "")`；`-t` 时 `add_terminal("Terminal", os.environ.get("SHELL", "bin/sh"), "")`；`ganglion_client.app_count` 为 0 时添加 `add_app("Welcome", "textual-web --welcome", "welcome")` 与 `add_app("Merlin Tribute", "textual-web --merlin", "merlin")`；非 WINDOWS 时尝试 `import uvloop`（Python ≥3.11 用 `asyncio.Runner(loop_factory=uvloop.new_event_loop)`）。
- **F-SW-05**：`external/dao/action/Textualize/textual-web/src/textual_web/environment.py`：`Environment` dataclass 字段 `name`/`api_url`/`url`；`ENVIRONMENTS` 含三项：`"prod"`（api_url `https://textual-web.io/api/`，url `wss://textual-web.io/app-service/`）、`"local"`（`ws://127.0.0.1:8080/api/` 与 `ws://127.0.0.1:8080/app-service/`）、`"dev"`（`https://textualize-dev.io/api/` 与 `wss://textualize-dev.io/app-service/`）；`get_environment()` 对未知名称 `raise RuntimeError`。
- **F-SW-06**：`external/dao/action/Textualize/textual-web/src/textual_web/constants.py`：`DEBUG: Final = get_environ_bool("DEBUG")`；`ENVIRONMENT: Final[str] = get_environ("GANGLION_ENVIRONMENT", "prod")`；`API_KEY: Final[str] = get_environ("GANGLION_API_KEY", "")`；`get_environ_bool` 当环境变量 == "1" 时返回 True。
- **F-SW-07**：`external/dao/action/Textualize/textual-web/src/textual_web/ganglion_client.py`：`_connect()` 中 api_key 非空时 `headers = {"GANGLIONAPIKEY": api_key}`；`session.ws_connect(self.websocket_url, headers=headers, heartbeat=15, compress=12)`；连接循环包在 `Retry()` 的 `async for retry_count in retry` 中；`WSServerHandshakeError` 时记录 "Received forbidden response, check your API Key"。
- **F-SW-08**：`external/dao/action/Textualize/textual-web/src/textual_web/retry.py`：`Retry(done_event=None, min_wait=2.0, max_wait=16.0)`；`__aiter__` 中 `sleep_for = random() * max(self.min_wait, min(self.max_wait, retry_squared))`，`retry_squared = self.retry_count**2`；`success()` 将 retry_count 置 0。
- **F-SW-09**：`external/dao/action/Textualize/textual-web/src/textual_web/ganglion_client.py`：`post_connect()` 构造 `apps = [app.model_dump(include={"name", "slug", "color", "terminal"}) for app in self.config.apps]` 并 `await self.send(packets.DeclareApps(apps))`；WINDOWS 分支过滤 `app["terminal"]` 为真的条目并记录 "textual-web does not currently support terminals on Windows"。
- **F-SW-10**：`external/dao/action/Textualize/textual-web/src/textual_web/ganglion_client.py`：`run_websocket()` 以 `partial(msgpack.unpackb, use_list=True, raw=False)` 解码 BINARY 消息为 envelope，经 `decode_envelope()` 分发；`decode_envelope()` 以首元素 int 查 `PACKET_MAP`，`packet_class.build(*packet_data[: len(packet_class._attributes)])`；`send()` 以 `msgpack.packb(packet, use_bin_type=True)` 发送。
- **F-SW-11**：`external/dao/action/Textualize/textual-web/src/textual_web/packets.py`：文件头注释 "auto-generated from packets.yml and packets.py.template"、"**Do not hand edit.**"；`PacketType(IntEnum)` 定义 NULL=0 至 REQUEST_DELIVER_CHUNK=17 共 18 个值；`Packet` 继承 `tuple`；`PACKET_MAP` 映射 1-17 到 17 个 Packet 类（Ping/Pong/Log/Info/DeclareApps/SessionOpen/SessionClose/SessionData/RoutePing/RoutePong/NotifyTerminalSize/Focus/Blur/OpenUrl/BinaryEncodedMessage/DeliverFileStart/RequestDeliverChunk）；`Handlers.dispatch_packet()` 执行 `await packet._get_handler(self)(packet)`。
- **F-SW-12**：`external/dao/action/Textualize/textual-web/src/textual_web/ganglion_client.py`：`on_session_open()` 调用 `session_manager.new_session(packet.application_slug, SessionID(packet.session_id), RouteKey(packet.route_key), devtools=self._devtools, size=(packet.width, packet.height))`，随后 `session_process.start(connector)`（connector 为 `_ClientConnector`）；`on_ping()` 立即回送 `packets.Pong(packet.data)`；`on_route_ping()` 回送 `RoutePong(packet.route_key, packet.data)`。
- **F-SW-13**：`external/dao/action/Textualize/textual-web/src/textual_web/session_manager.py`：`SessionManager` 属性 `sessions: dict[SessionID, Session]`、`routes: TwoWayDict[RouteKey, SessionID]`、`apps_by_slug`；`new_session()` 中 `app.terminal` 为真且非 WINDOWS 时创建 `TerminalSession(self.poller, session_id, app.command)`，否则 `AppSession(self.path, app.command, session_id, devtools=devtools)`；`close_all(timeout=3.0)` 以 `asyncio.wait` 等待全部 session 关闭。
- **F-SW-14**：`external/dao/action/Textualize/textual-web/src/textual_web/app_session.py`：`AppSession.open()` 设置 `environment["TEXTUAL_DRIVER"] = "textual.drivers.web_driver:WebDriver"`、`TEXTUAL_FPS="60"`、`TEXTUAL_COLOR_SYSTEM="truecolor"`、`TERM_PROGRAM="textual-web"`、`TERM_PROGRAM_VERSION=version("textual-web")`、`COLUMNS`/`ROWS`；devtools 为真时 `TEXTUAL="debug,devtools"`、`TEXTUAL_LOG="textual.log"`；`os.chdir(self.working_directory)` 后 `create_subprocess_shell`，finally 恢复原 cwd。
- **F-SW-15**：`external/dao/action/Textualize/textual-web/src/textual_web/app_session.py`：`run()` 读最多 10 行匹配 `b"__GANGLION__\n"`；随后按 type(1 字节)+size(4 字节 big-endian)+payload 读包：`b"D"` → `on_data(payload)`，`b"M"` → `json.loads` 且 `meta_type in {"exit", "blur", "focus"}` 时 `send_meta({"type": meta_type})` 回发、否则 `on_meta`，`b"P"` → `on_binary_encoded_message(payload)`；`ProcessState` 枚举含 PENDING/RUNNING/CLOSING/CLOSED；`close()` 发送 `{"type": "quit"}`。
- **F-SW-16**：`external/dao/action/Textualize/textual-web/src/textual_web/terminal_session.py`：`TerminalSession.open()` 调用 `pty.fork()`，子进程（`pid == pty.CHILD`）设置 `TERM_PROGRAM="textual-web"` 后 `os.execlp(argv[0], *argv)`；`_set_terminal_size()` 执行 `fcntl.ioctl(self.master_fd, termios.TIOCSWINSZ, array.array("h", [height, width, 0, 0]))`；`self.command = command or os.environ.get("SHELL", "sh")`；模块导入 `fcntl`、`pty`、`termios`（仅非 WINDOWS 时由 session_manager 导入）。
- **F-SW-17**：`external/dao/action/Textualize/textual-web/src/textual_web/config.py`：pydantic 模型 `Account(api_key: Optional[str])`、`App(name, slug="", path="./", color="", command="", terminal=False)`、`Config(account, apps: List[App])`；`ExpandVarsStr = Annotated[str, AfterValidator(expandvars)]`，path 与 command 两个字段的类型标注均为 ExpandVarsStr；`load_config()` 以 `tomli.load` 读 TOML，`config_data.get("app", {})` 与 `config_data.get("terminal", {})` 分别生成 App（terminal=True 时 `slug = generate().lower()`，否则空 slug 取 `slugify(name)`）。
- **F-SW-18**：`external/dao/action/Textualize/textual-web/src/textual_web/identity.py`：`IDENTITY_ALPHABET = "0123456789ABCDEFGHJKMNPQRSTUVWYZ"`（31 字符），`IDENTITY_SIZE = 12`；`generate(size=IDENTITY_SIZE)` 返回 `"".join(alphabet[byte % 31] for byte in os.urandom(size))`。
- **F-SW-19**：`external/dao/action/Textualize/textual-web/src/textual_web/web.py`：模块 docstring 含 "Note: Currently just a stub."；`run_web_interface(connected_event)` 注册 `web.get("/health-check/", health_check)`，handler 内 `asyncio.wait_for(connected_event.wait(), 5.0)` 后返回 `web.Response(text="Hello, world")`；以 `web.TCPSite(runner, "0.0.0.0", 8080)` 启动，日志 "Web interface started on port 8080"。
- **F-SW-20**：`external/dao/action/Textualize/textual-web/src/textual_web/exit_poller.py` 与 `external/dao/action/Textualize/textual-web/src/textual_web/poller.py`：`EXIT_POLL_RATE = 5`；`ExitPoller.run()` 中 `is_idle = not self.client.session_manager.sessions`，空闲超过 `idle_wait` 秒时记录 "Exiting due to --exit-on-idle" 并 `client.force_exit()`；`Poller(Thread)` 使用 `selectors.DefaultSelector()`，`add_file()` 注册 `EVENT_READ | EVENT_WRITE` 并返回 `asyncio.Queue`。
