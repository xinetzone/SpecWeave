- [ ] 原 `docs/` 已在迁移前完成全量备份，且备份可恢复
- [ ] 已记录迁移前基线：文件总数、相对路径清单、文件大小、内容哈希、关键引用方
- [ ] 原根目录 `docs/` 已整体迁移为 `.agents/docs/`
- [ ] 迁移结果中不存在误生成的嵌套根目录 `.agents/docs/docs/`
- [ ] 迁移后文件总数与迁移前一致，无遗漏、无重复、无损坏
- [ ] 迁移后文件内容哈希与基线一致
- [ ] `.agents/docs/` 作为迁移后的文档承载位置边界清晰
- [ ] `.agents/docs/` 下既有智能体专属目录未被覆盖、未被误归类
- [ ] `.agents/docs/` 下已补充显式边界说明，能够区分人类文档与智能体专属文档
- [ ] `.agents/docs/README.md` 或等效索引文档已采用 spec 规定的最小边界声明模板
- [ ] `.agents/docs/README.md` 或等效入口已包含目录归类决策表或等效归类摘要
- [ ] 仓库内所有对原 `docs/` 的链接、配置、规范路径已同步更新
- [ ] 所有跨文件相对路径在新目录层级下解析正确
- [ ] 仓库内不存在残留的失效旧路径引用
- [ ] 人类用户可通过 `.agents/docs/` 正常访问原文档体系与入口导航
- [ ] 相关 agent 程序仍可正确识别并读取 `.agents/docs/` 下所需文档
- [ ] 维护者无需依赖历史背景，仅凭目录结构与索引说明即可判断文件归属
- [ ] README 边界声明至少包含“本目录定位 / 人类文档范围 / 智能体专属文档范围 / 维护原则 / 快速查找入口”
- [ ] 至少 5 个目录或文件可依据决策表被稳定判定归属，且与实际用途一致
- [ ] 抽查样本优先覆盖 spec 预设的 5 类样本类型，若缺类需记录原因
- [ ] 抽查记录包含每个样本的路径、默认归属、判定依据、是否需要补充边界说明
- [ ] 至少 1 个混合型样本已按升级规则完成判定并验证
- [ ] 抽查记录已按 spec 规定的 Markdown 表格模板输出，字段完整且可复查
- [ ] 已运行文件迁移完整性校验并通过
- [ ] 已运行链接有效性校验并通过
- [ ] 迁移结果满足“物理聚合于 `.agents/docs/`，语义分层仍独立”的目标

## 本轮验证结论（2026-07-15）

- [ ] 本轮全量验证通过，可勾选 Task 4-6
- [x] 本轮全量验证未通过，Task 4-6 保持未完成状态
- [x] `python .agents/scripts/check-links.py` 未通过：当前报告 `6291` 个本地断链
- [x] `powershell -ExecutionPolicy Bypass -File .agents/scripts/ci-check.ps1` 未通过：当前阻断于仓库合规检查
- [x] 基于 `artifacts/docs-baseline-manifest.json` 的复核显示：基线 `2683` 个文件无缺失，但存在 `2353` 个哈希差异，需进一步判定哪些属于预期路径修复改动
- [x] 已在 `tasks.md` 新增 Task 7，用于收敛失败项并复验
