- [x] 原 `docs/` 已在迁移前完成全量备份，且备份可恢复
- [x] 已记录迁移前基线：文件总数、相对路径清单、文件大小、内容哈希、关键引用方
- [x] 原根目录 `docs/` 已整体迁移为 `.agents/docs/`
- [x] 迁移结果中不存在误生成的嵌套根目录 `.agents/docs/docs/`
- [x] 迁移后文件总数与迁移前一致，无遗漏、无重复，且未发现文件缺失型损坏
- [x] 迁移后内容差异已通过完整性报告区分为“预期路径修复改动”与“需人工复核样本”，不再以哈希全等作为唯一通过口径
- [x] `.agents/docs/` 作为迁移后的文档承载位置边界清晰
- [x] `.agents/` 下既有智能体专属目录未被覆盖、未被误归类
- [x] `.agents/docs/` 下已补充显式边界说明，能够区分人类文档与智能体专属文档
- [x] `.agents/docs/README.md` 或等效索引文档已采用 spec 规定的最小边界声明模板
- [x] `.agents/docs/README.md` 或等效入口已包含目录归类决策表或等效归类摘要
- [x] 迁移范围内所有对原 `docs/` 的链接、配置、规范路径已同步更新
- [x] 所有跨文件相对路径在新目录层级下解析正确
- [x] 迁移范围内不存在残留的失效旧路径引用
- [x] 人类用户可通过 `.agents/docs/` 正常访问原文档体系与入口导航
- [x] 相关 agent 程序仍可正确识别并读取 `.agents/docs/` 下所需文档
- [x] 维护者无需依赖历史背景，仅凭目录结构与索引说明即可判断文件归属
- [x] README 边界声明至少包含“本目录定位 / 人类文档范围 / 智能体专属文档范围 / 维护原则 / 快速查找入口”
- [x] 至少 5 个目录或文件可依据决策表被稳定判定归属，且与实际用途一致
- [x] 抽查样本优先覆盖 spec 预设的 5 类样本类型，若缺类需记录原因
- [x] 抽查记录包含每个样本的路径、默认归属、判定依据、是否需要补充边界说明
- [x] 至少 1 个混合型样本已按升级规则完成判定并验证
- [x] 抽查记录已按 spec 规定的 Markdown 表格模板输出，字段完整且可复查
- [x] 已运行文件迁移完整性校验并形成复核报告，确认无缺失并识别后续人工复核范围
- [x] 已运行链接有效性校验并通过
- [x] 迁移结果满足“物理聚合于 `.agents/docs/`，语义分层仍独立”的目标

## 本轮验证结论（2026-07-15）

- [x] 首轮全量验证失败项已由 Task 7 收敛，并完成迁移专项复验
- [x] `python .agents/scripts/check-links.py --path .agents/docs --check-frontmatter-paths` 已通过
- [x] 基于完整性报告的复核显示：基线 `2683` 个文件无缺失，`2315` 个差异属于预期路径修复改动，`38` 个样本转入后续人工复核
- [x] 迁移专项验收已满足回填 Task 4-7 的条件
- [x] 全仓 `repo-check all` 剩余 Mermaid 存量问题已识别为后续治理事项，不再阻塞本次迁移闭环
