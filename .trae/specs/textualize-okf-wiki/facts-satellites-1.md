# Facts: frogmouth / toolong / trogon / rich-cli（R 阶段·零推测）

- 采集日期：2026-09-01
- 信源根：`external/dao/action/Textualize/`（commit hash 见 `source-versions.md`）
  - frogmouth @ `15c3e85a6e84b2e4a6845723acf12beb54c81eb2`
  - toolong @ `5aa22ee878026f46d4d265905c4e1df4d37842ae`
  - trogon @ `eaa9e68c403cae6aff0a80957d8876b284fd76b0`
  - rich-cli @ `46f4d2469097be395558768714a5f07ebccf1412`
- 规则：只写"代码里有什么"；每条标注源码相对路径；无推断词。

## 1. frogmouth（app 架构 + Markdown 渲染管线，F-FM-）

- **F-FM-01**：`external/dao/action/Textualize/frogmouth/pyproject.toml`：`[tool.poetry]` name="frogmouth"，version="0.9.2"，description="A Markdown document viewer for the terminal"，license="MIT"；依赖 `python = "^3.8"`、`textual = "==0.53.1"`、`typing-extensions = "^4.5.0"`、`httpx = "^0.24.1"`、`xdg = "^6.0.0"`；`[tool.poetry.scripts]` 定义 `frogmouth = "frogmouth.app.app:run"`；build-backend 为 `poetry.core.masonry.api`。
- **F-FM-02**：`external/dao/action/Textualize/frogmouth/frogmouth/__init__.py`：模块属性 `__version__ = "0.9.1"`、`__author__ = "Textualize, Inc"`、`__credits__ = ["Dave Pearson"]`、`__maintainer__ = "Dave Pearson"`、`__email__ = "dave@textualize.io"`、`__licence__ = "MIT"`；docstring 为 "A terminal-based Markdown document viewer, written in Textual."。
- **F-FM-03**：`external/dao/action/Textualize/frogmouth/frogmouth/app/app.py`：`MarkdownViewer(App[None])` 设 `TITLE = APPLICATION_TITLE`、`ENABLE_COMMAND_PALETTE = False`；`__init__(cli_args: Namespace)` 保存 `self._args = cli_args` 并设 `self.dark = not load_config().light_mode`；`on_mount()` 执行 `self.push_screen(Main(" ".join(self._args.file) if self._args.file else None))`；`action_visit(url)` 调用 `webbrowser.open(url)`（导入名 `open_url`）。
- **F-FM-04**：`external/dao/action/Textualize/frogmouth/frogmouth/app/app.py`：`get_args()` 构造 `ArgumentParser(prog=PACKAGE_NAME, description=f"{APPLICATION_TITLE} -- A Markdown viewer for the terminal.", epilog=f"v{__version__}")`；注册 `-v/--version`（action="version"，version 字符串为 `%(prog)s {__version__} (Textual v{textual_version})`）与位置参数 `file`（nargs="*"）；`run()` 函数体为 `MarkdownViewer(get_args()).run()`；`__main__.py` 导入并调用 `run()`。
- **F-FM-05**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：模块级 `PLACEHOLDER = f"# {APPLICATION_TITLE} {__version__}\n\nWelcome to {APPLICATION_TITLE}!"`；`Viewer.compose()` yield `Markdown(PLACEHOLDER, parser_factory=lambda: MarkdownIt("gfm-like").use(front_matter.front_matter_plugin))`；`document` 属性返回 `self.query_one(Markdown)`；`scroll_to_block(block_id)` 调用 `self.scroll_to_widget(self.document.query_one(f"#{block_id}"), top=True)`。
- **F-FM-06**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：`History` 类含 `MAXIMUM_HISTORY_LENGTH: Final[int] = 256`，内部用 `deque(history or [], maxlen=MAXIMUM_HISTORY_LENGTH)`；`remember(location)` append 后置 `_current = len(_history) - 1`；`back()`/`forward()` 递减/递增 `_current` 并返回 bool；`__delitem__(index)` 删除条目后重算 `_current = max(len(_history) - 1, _current)`；`location` 属性在 IndexError 时返回 None。
- **F-FM-07**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：`Viewer(VerticalScroll, can_focus=True, can_focus_children=True)` 的 DEFAULT_CSS 为 `width: 1fr; scrollbar-gutter: stable;`；BINDINGS 为 `w,k→scroll_up`、`s,j→scroll_down`、`space→page_down`、`b→page_up`（均 show=False）；reactive 变量 `history: var[History]` 与 `viewing_location: var[bool]`；定义消息 `LocationChanged` 与 `HistoryUpdated`（均继承自带 `viewer` 引用的 `ViewerMessage`）。
- **F-FM-08**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：`_local_load(location, remember=True)` 标注 `@work(exclusive=True)`，调用 `await self.document.load(location)`；OSError 时 push `ErrorDialog("Error loading local document", f"{location}\n\n{error}.")`，成功则 `_post_load(location, remember)`；`_post_load` 依次执行 `scroll_home(animate=False)`、`viewing_location = True`、remember 为真时 `history.remember(location)` 并 post `HistoryUpdated`，最后 post `LocationChanged`。
- **F-FM-09**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：`_remote_load(location, remember=True)` 标注 `@work(exclusive=True)`，使用 `AsyncClient().get(location, follow_redirects=True, headers={"user-agent": USER_AGENT})` 并调用 `response.raise_for_status()`；`RequestError`/`HTTPStatusError` → `ErrorDialog("Error getting document", str(error))`；content-type 以 `text/plain`、`text/markdown` 或 `text/x-markdown` 开头时 `self.document.update(response.text)` + `_post_load`，否则调用 `open_url(str(location))`。
- **F-FM-10**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/viewer.py`：`visit(location, remember=True)` 按类型分派：`Path` → `_local_load(location.expanduser().resolve(), remember)`；`URL` → `_remote_load(location, remember)`；其他类型 `raise ValueError("Unknown location type passed to the Markdown viewer")`；`reload()` 在 location 非 None 时调用 `visit(self.location, False)`；`show(content)` 设 `viewing_location = False` 后 `document.update(content)` 并 `scroll_home(animate=False)`；`back()`/`forward()` 经 `_jump(self.history.back/forward)` 实现，跳转成功后 `visit(self.history.location, remember=False)`。
- **F-FM-11**：`external/dao/action/Textualize/frogmouth/frogmouth/screens/main.py`：`Main(Screen[None])` 定义 15 条 BINDINGS：`/,:→omnibox`、`ctrl+b→bookmarks`、`ctrl+d→bookmark_this`、`ctrl+l→local_files`、`ctrl+left→backward`、`ctrl+right→forward`、`ctrl+r→reload`、`ctrl+t→table_of_contents`、`ctrl+y→history`、`escape→escape`、`f1→help`、`f2→about`、`ctrl+n→navigation`、`ctrl+q→app.quit`、`f10→toggle_theme`。
- **F-FM-12**：`external/dao/action/Textualize/frogmouth/frogmouth/screens/main.py`：`compose()` 依次 yield `Omnibox(classes="focusable")`、`Horizontal`（内含 `Navigation()` 与 `Viewer(classes="focusable")`）、`Footer()`；`on_mount()` 设 `self.query_one(Markdown).can_focus_children = False`，加载 `load_history()`；无初始位置且有历史 → `visit(history[-1], remember=False)` 且 `omnibox.value = str(history[-1])`；有初始位置 → `omnibox.value = self._initial_location` 后 `await omnibox.action_submit()`。
- **F-FM-13**：`external/dao/action/Textualize/frogmouth/frogmouth/screens/main.py`：`on_markdown_link_clicked(event)` 分支顺序：`is_likely_url(event.href)` → `visit(URL(event.href))`；当前位置为 URL → `visit(current_location.copy_with().join(event.href))`；`Path(event.href).exists()` → visit 该路径；相对当前 Path 父目录 `.absolute().exists()` → visit；`href.startswith("#")` 且 `event.markdown.goto_anchor(event.href[1:])` 成立 → pass；其余情况 push `ErrorDialog("Unable to handle link", ...)`。
- **F-FM-14**：`external/dao/action/Textualize/frogmouth/frogmouth/screens/main.py`：`action_escape()`：omnibox 有焦点且 value 非空 → 清空 value；value 为空 → `self.app.exit()`；否则若 `self.query("Navigation:focus-within")` 命中则设 `Navigation.popped_out = False`，然后 focus omnibox；`action_toggle_theme()` 反转 `config.light_mode`、`save_config(config)`、`self.app.dark = not config.light_mode`；`action_bookmark_this()` 在 location 为 Path/URL 时以文件名作默认标题 push `InputDialog("Bookmark title:", title)`，回调为 `partial(self.add_bookmark, location)`，否则 push `ErrorDialog("Not a bookmarkable location", ...)`。
- **F-FM-15**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/omnibox.py`：`Omnibox(Input)` 的 reactive `visiting: var[str]`，`watch_visiting` 设 `placeholder = self.visiting or "Enter a location or command"`；`_ALIASES` 字典含 16 项：`a→about`、`b→bookmarks`、`bm→bookmarks`、`bb→bitbucket`、`c→contents`、`cb→codeberg`、`cd→chdir`、`cl→changelog`、`gh→github`、`gl→gitlab`、`h→history`、`l→local`、`obs→obsidian`、`toc→contents`、`q→quit`、`?→help`；命令经 `getattr(self, f"command_{self._ALIASES.get(command, command)}")` 分派。
- **F-FM-16**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/omnibox.py`：`on_input_submitted` 分派顺序：`is_likely_url(submitted)` → post `RemoteViewCommand(URL(submitted))`；`Path(submitted).expanduser().resolve()` 存在且 is_file → `LocalViewCommand(path)`，is_dir → `LocalChdirCommand(path)`；`_is_command` 命中 → `_execute_command`；均不匹配 → `LocalViewCommand(Path(submitted))` 并恢复 `self.value = submitted`；定义正则 `_GUESS_BRANCH = r"^(?P<owner>[^/ ]+)[/ ](?P<repo>[^ :]+)(?: +(?P<file>[^ ]+))?$"` 与 `_SPECIFIC_BRANCH`（在 repo 后增加 `:(?P<branch>[^ ]+)` 组）。
- **F-FM-17**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/omnibox.py`：`command_changelog(_)` 调用 `self.command_github(f"{ORGANISATION_NAME}/{PACKAGE_NAME} ChangeLog.md")`；`command_obsidian(vault)` 拼接 `Path("~/Library/Mobile Documents/iCloud~md~obsidian/Documents").expanduser() / vault`，exists 时 `command_chdir(str(target))`；`command_discord(_)` 调用 `open_url(DISCORD)`；`ForgeCommand` 消息携带 `owner`/`repository`/`branch`/`desired_file` 四字段，子类为 `GitHubCommand`/`GitLabCommand`/`BitBucketCommand`/`CodebergCommand`，经 `_forge_quick_look` 匹配两个正则后 post。
- **F-FM-18**：`external/dao/action/Textualize/frogmouth/frogmouth/data/config.py`：`Config` dataclass 三字段 `light_mode: bool = False`、`markdown_extensions: list[str] = [".md", ".markdown"]`、`navigation_left: bool = True`；`config_file()` 返回 `xdg_config_home() / ORGANISATION_NAME / PACKAGE_NAME / "configuration.json"`（目录 `mkdir(parents=True, exist_ok=True)`）；`load_config()` 标注 `@lru_cache(maxsize=None)`，文件不存在时返回 `save_config(Config())`；`save_config` 先 `load_config.cache_clear()` 再以 `dumps(asdict(config), indent=4)` 写文件。
- **F-FM-19**：`external/dao/action/Textualize/frogmouth/frogmouth/data/bookmarks.py`、`data/history.py`、`data/data_directory.py`：`Bookmark` 为 NamedTuple（title: str，location: Path | URL）；`bookmarks_file()` = `data_directory() / "bookmarks.json"`，`history_file()` = `data_directory() / "history.json"`；`BookmarkEncoder`/`HistoryEncoder`（JSONEncoder 子类）的 `default` 将 `Path`/`URL` 转为 `str`；`load_bookmarks`/`load_history` 以 `URL(location) if is_likely_url(location) else Path(location)` 重建条目；`data_directory()` = `xdg_data_home() / ORGANISATION_NAME / PACKAGE_NAME`（`mkdir(parents=True, exist_ok=True)`）。
- **F-FM-20**：`external/dao/action/Textualize/frogmouth/frogmouth/utility/advertising.py` 与 `utility/type_tests.py`：常量 `ORGANISATION_NAME = "textualize"`、`ORGANISATION_TITLE = "Textualize"`、`ORGANISATION_URL = "https://www.textualize.io/"`、`PACKAGE_NAME = "frogmouth"`、`APPLICATION_TITLE = "Frogmouth"`、`USER_AGENT = f"{PACKAGE_NAME} v{__version__}"`、`DISCORD = "https://discord.gg/Enf6Z3qhVr"`、`TEXTUAL_URL = "https://textual.textualize.io/"`；`maybe_markdown` 为 `@singledispatch`（默认实现返回 False），Path 注册实现返回 `resource.suffix.lower() in load_config().markdown_extensions`，str/URL 注册实现分别转发到 `Path(resource)`/`resource.path`；`is_likely_url(candidate)` 返回 `url.is_absolute_url and url.scheme in ("http", "https")`。
- **F-FM-21**：`external/dao/action/Textualize/frogmouth/frogmouth/utility/forge.py`：`build_raw_forge_url(url_format, owner, repository, branch=None, desired_file=None)` 中 `desired_file = desired_file or "README.md"`，branch 未给定时遍历 `("main", "master")`，以 `client.head(url, follow_redirects=True, headers={"user-agent": USER_AGENT})` 探测，`raise_for_status()` 通过则返回 `URL(url)`，`RequestError` 直接返回 None；四个构建函数对应的 URL 模板分别为 `https://raw.githubusercontent.com/{owner}/{repository}/{branch}/{file}`、`https://gitlab.com/{owner}/{repository}/-/raw/{branch}/{file}`、`https://bitbucket.org/{owner}/{repository}/raw/{branch}/{file}`、`https://codeberg.org/{owner}/{repository}/raw//branch/{branch}/{file}`。
- **F-FM-22**：`external/dao/action/Textualize/frogmouth/frogmouth/widgets/navigation.py` 与 `widgets/navigation_panes/local_files.py`：`Navigation(Vertical, can_focus=False, can_focus_children=True)` 的 DEFAULT_CSS 含 `width: 44; dock: left;`，BINDINGS 为 `comma,a,ctrl+left,shift+left,h→previous_tab`、`full_stop,d,ctrl+right,shift+right,l→next_tab`、`\\→toggle_dock`；reactive `popped_out`/`docked_left`，`watch_popped_out` 切换 "hidden" 类并 post `Hidden` 消息，`watch_docked_left` 设 `styles.dock` 为 left/right；`compose()` 以 `TabbedContent` 容纳 `TableOfContents`/`LocalFiles`/`Bookmarks`/`History` 四个面板；`FilteredDirectoryTree(DirectoryTree).filter_paths` 保留非隐藏（`not path.name.startswith(".")`）目录或 `maybe_markdown(path)` 为真的文件，`PermissionError` 时返回 `[]`；`LocalFiles.compose()` yield `FilteredDirectoryTree(Path("~").expanduser())`。

## 2. toolong（入口与核心模块，F-TL-）

- **F-TL-01**：`external/dao/action/Textualize/toolong/pyproject.toml`：`[tool.poetry]` name="toolong"，version="1.5.0"，description="A terminal log file viewer / tailer / analyzer"，license="MIT"，authors 为 Will McGugan；依赖 `python = "^3.8"`、`click = "^8.1.7"`、`textual = "^0.58.0"`、`typing-extensions = "^4.9.0"`；`[tool.poetry.scripts]` 定义 `tl = "toolong.cli:run"`；build-backend 为 `poetry.core.masonry.api`。
- **F-TL-02**：`external/dao/action/Textualize/toolong/src/toolong/cli.py`：`@click.command()` 函数 `run`，docstring 为 "View / tail / search log files."；装饰器依次为 `@click.version_option(version("toolong"))`、`@click.argument("files", metavar="FILE1 FILE2", nargs=-1)`、`@click.option("-m", "--merge", is_flag=True, help="Merge files.")`、`@click.option("-o", "--output-merge", metavar="PATH", nargs=1, help="Path to save merged file (requires -m).")`；`__main__.py` 导入并调用 `run()`。
- **F-TL-03**：`external/dao/action/Textualize/toolong/src/toolong/cli.py`：`run()` 以 `sys.__stdin__.isatty()` 分流：无文件且 tty → `click.echo(ctx.get_help()); ctx.exit()`；tty 分支构造 `UI(files, merge=merge, save_merge=output_merge).run()`（Exception 被 `except Exception: pass` 吞掉）；非 tty（管道）分支：`tempfile.NamedTemporaryFile(mode="w+b", buffering=0, prefix="tl_")` + `open("/dev/tty", "rb", buffering=0)` + `subprocess.Popen([sys.argv[0], temp_file.name], stdin=tty_stdin, close_fds=True, env={**os.environ, "TEXTUAL_ALLOW_SIGNALS": "1"})`；父进程以 `selectors.SelectSelector()` 监听 stdin，`os.read(sys.stdin.fileno(), 1024 * 64)` 写入临时文件；SIGINT/SIGTERM handler 仅向 stderr 写 "^C"。
- **F-TL-04**：`external/dao/action/Textualize/toolong/src/toolong/ui.py`：模块级执行 `locale.setlocale(locale.LC_ALL, "")`；`UI(App).__init__(file_paths, merge=False, save_merge=None)` 保存 `self.file_paths = self.sort_paths(file_paths)`、`self.merge`、`self.save_merge`、`self.watcher = get_watcher()`；`sort_paths` 以 `CompareTokens` 为 key 排序；`on_mount()` 设 `self.ansi_theme_dark = terminal_theme.DIMMED_MONOKAI`、`push_screen(LogScreen())`、focus `LogLines`、`self.watcher.start()`；`on_unmount()` 调用 `self.watcher.close()`。
- **F-TL-05**：`external/dao/action/Textualize/toolong/src/toolong/ui.py`：`CompareTokens` 标注 `@total_ordering`，`tokens = [int(token) if token.isdigit() else token.lower() for token in path.split("/")[-1].split(".")]`；`__lt__` 逐元素比较，TypeError 时回退 `str(token1) < str(token2)`，最后比较 `len(self.tokens)`；`__eq__` 比较 tokens 列表。
- **F-TL-06**：`external/dao/action/Textualize/toolong/src/toolong/ui.py`：`LogScreen(Screen)` 的 BINDINGS 为 `[Binding("f1", "help", "Help")]`；`compose()` 使用 `TabbedContent`：`self.app.merge and len(file_paths) > 1` 时单一 `TabPane`（标题为 `" + ".join(Path(path).name for path in file_paths)`）内 `Lazy(LogView(file_paths, watcher, can_tail=False))`；否则每个路径一个 `TabPane(path)` 内 `Lazy(LogView([path], watcher, can_tail=True))`；`on_mount()` 设 Tabs 的 display 为 `len(query(TabPane)) > 1` 并 focus 活动面板中的 `LogLines`；`action_help()` push `HelpScreen()`。
- **F-TL-07**：`external/dao/action/Textualize/toolong/src/toolong/log_file.py`：模块常量 `IS_WINDOWS = platform.system() == "Windows"`；定义 `LogError(Exception)`；`LogFile.__init__(path)` 保存 `self.path = Path(path)`、`name`、`file=None`、`size=0`、`can_tail=False`、`timestamp_scanner = TimestampScanner()`、`format_parser = FormatParser()`、`_lock = Lock()`；类标注 `@rich.repr.auto(angular=True)`；`is_compressed` 属性以 `mimetypes.guess_type(self.path.name, strict=False)` 的 encoding 是否属于 `("gzip", "bzip2")` 判定；`parse(line)` 委托 `self.format_parser.parse(line)`。
- **F-TL-08**：`external/dao/action/Textualize/toolong/src/toolong/log_file.py`：`open(exit_event)`：压缩文件 → `open_compressed(exit_event, encoding)`；非压缩 → `open(self.path, "rb", buffering=0)`、`seek(0, os.SEEK_END)`、`size = tell()`、`can_tail = True`；`open_compressed` 使用 `TemporaryFile("wb+")` 与 `chunk_size = 1024 * 256`，gzip/bzip2 分支分别调用 `gzip.open`/`bz2.open`，循环 `read(chunk_size)` 写入临时文件，`exit_event.is_set()` 时关闭并返回 False；完成后 `self.file = temp_file`、`size = temp_file.tell()`、`can_tail = False`；其他 encoding `raise AssertionError("Not supported")`。
- **F-TL-09**：`external/dao/action/Textualize/toolong/src/toolong/log_file.py`：`get_raw(start, end)` 双平台实现：IS_WINDOWS 分支在 `self._lock` 内以 `os.lseek` 保存/恢复位置并 `os.read(self.fileno, end - start)`；非 Windows 分支直接 `os.pread(self.fileno, end - start, start)`；`start >= end or self.file is None` 时均返回 `b""`；`get_line(start, end)` 返回 `get_raw(...).decode("utf-8", errors="replace").strip("\n\r").expandtabs(4)`；`get_create_time()` 读 `stat_result.st_birthtime`，AttributeError 时返回 `datetime.fromtimestamp(0)`。
- **F-TL-10**：`external/dao/action/Textualize/toolong/src/toolong/log_file.py`：`scan_line_breaks(batch_time=0.25)` 创建 mmap（IS_WINDOWS 用 `access=mmap.ACCESS_READ`，否则 `prot=mmap.PROT_READ`），以 `rfind(b"\n", 0, position)` 从文件尾部向前扫描；`log_mmap[-1] != "\n"` 时先把 `position`（即 size）加入 batch；每收集 1000 个且 `time.monotonic() - break_time > batch_time` 时 yield `(position, batch)` 并重置；循环结束 yield `(0, batch)`；`finally` 关闭 mmap；size 为 0 时直接 return。
- **F-TL-11**：`external/dao/action/Textualize/toolong/src/toolong/log_file.py`：`scan_timestamps(batch_time=0.25)` 以 `log_mmap.readline()` 逐行迭代，每行 `decode("utf-8", errors="replace")` 后调用 `self.timestamp_scanner.scan(line)`，累计 `position += len(line_bytes)`，append `(line_no, position, timestamp.timestamp() if timestamp else 0.0)`；每 1000 行且超过 batch_time yield 一次 results，末尾 yield 剩余；mmap 创建方式与 `scan_line_breaks` 相同的平台分支。
- **F-TL-12**：`external/dao/action/Textualize/toolong/src/toolong/timestamps.py`：`TimestampFormat` 为 NamedTuple（regex, parser）；`TIMESTAMP_FORMATS` 列表共 17 项：12 个 ISO-8601 变体（空格或 T 分隔、`,`/`.` 毫秒、可选时区后缀，parser 均为 `datetime.fromisoformat`）、`[JFMASOND][a-z]{2}\s(\s|\d)\d \d{2}:\d{2}:\d{2}`（parser 为 `parse_timestamp("%b %d %H:%M:%S")`）、`\d{2}/\w+/\d{4} \d{2}:\d{2}:\d{2}`（`"%d/%b/%Y %H:%M:%S"`）、`\d{2}/\w+/\d{4}:\d{2}:\d{2}:\d{2} [+-]\d{4}`（`"%d/%b/%Y:%H:%M:%S %z"`）、`\d{10}\.\d+`（`lambda s: datetime.fromtimestamp(float(s))`）、`\d{13}`（`lambda s: datetime.fromtimestamp(int(s))`）；文件内注释标注数据取自 logmerger 项目。
- **F-TL-13**：`external/dao/action/Textualize/toolong/src/toolong/timestamps.py`：`TimestampScanner.__init__` 复制 `TIMESTAMP_FORMATS` 为 `self._timestamp_formats`；`scan(line)`：`len(line) > 10_000` 时截取 `line[:10000]`；按序 `re.search(regex, line)`，`parse_callable(match.group(0))` 返回 None 或抛异常则 continue；命中且 `index` 非 0 时 `del self._timestamp_formats[index : index + 1]` 并 `insert(0, timestamp_format)`；返回 datetime 或 None；模块级 `parse(line)` 函数遍历 TIMESTAMP_FORMATS，返回 `(timestamp_format, datetime)` 或 `(None, None)`。
- **F-TL-14**：`external/dao/action/Textualize/toolong/src/toolong/watcher.py`：`get_watcher()` 在 `platform.system() == "Darwin"` 时返回 `SelectorWatcher()`，否则返回 `PollWatcher()`；`WatchedFile` dataclass 字段为 `log_file`、`callback: Callable[[int, list[int]], None]`、`error_callback: Callable[[Exception], None]`；`WatcherBase(ABC)` 持有 `_file_descriptors: dict[int, WatchedFile]`、`_thread`、`_exit_event = Event()`；classmethod `scan_chunk(chunk, position)` 以 `chunk.find(b"\n", offset)` 循环收集 `position + offset` 列表；`start()` 创建 `Thread(target=self.run, name=repr(self))`；`add()` 以 `log_file.fileno` 为键注册；`run()` 为抽象方法。
- **F-TL-15**：`external/dao/action/Textualize/toolong/src/toolong/poll_watcher.py`：`PollWatcher.run()` 中 `chunk_size = 64 * 1024`；循环遍历 `_file_descriptors`：`lseek(fileno, 0, SEEK_CUR)` 取 position，`read(fileno, chunk_size)` 非空时 `successful_read = True` 并调用 `watched_file.callback(position + len(chunk), breaks)`；异常时 `error_callback(error)`、`self._file_descriptors.pop(fileno, None)` 并 break；一轮无成功读取时 `time.sleep(0.05)`。
- **F-TL-16**：`external/dao/action/Textualize/toolong/src/toolong/selector_watcher.py`：`SelectorWatcher.__init__` 创建 `DefaultSelector()`；`add()` 先调用基类 `add`，再 `os.lseek(fileno, size, os.SEEK_SET)` 并 `self._selector.register(fileno, EVENT_READ)`；`run()` 循环 `self._selector.select(timeout=0.1)`，`mask & EVENT_READ` 时 `os.lseek` 取 position、`os.read(fileno, chunk_size)`（chunk_size = 64 * 1024），chunk 非空则调用 callback；异常时 error_callback、pop fileno 并 `self._selector.unregister(fileno)`；`close()` 仅 set `_exit_event`。
- **F-TL-17**：`external/dao/action/Textualize/toolong/src/toolong/log_view.py`：模块常量 `SPLIT_REGEX = r"[\s/\[\]]"`、`MAX_DETAIL_LINE_LENGTH = 100_000`；`LogView(Horizontal)` 的 BINDINGS：`ctrl+t→toggle_tail`（key_display "^t"）、`ctrl+l→toggle('show_line_numbers')`、`ctrl+f→show_find_dialog`、`slash→show_find_dialog`（show=False）、`ctrl+g→goto`；reactive 变量 `show_find`/`show_panel`/`show_line_numbers`/`tail`/`can_tail`；`compose()` yield `LogLines(watcher, file_paths).data_bind(LogView.tail, LogView.show_line_numbers, LogView.show_find, LogView.can_tail)`、`LinePanel()`、`FindDialog(log_lines._suggester)`、`InfoOverlay().data_bind(LogView.tail)`、`LogFooter().data_bind(LogView.tail, LogView.can_tail)`。
- **F-TL-18**：`external/dao/action/Textualize/toolong/src/toolong/log_view.py`：`action_toggle_tail()` 在 `can_tail` 为假时 `notify("Can't tail merged files", title="Tail", severity="error")`，否则反转 `tail`；`on_scan_complete` 移除 `ScanProgressBar`、设 `log_lines.loading = False`、移除 "-scanning" 类、`self.tail = True`、`footer.mount_keys`；`on_pending_lines` 设 `InfoOverlay.message = f"+{event.count:,} lines"`；`action_goto()` push `GotoScreen(self.query_one(LogLines))`；`update_panel()` 调用 `LogLines.get_text(pointer_line, block=True, abbreviate=True, max_line_length=MAX_DETAIL_LINE_LENGTH)`；`InfoOverlay` 的 tooltip 为 "Click to tail file"，点击 post `TailFile()`。
- **F-TL-19**：`external/dao/action/Textualize/toolong/src/toolong/log_lines.py`：模块常量 `SPLIT_REGEX = r"[\s/\[\]\(\)\"\/]"`、`MAX_LINE_LENGTH = 1000`；`LineReader(Thread)` 持有 `Queue(maxsize=1000)` 与 `pending` 集合，`request_line` 去重入队，`run()` 以 `queue.get(timeout=0.2)` 取请求并 post `LineRead(index, log_file, start, end, log_file.get_line(start, end))`，`stop()` set exit_event 并放入 `(None, -1, 0, 0)` 哨兵；`SearchSuggester(Suggester)` 以 `use_cache=False, case_sensitive=True` 初始化，`get_suggestion` 以 `re.split(SPLIT_REGEX, value)[-1]` 的单词查 `search_index`（键为小写）。
- **F-TL-20**：`external/dao/action/Textualize/toolong/src/toolong/log_lines.py`：`LogLines(ScrollView, inherit_bindings=False)` 的 BINDINGS 共 16 条：`up,w,k→scroll_up`、`down,s,j→scroll_down`、`left,h→scroll_left`、`right,l→scroll_right`、`home,G→scroll_home`、`end,g→scroll_end`、`pageup,b→page_up`、`pagedown,space→page_down`、`enter→select`、`escape→dismiss`（priority=True）、`m/M→navigate(±1, 'm')`、`o/O→navigate(±1, 'h')`、`d/D→navigate(±1, 'd')`；缓存：`_render_line_cache = LRUCache(maxsize=1000)`、`_search_index = LRUCache(maxsize=10000)`、`_line_cache = LRUCache(10000)`、`_text_cache = LRUCache(1000)`；COMPONENT_CLASSES 含 `loglines--filter-highlight`、`loglines--pointer-highlight`、`loglines--line-numbers`、`loglines--line-numbers-active` 4 项。
- **F-TL-21**：`external/dao/action/Textualize/toolong/src/toolong/log_lines.py`：`run_scan(save_merge=None)` 标注 `@work(thread=True)`：`len(log_files) > 1` → `merge_log_files()`（save_merge 非 None 时再 `call_later(self.save, save_merge, self.line_count)`）；单文件 `open` 抛 FileNotFoundError → `notify(f"File {name!r} not found.", severity="error")`；遍历 `scan_line_breaks()` post `ScanProgress(f"Scanning… ({line_count_thousands:,}K lines)- ESCAPE to cancel", 1 - position/size, position)` 与 `NewBreaks(log_file, breaks)`，`worker.is_cancelled` 时 break；最后 post `ScanComplete(size, position)`；`merge_log_files()` 对每个文件 `scan_timestamps()` 收集 `(timestamp, line_no, log_file)` 三元组，对头部缺失时间戳回填（`offset > 10` 停止），最后 `merge_lines.sort(key=itemgetter(0, 1))`。
- **F-TL-22**：`external/dao/action/Textualize/toolong/src/toolong/format_parser.py`：`ParseResult: TypeAlias = "tuple[Optional[datetime], str, Text]"`；`FORMATS = [JSONLogFormat(), CommonLogFormat(), CombinedLogFormat()]`（`DefaultLogFormat()` 一行被注释），模块级 `default_log_format = DefaultLogFormat()`；`HTTP_GROUPS = {"1": "cyan", "2": "green", "3": "yellow", "4": "red", "5": "reverse red"}`；`RegexLogFormat.HIGHLIGHT_WORDS` 含 GET/POST/PUT/HEAD/DELETE/OPTIONS/PATCH（POST 出现两次）；`FormatParser.parse(line)`：`len(line) > 10_000` 截取 `line[:10_000]`，命中格式时轮转列表（`self._formats = [*self._formats[index:], *self._formats[:index]]`），均未命中走 `default_log_format`，仍为 None 时返回 `(None, "", Text())`；`JSONLogFormat.parse` 以 `json.loads(line)` 验证。
- **F-TL-23**：`external/dao/action/Textualize/toolong/src/toolong/highlighter.py`：`LogHighlighter(RegexHighlighter)` 设 `base_style = "repr."`；`highlights` 为 `_combine_regex`（以 `"|"` 连接）组合的命名组正则：`ipv4`、`ipv6`、`eui64`、`eui48`、`uuid`、`bool_true`/`bool_false`/`none`、`number`、`str`、`path`（url 组一行被注释）；`highlight(text)`：`len(text) >= 10_000` 时直接 return，否则对每个 highlight 调用 `text.highlight_regex(re_highlight, style_prefix=self.base_style)`。
- **F-TL-24**：`external/dao/action/Textualize/toolong/src/toolong/messages.py`、`find_dialog.py`、`goto_screen.py`、`help.py`：消息 `SizeChanged`、`PendingLines`、`PointerMoved` 定义 `can_replace`（同类型消息可替换）；`NewBreaks` 字段 `log_file`、`breaks`、`scanned_size=0`、`tail=False`；`ScanProgress` 字段 `message`、`complete`、`scan_start=None`；`FindDialog(Widget, can_focus_children=True)` 的 BINDINGS 含 `escape→dismiss_find`、`down,j→pointer_down`、`up,k→pointer_up`，定义 `Regex(Validator)` 与消息 Update/Dismiss/MovePointer/SelectLine；`GotoScreen(ModalScreen)` 的 BINDINGS 为 `[("escape", "dismiss")]`；`help.py` 定义模块级常量 `HELP_MD` 与 `HelpScreen(ModalScreen)`（compose yield `Markdown(HELP_MD)`）。

## 3. trogon（CLI→TUI 表单生成机制，F-TG-）

- **F-TG-01**：`external/dao/action/Textualize/trogon/pyproject.toml`：`[tool.poetry]` name="trogon"，version="0.6.0"，description="Automatically generate a Textual TUI for your Click CLI"，authors 为 "Darren Burns <darrenb900@gmail.com>"，license="MIT"，homepage="https://github.com/Textualize/trogon"，packages=[{include = "trogon"}]；依赖 `python = "^3.9.1"`、`textual = ">=2.1.2"`、`click = ">=8.0.0"`、`typer = {version = ">=0.9.0", optional = true}`；`[tool.poetry.extras]` typer = ["typer"]；dev 组含 mypy ^1.2.0、black ^24.3.0、pytest >=8.0.0、textual-dev >=1.0；build-backend 为 `poetry.core.masonry.api`。
- **F-TG-02**：`external/dao/action/Textualize/trogon/trogon/__init__.py`：仅 `from trogon.trogon import Trogon, tui` 与 `__all__ = ["tui", "Trogon"]`。`external/dao/action/Textualize/trogon/trogon/constants.py`：`APP_TITLE = "Trogon"`、`PACKAGE_NAME = "trogon"`、`TEXTUAL_URL = "https://github.com/textualize/textual"`、`ORGANIZATION_NAME = "T"`。
- **F-TG-03**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`Trogon(App[None])` 设 `CSS_PATH = Path(__file__).parent / "trogon.scss"`；`__init__(cli: click.Group | click.Command, app_name: str | None = None, command_name: str = "tui", click_context: click.Context | None = None)` 保存 `self.cli = cli`、`self.post_run_command: list[str] = []`、`self.is_grouped_cli = isinstance(cli, click.Group)`、`self.execute_on_exit = False`；`app_name is None and click_context is not None` 时 `self.app_name = detect_run_string()`，否则 `self.app_name = app_name or "cli"`；`get_default_screen()` 返回 `CommandBuilder(self.cli, self.app_name, self.command_name)`。
- **F-TG-04**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`Trogon.run(*args, headless=False, size=None, auto_pilot=None, **kwargs)` 在 `try: super().run(...) finally:` 中：`self.post_run_command` 非空时创建 `Console()`，`post_run_command and execute_on_exit` 时 `console.print(f"Running [b cyan]{self.app_name} {' '.join(shlex.quote(s) for s in self.post_run_command)}[/]")`，随后 `split_app_name = shlex.split(self.app_name)`、`program_name = shlex.split(self.app_name)[0]`、`arguments = [*split_app_name, *self.post_run_command]`、`os.execvp(program_name, arguments)`；`@on(CommandForm.Changed) update_command_to_run` 以 `include_root_command = not self.is_grouped_cli` 调 `event.command_data.to_cli_args(include_root_command)` 赋给 `post_run_command`；`action_focus_command_tree()` 查询 `CommandTree`（`NoMatches` 时 return）后 focus；`action_show_command_info()` push `CommandInfo(command_builder.selected_command_schema)`；`action_visit(url)` 调用 `webbrowser.open`（导入名 `open_url`）。
- **F-TG-05**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`tui(name: str | None = None, command: str = "tui", help: str = "Open Textual TUI.")` 返回 decorator；内部 `wrapped_tui` 标注 `@click.pass_context`，函数体为 `Trogon(app, app_name=name, command_name=command, click_context=ctx).run()`；`isinstance(app, click.Group)` 时执行 `app.command(name=command, help=help)(wrapped_tui)` 并返回 app；否则创建 `new_group = click.Group()`、`new_group.add_command(app)`、`new_group.command(name=command, help=help)(wrapped_tui)` 并返回 `new_group`。
- **F-TG-06**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`CommandBuilder(Screen[None])` 的 `COMPONENT_CLASSES = {"version-string", "prompt", "command-name-syntax"}`；BINDINGS 5 条：`ctrl+r→close_and_run`（"Close & Run"）、`ctrl+t→app.focus_command_tree`、`ctrl+o→app.show_command_info`、`ctrl+s→app.focus('search')`、`f1→about`；`__init__(cli: click.BaseCommand, click_app_name, command_name, name, id, classes)` 设 `self.command_data = UserCommandData(CommandName("_default"))`、`self.command_schemas = introspect_click_app(cli)`、`self.is_grouped_cli = isinstance(cli, click.Group)`，`try: self.version = metadata.version(self.click_app_name) except Exception: self.version = None`，`self.highlighter = ReprHighlighter()`。
- **F-TG-07**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`CommandBuilder.compose()`：`tree = CommandTree("Commands", self.command_schemas, self.command_name)`；`title_parts = [Text(self.click_app_name, style="b")]`，version 存在时追加 `"\n"` 与 `(f"v{self.version}", version_style)`；`sidebar = Vertical(Label(title, id="home-commands-label"), tree, id="home-sidebar")`；`is_grouped_cli` 时 `tree.focus()`，否则 `sidebar.display = False`；yield sidebar 后 `Vertical(id="home-body")` 内含 `Horizontal(id="home-command-description-container")`（`can_focus=False`，子为 `Static(self.click_app_name or "", id="home-command-description")`）、`VerticalScroll(Static(""), id="home-body-scroll")`（`can_focus=False`）、`Horizontal(NonFocusableVerticalScroll(Static("", id="home-exec-preview-static"), id="home-exec-preview-container"), id="home-exec-preview")`（`Button.success("Close & Run", id="home-exec-button")` 代码块被注释）；最后 yield `Footer()`。
- **F-TG-08**：`external/dao/action/Textualize/trogon/trogon/trogon.py`：`action_close_and_run()` 设 `self.app.execute_on_exit = True` 后 `self.app.exit()`；`action_about()` push `AboutDialog()`；`@on(Tree.NodeHighlighted) selected_command_changed` 调用 `_refresh_command_form(event.node)`；`@on(CommandForm.Changed) update_command_data` 设 `self.command_data = event.command_data` 后 `_update_execution_string_preview()`；`_update_command_description(command)` 取 `command.docstring or ""` 并 lstrip，拼为 `f"[b]{command.name}[/]\n{description_text}"` 更新 `#home-command-description`；`_update_execution_string_preview()` 以 `Text(f"{self.click_app_name} ", command_name_syntax_style)` 为前缀、`self.command_data.to_cli_string(include_root_command=False)` 经 `self.highlighter` 高亮，再以 `("$ ", prompt_style)` 拼成预览写入 `#home-exec-preview-static`；`_update_form_body(node)` 移除 `#home-body-scroll` 全部子节点后 `parent.mount(CommandForm(command_schema=node.data, command_schemas=self.command_schemas))`，非 grouped cli 时 `command_form.focus()`。
- **F-TG-09**：`external/dao/action/Textualize/trogon/trogon/introspect.py`：`generate_unique_id()` 返回 `f"id_{str(uuid.uuid4())[:8]}"`；`MultiValueParamData` dataclass 字段 `values: list[tuple[int | float | str]]`；静态方法 `process_cli_option(value)`：None → `MultiValueParamData([])`；tuple → `MultiValueParamData([value])`；list → 逐项 `(item,) if not isinstance(item, tuple) else item`；其他 → `MultiValueParamData([(value,)])`。
- **F-TG-10**：`external/dao/action/Textualize/trogon/trogon/introspect.py`：`OptionSchema` dataclass 字段：`name: list[str]`、`type: ParamType`、`default: MultiValueParamData | None = None`、`required: bool = False`、`is_flag: bool = False`、`is_boolean_flag: bool = False`、`flag_value: Any = ""`、`opts: list[str]`、`counting: bool = False`、`secondary_opts: list[str]`、`key: str | tuple[str] = field(default_factory=generate_unique_id)`、`help: str | None = None`、`choices: Sequence[str] | None = None`、`multiple: bool = False`、`multi_value: bool = False`、`nargs: int = 1`；`__post_init__` 设 `self.multi_value = isinstance(self.type, click.Tuple)`。`ArgumentSchema` dataclass 字段：`name: str`、`type: str`、`required: bool = False`、`key`（default_factory=generate_unique_id）、`default: MultiValueParamData | None = None`、`choices`、`multiple: bool = False`、`nargs: int = 1`。
- **F-TG-11**：`external/dao/action/Textualize/trogon/trogon/introspect.py`：`CommandSchema` dataclass 字段：`name: CommandName`、`function: Callable[..., Any | None]`、`key`（default_factory=generate_unique_id）、`docstring: str | None = None`、`options: list[OptionSchema]`、`arguments: list[ArgumentSchema]`、`subcommands: dict[CommandName, CommandSchema]`、`parent: CommandSchema | None = None`、`is_group: bool = False`；`path_from_root` 属性沿 `parent` 链向上收集后 `list(reversed(path))`；模块末行 `CommandName = NewType("CommandName", str)`。
- **F-TG-12**：`external/dao/action/Textualize/trogon/trogon/introspect.py`：`introspect_click_app(app: BaseCommand)` 内部 `process_command(cmd_name, cmd_obj, parent=None)` 构造 `CommandSchema(name=cmd_name, docstring=cmd_obj.help, function=cmd_obj.callback, options=[], arguments=[], subcommands={}, parent=parent, is_group=isinstance(cmd_obj, click.Group))`；遍历 `cmd_obj.params`：`isinstance(param, (click.Option, click.core.Group))` → `OptionSchema(name=param.opts, type=param.type, is_flag=param.is_flag, is_boolean_flag=param.is_bool_flag, flag_value=param.flag_value, counting=param.count, opts=param.opts, secondary_opts=param.secondary_opts, required=param.required, default=default, help=param.help, multiple=param.multiple, nargs=param.nargs)`，`isinstance(param.type, click.Choice)` 时 `option_data.choices = param.type.choices`；`isinstance(param, click.Argument)` → `ArgumentSchema(name=param.name, type=param.type, required, multiple, default, nargs)` 同理附 choices；`isinstance(cmd_obj, click.core.Group)` 时递归 `cmd_obj.commands`（`parent=cmd_data`）；顶层：`isinstance(app, click.Group)` → `data[CommandName("root")] = process_command(CommandName("root"), app)`，`isinstance(app, click.Command)` → `data[CommandName(app.name)] = process_command(...)`。
- **F-TG-13**：`external/dao/action/Textualize/trogon/trogon/run_command.py`：`UserOptionData` dataclass 字段 `name: str | list[str]`、`value: tuple[Any]`、`option_schema: OptionSchema`，`string_name` 属性返回 `self.name`（str）或 `self.name[0]`；`UserArgumentData` 字段 `name: str`、`value: tuple[Any]`、`argument_schema: ArgumentSchema`；`UserCommandData` 字段 `name: CommandName`、`options: list[UserOptionData]`、`arguments: list[UserArgumentData]`、`subcommand: UserCommandData | None = None`、`parent: UserCommandData | None = None`、`command_schema: CommandSchema | None = None`。
- **F-TG-14**：`external/dao/action/Textualize/trogon/trogon/run_command.py`：`to_cli_args(include_root_command=False)` 调 `_to_cli_args()`，`not include_root_command` 时 `cli_args = cli_args[1:]`；`_to_cli_args()` 以 `args = [self.name]` 开始；`multiple` 选项按 `option.string_name` 收入 `defaultdict(list) multiples` 与 `multiples_schemas`；非 multiple 选项：`value_data = MultiValueParamData.process_cli_option(option.value).values`，`default_data = option.option_schema.default.values`（None 时为 `[tuple()]`），`flattened_values/flattened_defaults` 经 `sorted(itertools.chain.from_iterable(...))`，`values_supplied = any(value != ValueNotSupplied() ...)`，`values_are_defaults = list(map(str, flattened_values)) == list(map(str, flattened_defaults))`；`values_supplied and not values_are_defaults` 时输出：选项名选择——`counting` 取 `min(option.name, key=len)`，否则 `max(option.name, key=len)`；`is_true_bool = value_data == [(True,)]`；is_flag 且 True → append 选项名，False 且 secondary_opts 存在 → append `max(secondary_opts, key=len)`；非 flag 非 counting → append 选项名后逐 `subvalue_tuple` extend；counting → `count = next(itertools.chain.from_iterable(value_data), 1)`，int 转换 ValueError 时置 1，`count = max(1, min(count, 5))`，`option_name.startswith("--")` → `[option_name] * count`，否则 `f"-{option_name.lstrip('-') * count}"`。
- **F-TG-15**：`external/dao/action/Textualize/trogon/trogon/run_command.py`：multiples 循环：`defaults = multiples_schemas[option_name].default or []`，`supplied_defaults`/`supplied_values` 过滤 `ValueNotSupplied()` 后经 `map(str)` 与 `sorted` 比较，`values_supplied and not values_are_defaults` 时逐条 `args.append(option_name)` 并 extend 值（跳过全为 `ValueNotSupplied()` 的元组）；arguments 循环逐值 `argument_value != ValueNotSupplied()` 时 append；`self.subcommand` 存在时 `args.extend(self.subcommand._to_cli_args())`；`to_cli_string(include_root_command=False)` 对 `to_cli_args` 结果逐项 `Text(shlex.quote(str(arg)))`，`ValueNotSupplied()` 替换为 `Text("???", style="bold black on red")`，以 `Text(" ").join(text_renderables)` 拼接。
- **F-TG-16**：`external/dao/action/Textualize/trogon/trogon/detect_run_string.py`：`get_orig_argv()` 有 `sys.orig_argv` 时直接返回，否则经 `ctypes.pythonapi.Py_GetArgcArgv(ctypes.byref(_argc), ctypes.byref(_argv))` 取 `_argv[: _argc.value]`；`detect_run_string(_main: ModuleType = sys.modules["__main__"])` docstring 标注 "a slightly modified version of a function from Click"；`path = sys.argv[0]`；`getattr(_main, "__package__", None) is None` 或（`os.name == "nt"` 且 `__package__ == ""` 且 `not os.path.exists(path)` 且 `os.path.exists(f"{path}.exe")`）时：`file_path = shlex.quote(os.path.basename(path))`，`argv[0] == "python"` 时 prefix 为 `f"{argv[0]} "`，否则 `""`，返回 `f"{prefix}{file_path}"`；否则模块模式：`py_module = _main.__package__`，`name = os.path.splitext(os.path.basename(path))[0]`，`name != "__main__"` 时 `py_module = f"{py_module}.{name}"`，返回 `f"python -m {py_module.lstrip('.')}"`。
- **F-TG-17**：`external/dao/action/Textualize/trogon/trogon/typer.py`：`try: import typer except ImportError: raise ImportError("The extra `trogon[typer]` is required to enable tui generation from Typer apps.")`；`init_tui(app: typer.Typer, name: str | None = None)` 定义 `wrapped_tui()` 执行 `Trogon(typer.main.get_group(app), app_name=name, click_context=click.get_current_context()).run()`，以 `app.command("tui", help="Open Textual TUI.")(wrapped_tui)` 注册并返回 app。
- **F-TG-18**：`external/dao/action/Textualize/trogon/trogon/widgets/form.py`：`CommandForm(Widget)` docstring "Form which is constructed from an introspected Click app. Users make use of this form in order to construct CLI invocation strings."；DEFAULT_CSS 定义 `.command-form-heading`/`.command-form-input`/`.command-form-label`/`.command-form-checkbox`/`.command-form-command-group`/`.command-form-control-help-text` 六组样式；消息 `Changed(command_data: UserCommandData)`；`__init__` 保存 `command_schema`、`command_schemas`、`first_control: ParameterControls | None = None`；`compose()` 以 `iter(reversed(self.command_schema.path_from_root))` 遍历命令链：先 yield `Input(placeholder="Search...", classes="command-form-filter-input", id="search")`；每个含 options/arguments 的节点以 `Vertical(classes="command-form-command-group", id=command_node.key)` 包裹，`is_inherited = command_node is not self.command_schema`，`border_title = f"{'↪ ' if is_inherited else ''}{command_node.name}"`，inherited 时追加 `" [dim not bold](inherited)"`；arguments 段 yield `Label("Arguments", classes="command-form-heading")` 后逐个 `ParameterControls(argument, id=argument.key)`，options 段同构；首个控件记入 `self.first_control`。
- **F-TG-19**：`external/dao/action/Textualize/trogon/trogon/widgets/form.py`：`on_mount`/`on_input_changed`/`on_select_changed`/`on_checkbox_changed`/`on_multiple_choice_changed` 均调用 `_form_changed()`；`_form_changed()` 以哨兵 `UserCommandData(name=CommandName("_"), options=[], arguments=[])` 起步，对 `path_from_root` 中每个命令：`self.query_one(f"#{option.key}", ParameterControls).get_values()` 逐元组构造 `UserOptionData(option.name, v, option)`，arguments 同理构造 `UserArgumentData`，逐层构造 `UserCommandData(name, options, arguments, parent=parent_command_data, command_schema=command)` 并链 `parent_command_data.subcommand = command_data`；末尾 `root_command_data = root_command_data.subcommand`、`root_command_data.parent = None`、`self.post_message(self.Changed(root_command_data))`；`focus()` 转发 `self.first_control.focus()`；`@on(Input.Changed, ".command-form-filter-input") apply_filter` 将 `event.value` casefold 后对每个 `ParameterControls` 调 `control.apply_filter(filter_query)`。
- **F-TG-20**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`ControlWidgetType = Union[Input, Checkbox, MultipleChoice, Select[str]]`；`ControlGroup(Vertical)` 与 `ControlGroupsContainer(Vertical)` 为空子类；`ValueNotSupplied` 标注 `@functools.total_ordering`：`__eq__` 返回 `isinstance(other, ValueNotSupplied)`，`__lt__` 返回 False，`__bool__` 返回 False。
- **F-TG-21**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`ParameterControls(Widget).__init__(schema: ArgumentSchema | OptionSchema, ...)` 保存 `self.schema` 与 `self.first_control`；`apply_filter(filter_query)`：`help_text = getattr(self.schema, "help", "") or ""`；空查询 → 可见；`name` 为 str 时 `filter_query in name.casefold()`；为 list 时 `any(filter_query in name.casefold() for name in self.schema.name)` 或 `filter_query in help_text.casefold()`；设置 `self.display`；help 文本存在时 `Text(help_text).highlight_words(filter_query.split(), "black on yellow", case_sensitive=False)` 更新 `.command-form-control-help-text`（`NoMatches` 时 pass）；返回 `should_be_visible`。
- **F-TG-22**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`compose()` 读取 `schema.name/type/default/help/multiple/nargs`，`is_option = isinstance(schema, OptionSchema)`，label 经 `_make_command_form_control_label(name, argument_type, is_option, schema.required, multiple=multiple)`；`ControlGroupsContainer()` 内：`argument_type != click.BOOL` 时 yield `Label(label, classes="command-form-label")`；`isinstance(argument_type, click.Choice) and multiple` → 经 `get_control_method(argument_type)` 生成单个 `MultipleChoice`（参数 `default, label, multiple, schema, control_id=schema.key`）并 `yield from`；否则对 `default.values` 每个元组：`make_widget_group()` 生成控件组，`ControlGroup` 内 `zip(default_value_tuple, widget_group)` 逐个 `_apply_default_value` 后 yield，单控件组加 "single-item" 类；`multiple or not default.values` 时额外渲染一组无默认值控件；`self.first_control` 记录首个控件；`(multiple or nargs == -1) and not isinstance(argument_type, click.Choice)` 时 yield `Horizontal(classes="add-another-button-container")` 内 `Button("+ value", variant="success", classes="add-another-button")`；`help_text` 非空时 yield `Static(help_text, classes="command-form-control-help-text")`。
- **F-TG-23**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`make_widget_group()`：`parameter_types = parameter_type.types if isinstance(parameter_type, click.Tuple) else [parameter_type]`，逐类型 `get_control_method(_type)(default, label, multiple, schema, schema.key)` 并 `yield from`；`get_control_method(argument_type)`：`text_click_types = {click.STRING, click.FLOAT, click.INT, click.UUID}`，`text_types = (click.Path, click.File, click.IntRange, click.FloatRange, click.types.FuncParamType)`，命中 → `make_text_control`；`argument_type == click.BOOL` → `make_checkbox_control`；`isinstance(argument_type, click.types.Choice)` → `partial(make_choice_control, choices=argument_type.choices)`；其余 → `make_text_control`。
- **F-TG-24**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`make_text_control` yield `Input(classes=f"command-form-input {control_id}")`；`make_checkbox_control`：`default = default.values[0][0] if default.values else ValueNotSupplied()`，yield `Checkbox(label, button_first=True, classes=f"command-form-checkbox {control_id}", value=default)`；`make_choice_control`：`isinstance(schema.type, click.Tuple)` 时 `multiple = False`；multiple → `MultipleChoice(choices, classes=f"command-form-multiple-choice {control_id}", defaults=default.values)`，否则 `Select[str]([(choice, choice) for choice in choices], classes=f"{control_id} command-form-select")`；`_make_command_form_control_label` 以 `Text.from_markup` 拼接名称（多名称以 `Text(" / ", style="dim")` 连接）、`{' multiple' if multiple else ''} {type.name}`、required 时 `" [b red]*[/]required"`，`IntRange/FloatRange` 追加 `min=`/`max=` dim 文本。
- **F-TG-25**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`_apply_default_value(control_widget, default_value)`：Input → `value = str(default_value)`、`placeholder = f"{default_value} (default)"`；Select → `value = str(default_value)`、`prompt = f"{default_value} (default)"`；`_get_form_control_value(control)`：MultipleChoice → `control.selected`；Select → `control.value is None or control.value is Select.BLANK` 时 `ValueNotSupplied()`，否则 `control.value`；Input → 空串时 `ValueNotSupplied()`，否则 `control.value`；Checkbox → `control.value`。
- **F-TG-26**：`external/dao/action/Textualize/trogon/trogon/widgets/parameter_controls.py`：`get_values()` 内 `list_to_tuples(lst, tuple_size)`：`tuple_size == 0` → `[tuple()]`，`-1` → 按 1 处理，其余按 `tuple(lst[i:i+tuple_size])` 分块；`controls = list(self.query(f".{self.schema.key}"))`；单一 MultipleChoice → `MultiValueParamData.process_cli_option(self._get_form_control_value(control))`；否则逐控件收集值后 `list_to_tuples(collected_values, self.schema.nargs)` 再 `process_cli_option`；`@on(Button.Pressed, ".add-another-button") add_another_widget_group`：`make_widget_group()` 后 `widget_group[0].focus()`，`ControlGroup(*widget_group)`（≤1 个控件加 "single-item"），mount 到 `ControlGroupsContainer`，`event.button.scroll_visible(animate=False)`。
- **F-TG-27**：`external/dao/action/Textualize/trogon/trogon/widgets/command_tree.py`：`CommandTree(Tree[CommandSchema])` 的 `COMPONENT_CLASSES = {"group"}`；`__init__(label, cli_metadata, command_name)` 设 `show_root = False`、`guide_depth = 2`、`show_guides = False`；`render_label` 返回 `node._label.copy()` 并 `stylize(style)`；`on_mount` 内 `build_tree(data, node)`：按键排序遍历，`cmd_name == self.command_name` 时跳过；有 subcommands 时 label 为 `Text(cmd_name)`，`is_group` 时叠加 "group" 组件样式并 `label.append("group", "dim i")`，`node.add(label, allow_expand=False, data=cmd_data)` 后递归；否则 `node.add_leaf(cmd_name, data=cmd_data)`；末尾 `self.root.expand_all()` 与 `self.select_node(self.root)`。
- **F-TG-28**：`external/dao/action/Textualize/trogon/trogon/widgets/command_info.py`：`CommandMetadata(DataTable)` 设 `show_header = False`、`zebra_stripes = True`、`cursor_type = "none"`；`on_mount` `add_columns("Key", "Value")` 并 add_rows 六行：Name、Parent（`getattr(schema.parent, "name", "No parent")`）、Subcommands（`list(schema.subcommands.keys())`）、Group（`schema.is_group`）、Arguments（`len(schema.arguments)`）、Options（`len(schema.options)`）；`CommandInfo(ModalScreen)` 的 `COMPONENT_CLASSES = {"title", "subtitle"}`，BINDINGS 为 `Binding("q,escape", "close_modal", "Close Modal")`；compose 以 `" ➜ ".join(command.name for command in path)` 拼路径，`Tabs(Tab("Description", id="command-info-text"), Tab("Metadata", id="command-info-metadata"), classes="command-info-tabs")` + `ContentSwitcher(initial="command-info-text", id="command-info-switcher")`，描述为 `docstring.strip()` 或 "No description available"；`@on(Tabs.TabActivated) switch_content` 设 `ContentSwitcher.current = event.tab.id`；`action_close_modal()` 调 `self.app.pop_screen()`。
- **F-TG-29**：`external/dao/action/Textualize/trogon/trogon/widgets/multiple_choice.py`：`NonFocusableVerticalScroll(VerticalScroll, can_focus=False)`；`MultipleChoice(Widget)` 的 DEFAULT_CSS 为 `border: round #666; width: auto; height: auto;` 与 `:focus-within { border: round $primary-lighten-2; }`；BINDINGS：`down,right→next_button`、`up,left→previous_button`（均 show=False）；`__init__(options: list[TextType], defaults=None)`：defaults 为 None 时置 `[()]`，`self.selected = defaults`；消息 `Changed(selected: list[Checkbox])`；compose 在 `NonFocusableVerticalScroll` 内逐选项 yield `Checkbox(option, value=(option,) in self.defaults)`；`@on(Checkbox.Changed) checkbox_toggled` 收集 `checkbox.value is True` 的控件，`self.selected = [(checkbox.label.plain,) for checkbox in selected]` 并 post `Changed`；`action_next_button`/`action_previous_button` 在首尾 Checkbox 处回绕 focus。
- **F-TG-30**：`external/dao/action/Textualize/trogon/trogon/widgets/about.py`：`TextDialog(ModalScreen[None])` 的 BINDINGS 为 `Binding("escape", "dismiss(None)", "", show=False)`；`button_style` 属性返回 `"primary"`；compose 为 `Vertical` 内 `Center` 包 `Static(self._title, classes="spaced")`、`Static(self._message, id="message", classes="spaced")`、`Center(classes="spaced")` 内 `Button("OK", variant=self.button_style)`；`on_mount` focus Button；`on_button_pressed` 调 `self.dismiss(None)`；`AboutDialog(TextDialog)` 的 DEFAULT_CSS 为 `TextDialog > Vertical { border: thick $primary 50%; }`，title 为 `f"About Trogon"`，message 经 `Text.from_markup` 含 `[@click=app.visit('https://github.com/textualize/textual')]Textual[/]`、`[@click=app.visit('https://textualize.io')]Textualize[/]` 与 `https://github.com/textualize/{constants.PACKAGE_NAME}` 三个链接。

## 4. rich-cli（命令行参数与渲染，F-RC-）

- **F-RC-01**：`external/dao/action/Textualize/rich-cli/pyproject.toml`：`[project]` name="rich-cli"，version="1.8.1"，description="Command Line Interface to Rich"，license="MIT"，authors 为 Will McGugan <willmcgugan@gmail.com>，urls.homepage="https://github.com/Textualize/rich-cli"，requires-python=">=3.9"；dependencies：`rich (>=12.4.0,<13.0.0)`、`click (>=8.0.0,<9.0.0)`、`requests (>=2.0.0,<3.0.0)`、`textual (>=0.1.18,<0.2.0)`、`rich-rst (>=1.1.7,<2.0.0)`；`[project.scripts]` `rich = "rich_cli.__main__:run"`；`[project.entry-points."pipx.run"]` `rich-cli = "rich_cli.__main__:run"`；build-backend 为 `poetry.core.masonry.api`；classifiers 列 Python 3.9–3.13。
- **F-RC-02**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：模块级 `console = Console()`、`error_console = Console(stderr=True)`；`BOXES = ["none", "ascii", "ascii2", "square", "rounded", "heavy", "double"]`；`BOX_TEXT = ", ".join(sorted(BOXES))`；`COMMON_LEXERS = {"html": "html", "py": "python", "md": "markdown", "js": "javascript", "xml": "xml", "json": "json", "toml": "toml"}`；`VERSION = "1.8.0"`。`src/rich_cli/__init__.py` 为空文件。
- **F-RC-03**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：格式常量 `AUTO = 0`、`SYNTAX = 1`、`PRINT = 2`、`MARKDOWN = 3`、`RST = 4`、`JSON = 5`、`RULE = 6`、`INSPECT = 7`、`CSV = 8`、`IPYNB = 9`。
- **F-RC-04**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`on_error(message: str, error: Optional[Exception] = None, code=-1) -> NoReturn`：error 存在时 `error_text = Text(message)` 经 `stylize("bold red")` 后拼 `": "` 与 `error_console.highlighter(str(error))`，经 `error_console.print` 输出；否则 `Text(message, style="bold red")`；最后 `sys.exit(code)`。
- **F-RC-05**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`read_resource(path, lexer)` URL 分支：`path.startswith(("http://", "https://"))` 时 `requests.get(path)`，`text = response.text`；`mime_type = response.headers["Content-Type"]`，含 ";" 时取首段，KeyError 时 pass；lexer 未给定时先以 `path.rpartition(".")` 的扩展名（小写）查 `COMMON_LEXERS`，仍为 None 时 `from pygments.lexers import get_lexer_for_mimetype` 调 `get_lexer_for_mimetype(mime_type).name`（Exception 时 pass）；返回 `(text, lexer)`。
- **F-RC-06**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`read_resource` 文件分支：空 path → `on_error("missing path or URL")`；`path == "-"` → `(sys.stdin.read(), None)`；`open(path, "rt", encoding="utf8", errors="replace")` 读取；lexer 未给定时先按扩展名查 `COMMON_LEXERS`，仍未给定时 `guess_lexer_for_filename(path, text).name`，`ClassNotFound` → `return (text, "text")`；外层 Exception → `on_error(f"unable to read {escape(path)}", error)`。
- **F-RC-07**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`ForceWidth(renderable, width: int = 80)` 的 `__rich_console__` 以 `options.update_width(self.width)` 得 child_options 后 `yield from console.render(self.renderable, child_options)`，`__rich_measure__` 返回 `Measurement(self.width, self.width)`；`blend_text(message, color1, color2)` 对每字符按 `blend = index / size` 线性插值 RGB 并以 `f"#{...:02X}{...:02X}{...:02X}"` 逐字符 `text.stylize(color, index, index + 1)`。
- **F-RC-08**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`RichCommand(click.Command)` docstring "Override Clicks help with a Richer version."；`format_help` 内定义 `OptionHighlighter(RegexHighlighter)`，highlights 为 `r"(?P<switch>\-\w)"` 与 `r"(?P<option>\-\-[\w\-]+)"`；`Console(theme=Theme({"option": "bold cyan", "switch": "bold green"}), highlighter=highlighter)`；打印 `[b]Rich CLI[/b] [magenta]v{VERSION}[/] 🤑\n\n[dim]Rich text and formatting in the terminal\n`（justify="center"）与 Usage 行；`options_table = Table(highlight=True, box=None, show_header=False)` 遍历 `self.get_params(ctx)[1:]`，metavar 以 "bold yellow" 附加，help 经 `Text.from_markup(..., emoji=False)`；以 `Panel(options_table, border_style="dim", title="Options", title_align="left")` 输出；末尾 `blend_text("♥ https://www.textualize.io", Color.parse("#b169dd").triplet, Color.parse("#542c91").triplet)`（justify="left"，style="bold"）。
- **F-RC-09**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`main` 命令装饰器（一）：`@click.command(cls=RichCommand)`；`@click.argument("resource", metavar="<PATH or TEXT or '-'>", default="")`；格式开关（均 is_flag=True）：`--print/-p`（参数名 `_print`）、`--rule/-u`、`--json/-J`、`--markdown/-m`、`--rst`、`--csv`、`--ipynb`、`--syntax`、`--inspect`。
- **F-RC-10**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`main` 命令装饰器（二）：`--head/-h` 与 `--tail/-t`（`type=click.IntRange(min=1)`，metavar="LINES"，default=None，help 标注 "requires --syntax or --csv"）；`--emoji/-j`、`--left/-l`、`--right/-r`、`--center/-c`、`--text-left/-L`、`--text-right/-R`、`--text-center/-C`、`--text-full/-F`（均 is_flag）；`--soft`（help 标注 "requires --print"）；`--expand/-e`（help 标注 "requires --panel"）；`--width/-w`（type=int，default=-1）；`--max-width/-W`（type=int，default=-1）。
- **F-RC-11**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`main` 命令装饰器（三）：`--style/-s`（default=""）、`--rule-style`（default="bright_green"）、`--rule-char`（default="─"）、`--padding/-d`（metavar="TOP,RIGHT,BOTTOM,LEFT"）、`--panel/-a`（`type=click.Choice(BOXES)`，default="none"，metavar="BOX"）、`--panel-style/-S`（default=""）、`--theme`（default="ansi_dark"，`envvar="RICH_THEME"`）、`--line-numbers/-n`、`--guides/-g`、`--lexer/-x`（default=None）、`--hyperlinks/-y`、`--no-wrap`、`--title`（default=""）、`--caption`（default=""）、`--force-terminal`、`--export-html/-o`（default=""）、`--export-svg`（default=""）、`--pager`、`--version/-v`。
- **F-RC-12**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`main()` 函数签名中 `inspect: bool = True`、`no_wrap: bool = True`，docstring "Rich toolbox for console output."；`version` 为真时 `sys.stdout.write(f"{VERSION}\n")` 后 return；`console = Console(emoji=emoji, record=bool(export_html or export_svg), force_terminal=force_terminal if force_terminal else None)`；`print_usage()` 打印 Usage 与 "See [bold green]rich --help[/] for options" 后 `sys.exit(0)`；`width > 0` 时 `expand = True`；padding 解析为整数列表，长度不属于 (1, 2, 4) 时 `on_error("padding should be 1, 2 or 4 integers separated by commas")`。
- **F-RC-13**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`resource_format` 初值 AUTO，分派顺序：`_print→PRINT`、`syntax→SYNTAX`、`json→JSON`、`markdown→MARKDOWN`、`rule→RULE`、`inspect→INSPECT`、`csv→CSV`、`rst→RST`、`ipynb→IPYNB`；AUTO 且 `"." in resource` 时：URL 经 `urlparse(resource).path` 后 `os.path.splitext`，否则直接 `os.path.splitext(resource)`，扩展名 `.md→MARKDOWN`、`.json→JSON`、`.csv/.tsv→CSV`、`.rst→RST`、`.ipynb→IPYNB`；仍为 AUTO 时 `resource_format = SYNTAX`。
- **F-RC-14**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：PRINT/RULE 分支：justify 依 `text_left/text_right/text_center/text_full` 取 "left"/"right"/"center"/"full"，否则 "default"；`resource == "-"` 时 `Text.from_markup(sys.stdin.read(), justify=justify, emoji=emoji)`，否则 `Text.from_markup(resource, ...)`；`renderable.no_wrap = no_wrap`；Exception → `on_error("unable to parse console markup", error)`；rule 时 `Style.parse(rule_style)`（失败 → `on_error("unable to parse rule style", error)`），`Rule(resource, style=render_rule_style, characters=rule_char or "─", align="center" if justify in ("full", "default") else justify)`。
- **F-RC-15**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：JSON 分支：`from rich.json import JSON as RichJSON`，`json_data, _lexer = read_resource(resource, lexer)`，`RichJSON(json_data)`（Exception → `on_error("unable to read json", error)`）；MARKDOWN 分支：`from .markdown import Markdown`，`markdown_data, lexer = read_resource(resource, lexer)`，`Markdown(markdown_data, code_theme=theme, hyperlinks=hyperlinks)`；RST 分支：`from rich_rst import RestructuredText`，`RestructuredText(rst_data, code_theme=theme, default_lexer=lexer or "python", show_errors=False)`。
- **F-RC-16**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：INSPECT 分支：`inspect_data = eval(resource)`，Exception → `console.print_exception()` + `on_error(f"unable to eval {resource!r}")`，`Inspect(inspect_data, help=False, dunder=False, all=False, methods=True)`；CSV 分支调 `render_csv(resource, head, tail, title, caption)`；IPYNB 分支调 `render_ipynb(resource, theme, hyperlinks, lexer, head, tail, line_numbers, guides, no_wrap)`；SYNTAX（else）分支：resource 为空 → `print_usage()`；`"-"` → `sys.stdin.read()`，否则 `read_resource`；`num_lines = len(code.splitlines())`，`line_range = _line_range(head, tail, num_lines)`，`Syntax(code, lexer, theme=theme, line_numbers=line_numbers, indent_guides=guides, word_wrap=not no_wrap, line_range=line_range)`；Exception → `on_error("unable to read file", error)`。
- **F-RC-17**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：渲染后处理：`print_padding` 非空 → `Padding(renderable, tuple(print_padding), expand=expand)`；`panel != "none"` → `Style.parse(panel_style)`（失败 → `on_error("unable to parse panel style", error)`），`Panel(renderable, getattr(box, panel.upper()), expand=expand, title=title, subtitle=caption, border_style=render_border_style)`；`style` 非空 → `Style.parse(style)`（失败 → `on_error("unable to parse style", error)`）后 `Styled(renderable, text_style)`；`width > 0 and not pager` → `ForceWidth(renderable, width=width)`。
- **F-RC-18**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：输出阶段：justify 依 `left/right/center` 取值；pager 分支：justify 非 "default" 时 `Align(renderable, justify)`，`width < 0` 时 `width = console.width`，`render_options = console.options.update(width=width - 1)`，`lines = console.render_lines(renderable, render_options, new_lines=True)`，`PagerApp.run(title=resource, content=PagerRenderable(lines, width=width))`；非 pager 分支：`console.print(renderable, width=None if max_width <= 0 else max_width, soft_wrap=soft, justify=justify)`（Exception → `on_error("failed to print resource", error)`）；`export_html` → `console.save_html(export_html, clear=False)`（失败 → "failed to save HTML"）；`export_svg` → `console.save_svg(export_svg, clear=False)`（失败 → "failed to save SVG"）。
- **F-RC-19**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`render_csv(resource, head, tail, title, caption)`：`is_number = re.compile(r"\-?[0-9]*?\.?[0-9]*?").fullmatch`；`csv_data, _ = read_resource(resource, "csv")`；`sniffer = csv.Sniffer()`，`dialect = sniffer.sniff(csv_data[:1024], delimiters=",\t|;")`，`has_header = sniffer.has_header(csv_data[:1024])`；`csv.Error` 时：`.csv` 结尾 → `csv.get_dialect("excel")` 且 has_header=True，`.tsv` 结尾 → `csv.get_dialect("excel-tab")`，否则 `on_error(str(error))`；`Table(show_header=has_header, box=box.HEAVY_HEAD if has_header else box.SQUARE, border_style="blue", title=title, caption=caption, caption_justify="right")`；header 行逐列 `add_column`；`head` → `table_rows[:head]`，`tail` → `table_rows[-tail:]`；逐列全为数字时设 `justify="right"`、`style="bold green"`、`header_style="bold green"`。
- **F-RC-20**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`render_ipynb(...)`：`notebook_str, _ = read_resource(resource, None)` 后 `json.loads`；`lexer = lexer or notebook_dict.get("metadata", {}).get("kernelspec", {}).get("language", "")`；遍历 `notebook_dict["cells"]`：`execution_count` 存在时 append `f"[green]In [[#66ff00]{execution_count}[/#66ff00]]:[/green]"`；`source = "".join(cell["source"])`；code 单元 → `Panel(Syntax(source, lexer, theme=theme, line_numbers=line_numbers, indent_guides=guides, word_wrap=not no_wrap, line_range=_line_range(head, tail, num_lines)), border_style="dim")`；markdown 单元 → `Markdown(source, code_theme=theme, hyperlinks=hyperlinks)`；其余 → `Text(source)`；outputs 分派：`"stream"` → `Text.from_ansi("".join(output["text"]))`；`"error"` → `Text.from_ansi("\n".join(output["traceback"]).rstrip())`；`"execute_result"` → `Text.from_markup(f"[red]Out[[#ee4b2b]{execution_count}[/#ee4b2b]]:[/red]\n")` 拼 `output["data"].get("text/plain", "")` 的 `Text.from_ansi`；返回 `Group(*cells)`。
- **F-RC-21**：`external/dao/action/Textualize/rich-cli/src/rich_cli/__main__.py`：`_line_range(head, tail, num_lines)`：head 与 tail 同时给定 → `on_error("cannot specify both head and tail")`；head → `(1, head)`；tail → `(num_lines - tail + 2, num_lines + 1)`；否则 None；`run()` 函数体为 `main()`；`if __name__ == "__main__": run()`。
- **F-RC-22**：`external/dao/action/Textualize/rich-cli/src/rich_cli/markdown.py`：`CodeBlock(TextElement)` 设 `style_name = "markdown.code_block"`；`create(cls, markdown, node)` 取 `node_info = node.info or ""`、`lexer_name = node_info.partition(" ")[0]`，返回 `cls(lexer_name or "default", markdown.code_theme)`；`__rich_console__` 以 `code = str(self.text).rstrip()` 构造 `Syntax(code, self.lexer_name, theme=self.theme, word_wrap=True)` 并 `yield Padding(syntax, (0, 4))`；模块末行 `Markdown.elements["code_block"] = CodeBlock`。
- **F-RC-23**：`external/dao/action/Textualize/rich-cli/src/rich_cli/pager.py`：`PagerRenderable(lines, new_lines: bool = False, width: int = 80)` 的 `__rich_console__` 逐行 yield segments（`new_lines` 时每行后追加 `Segment.line()`），`__rich_measure__` 返回 `Measurement(self.width, self.width)`；`PagerApp(App)` 的 `__init__` 保存 `content`；`on_load` 绑定 `"q"→"quit"`（"Quit"）；`on_key`：`j→body.scroll_up()`、`k→body.scroll_down()`、`space→body.page_down()`、`ctrl+u→target_y -= size.height // 2` 后 `animate("y", target_y, easing="out_cubic")`、`ctrl+d→target_y += size.height // 2` 同动画；`on_mount`：`body = ScrollView(auto_width=True)`，`await self.view.dock(body)`、`await body.focus()`、`await body.update(self.content)`。
- **F-RC-24**：`external/dao/action/Textualize/rich-cli/src/rich_cli/win_vt.py`：`__all__ = ["enable_windows_virtual_terminal_processing"]`；`WINDOWS = platform.system() == "Windows"`；Windows 分支：`_STD_OUTPUT_HANDLE = -11`、`_ENABLE_VIRTUAL_TERMINAL_PROCESSING = 0x0004`、`_KERNEL32 = ctypes.WinDLL("kernel32", use_last_error=True)`；`_get_console_mode()` 经 `GetConsoleMode` 读取，`_set_console_mode(mode)` 经 `SetConsoleMode(_STD_OUTPUT_HANDLE, mode | _ENABLE_VIRTUAL_TERMINAL_PROCESSING)`；`enable_windows_virtual_terminal_processing()` contextmanager 保存当前模式、置位、yield、成功时恢复；非 Windows 分支为空操作 contextmanager。

## 条数统计

| 仓库 | 前缀 | 条数 |
|---|---|---|
| frogmouth | F-FM- | 22 |
| toolong | F-TL- | 24 |
| trogon | F-TG- | 30 |
| rich-cli | F-RC- | 24 |
| **合计** | | **100** |
